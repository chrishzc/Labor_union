/**
 * File: subsidy_report_query_adapter.ts
 * Description: 將server補助報表映射為唯讀view且不重算公式或aggregates。
 */
import type { SubsidyReportPreview } from '../../api/reports/subsidy_report_query_schemas';
export function adaptSubsidyReport(source: SubsidyReportPreview) { return { kind: source.period_kind, year: source.application_year, quarter: source.quarter, generatedAt: source.generated_at, revision: source.source_revision, totalRows: source.total_row_count, totalAmount: `NT$ ${source.total_amount_ntd.toLocaleString()}`, partitions: source.partitions.map((partition) => ({ kind: partition.citizen_kind, rowCount: partition.row_count, totalAmount: `NT$ ${partition.total_amount_ntd.toLocaleString()}`, rows: partition.rows.map((row) => ({ serial: row.serial_number, caseNo: row.case_no, eligibility: row.eligibility, serviceRange: `${row.service_start} ~ ${row.service_end}`, subsidyHours: String(row.subsidy_hours), subsidyDays: String(row.subsidy_days), serviceDays: row.service_days, amount: `NT$ ${row.subsidy_amount_ntd.toLocaleString()}`, unitPrice: `NT$ ${row.unit_price_ntd.toLocaleString()}`, employer: row.employer_name_masked, staff: row.staff_name_masked, identity: row.identity_card_masked, address: row.address_masked })) })) }; }
