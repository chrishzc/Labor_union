{
  "contract": "labor-union-verification-scenario/v1",
  "track": "B",
  "suite_id": "PERF",
  "scenario_id": "UI-ORD-BLOCK-001",
  "status": "specified",
  "requires_database": true,
  "test_kinds": ["process_network_harness"],
  "coverage_ids": ["PERF-UI-ORD-BLOCK-001"],
  "source_refs": ["document/架構重整/01_規格基線/21_Contract_Signing_Commitment與正常驗收資料鏈正式規格.md#Contract Signing"],
  "root_facts": ["A canonical Case Import order root that has not completed staff commitment or client signing."],
  "forbidden_direct_seed": ["Direct creation of Contract Signing identity, signing documents, events or commitment records."],
  "commands": ["Open the order through the UI and inspect contract blockers without applying a contract mutation."],
  "receipt_requirements": ["The scenario receipt records the blocked lifecycle state and absent contract artifacts."],
  "fixture": "validation/datasets/dataset_v1_foundation.json",
  "verifier": "scripts/verify_contract_signing_dataset.py",
  "expected": [
    "Case Import creates canonical order roots only.",
    "Before staff commitment and client signing, the contract identity, signing documents, events and commitment are absent.",
    "The order remains 洽談中 so the UI must expose contract blockers rather than execution data."
  ]
}
