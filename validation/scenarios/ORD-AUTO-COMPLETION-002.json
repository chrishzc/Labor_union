{
  "contract": "labor-union-verification-scenario/v1",
  "scenario_id": "ORD-AUTO-COMPLETION-002",
  "track": "A",
  "suite_id": "ORD",
  "status": "bound",
  "requires_database": true,
  "database_execution_mode": "persistent_append_only",
  "test_kinds": ["domain_root_data", "expected_manifest"],
  "coverage_scope": "supplemental",
  "coverage_ids": ["OSM-03"],
  "source_refs": [
    "document/資料庫、資料處理/新版測試資料規則矩陣_草案.md#OSM-03",
    "document/資料庫、資料處理/新版測試資料規則矩陣_草案.md#5.7"
  ],
  "root_facts": [
    "A disposable in-service Order, effective assignment-owned official work day and authoritative payment/payroll terms, scoped to the auto-completion transition boundary.",
    "Fixed timezone-aware evaluation instants, expected aggregate versions, idempotency identities and actor identities."
  ],
  "forbidden_direct_seed": [
    "completed order status or lifecycle version",
    "completion lifecycle event, command claim, receipt or Orders outbox intent"
  ],
  "commands": [
    "Apply one minute before the authoritative completion instant.",
    "Apply at the completion instant and replay the identical request.",
    "Apply a fresh idempotency key with the stale pre-completion version."
  ],
  "expected": [
    "The early command returns auto_completion_time_not_reached with zero claim, event, receipt and outbox row delta.",
    "The completion command derives 訂單完成/lifecycle version 1 and creates exactly one claim, event, receipt and outbox intent.",
    "Identical replay returns the stored receipt; stale version returns order_version_conflict with no additional writes."
  ],
  "receipt_requirements": [
    "root fixture and expected-manifest digests",
    "early and stale typed errors with row snapshots",
    "completed order state and event/receipt/outbox counts",
    "idempotency replay equality"
  ],
  "execution": {
    "runner": [
      ".\\.venv\\Scripts\\python.exe -m pytest -W error tests\\test_order_auto_completion_persistent_mysql_e2e.py"
    ],
    "test_paths": ["tests/test_order_auto_completion_persistent_mysql_e2e.py"]
  }
}
