{
  "contract": "labor-union-verification-scenario/v1",
  "scenario_id": "GERR-TYPED-UI-RETRY-002",
  "track": "B",
  "suite_id": "GERR",
  "status": "blocked",
  "blocker": "The existing FinancePage now retains the first submitted reason and correlation for replay. The real-client React regression covers original-command replay, Preview switching, queued/running without false completion, and terminal receipts. Runtime verification of this revision is still pending; source-function ablation is not a substitute for the React and retained API runners.",
  "requires_database": false,
  "test_kinds": ["process_network_harness"],
  "coverage_ids": ["GERR-01", "GERR-02", "GERR-03", "GERR-05"],
  "source_refs": [
    "document/架構重整/01_規格基線/00_Global_共同契約.md#2. 跨 Domain 不變量",
    "document/架構重整/01_規格基線/00_Global_共同契約.md#4. Typed errors",
    "document/架構重整/01_規格基線/19_Global_Entry_Point_Governance.md#3"
  ],
  "root_facts": [
    "Typed HTTP responses for unauthorized, forbidden, conflict and unavailable outcomes.",
    "A lost-response unavailable error, an idempotency identity and a durable job status response."
  ],
  "forbidden_direct_seed": [
    "UI decision, typed transport category, retry identity or completed job display"
  ],
  "commands": [
    "Map typed server errors through the UI transport client.",
    "Retry an unavailable apply with unchanged operation state and refresh the durable job status.",
    "Map a database contention and stale version error through the order API boundary."
  ],
  "expected": [
    "Typed clients preserve category, code and correlation identity without branching on human message text.",
    "Unavailable retry reuses the original idempotency key; changed UI input does not silently alter the retry command.",
    "The UI displays queued work before it displays completion, while contention is HTTP 503 and stale version is HTTP 409."
  ],
  "receipt_requirements": [
    "typed transport assertions",
    "idempotency-key replay evidence",
    "queued-to-succeeded UI state assertions",
    "409/503 mapping assertions"
  ],
  "execution": {
    "runner": [
      ".\\.venv\\Scripts\\python.exe -m pytest -W error tests\\domains\\orders\\subsystems\\orders\\integration\\test_order_auto_completion_api.py",
      "npm --prefix ui_react test -- src/tests/challenger_g2_orders_client_resilience.test.ts src/tests/orders_mutation_flow_store.test.ts src/tests/finance_import_mutation_client.test.ts src/tests/finance_query_page.test.tsx"
    ],
    "test_paths": [
      "tests/domains/orders/subsystems/orders/integration/test_order_auto_completion_api.py",
      "ui_react/src/tests/challenger_g2_orders_client_resilience.test.ts",
      "ui_react/src/tests/orders_mutation_flow_store.test.ts",
      "ui_react/src/tests/finance_import_mutation_client.test.ts",
      "ui_react/src/tests/finance_query_page.test.tsx"
    ]
  }
}
