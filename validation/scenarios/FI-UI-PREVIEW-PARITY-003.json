{
  "contract": "labor-union-verification-scenario/v1",
  "scenario_id": "FI-UI-PREVIEW-PARITY-003",
  "track": "A",
  "suite_id": "FI",
  "status": "blocked",
  "blocker": "An append-only real workbook/API/MySQL test now passes its same-run Preview output through the real React client and panel, but no configured-database execution receipt has been verified. Normal React CI skips that same-run case when its Python-produced exchange is absent; the current MySQL CI job runs historical cancellation only. Preserve blocked until the full runner passes; mocked React tests alone do not prove zero-formal-ledger deltas.",
  "requires_database": true,
  "database_execution_mode": "persistent_append_only",
  "test_kinds": ["external_input_fixture", "expected_manifest"],
  "coverage_scope": "supplemental",
  "coverage_ids": ["FI-01", "FSM-01"],
  "source_refs": [
    "document/架構重整/01_規格基線/00_Global_共同契約.md#2. 跨 Domain 不變量",
    "document/架構重整/01_規格基線/19_Global_Entry_Point_Governance.md#3"
  ],
  "root_facts": [
    "A unique external Taishin workbook source and the API client credentials used only by the test application boundary."
  ],
  "forbidden_direct_seed": [
    "batch preview, UI session state, classification result, formal ledger or success display"
  ],
  "commands": [
    "Ingest the workbook through the Finance Import API, request Preview through the typed UI API client and render it into the panel state.",
    "Request the same Preview through the authoritative server application and compare typed values."
  ],
  "expected": [
    "UI client preview version, fingerprint, blocker list and counts exactly match the authoritative server Preview.",
    "Ingest and Preview may create canonical import roots, but make no client ledger, staff payout or government-subsidy formal transaction."
  ],
  "receipt_requirements": [
    "root fixture and expected-manifest digests",
    "API/client/panel versus server Preview parity assertion",
    "zero-formal-ledger delta assertion"
  ],
  "execution": {
    "runner": [
      "python -m pytest -W error tests/test_finance_import_staging_persistent_mysql_e2e.py -k test_workbook_api_preview_reaches_react_without_formal_posting"
    ],
    "test_paths": [
      "tests/test_finance_import_staging_persistent_mysql_e2e.py",
      "ui_react/src/tests/finance_query_page.test.tsx"
    ]
  }
}
