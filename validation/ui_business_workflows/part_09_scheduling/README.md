# Part 09 — Scheduling current query, leave/substitution, and holiday policy

覆蓋 `SCH-REACT-ADMIN-CURRENT-QUERY`、`SCH-REACT-ADMIN-LEAVE-SUBSTITUTION` 與 `SCH-REACT-ADMIN-HOLIDAY-POLICY`。流程依序觀察 current query，再驗證 linked leave/substitution 的 server outcome，最後驗證 policy horizon。Part 只保存操作語意與 oracle，不授權 mutation 或資料庫套用。
