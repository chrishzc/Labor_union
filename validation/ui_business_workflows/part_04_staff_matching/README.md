# Part 04 — Staff matching safe actions

覆蓋 `SCH-REACT-ADMIN-STAFF-SAFE-ACTIONS` 的 staff summary、可用性與明確不可用 mutation 控制項。此 Part 只驗證真實 typed query 與 server blocker 的可觀察結果，不驗證 Staff 主檔建立、編輯或 payroll projection。
