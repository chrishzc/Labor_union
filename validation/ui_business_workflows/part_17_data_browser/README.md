# Part 17 — Data Browser masked query

Canonical semantic identity為`part-data-browser`，owner為Data Browser allowlisted、server-masked Query／typed detail
與immutable source lineage顯示。scenario為`GDATA-REACT-ADMIN-DATA-BROWSER-QUERY`，React entry為
`ui-react:#data-browser`，rollback identity為`data-browser`（legacy `ui:01_data_browser.py`）。raw row、任意table／SQL、
source correction、production entry switch與retirement不在本Part授權範圍。
