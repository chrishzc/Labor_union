# 正式規格收斂出口

## 狀態

本目錄的無損合併稿、狀態機盤點與逐表欄位盤點自 2026-08-03 起定位為：

- `historical-source`
- `source-analysis`
- `field-lineage-source`

它們保留來源文字、hash、衝突與 live 漂移，不再作為可直接施工或授權的 SSOT。

## 正式出口

本輪收斂後，已於 2026-08-03 經人工整體確認的正式入口是：

1. `document/架構重整/15_正式規格索引與裁決總表.md`
2. `document/架構重整/16_Staff_Payables與Client_Refund正式規格.md`
3. `document/架構重整/17_External_Integration_LINE_Access正式規格.md`
4. `document/架構重整/18_Global_Deployment與治理正式規格.md`

既有 Orders、Scheduling、Payroll、Client Finance、Staff Payables、Anomalies、
Finance Import、Migration、Performance 與 Government Subsidy 規格，仍由
`document/架構重整/README.md` 索引。

## 已收斂衝突

- 人工月結 aggregate 退出，改以 payable obligation／ledger／Query／XLSX。
- 一般客戶退款成為 Client Finance 正式目標能力，與 receipt reversal 分離；
  production 完整閉環目前仍為 `partial`。
- BreezySign 只保存外部合約證據，不得直接改訂單日期、狀態或排班。
- LINE 只擁有平台 identity、inbox、delivery、review、publication 與 media。
- 管理權限採 `AdminPrincipal + operation capability`；internal key 不等於人類授權。
- 部署固定 logical security topology；`local-primary` 僅為 `recommended-candidate`，
  profile、provider、RTO／RPO 與營運 owner 均待 deployment decision record 人工決定。
- ADAD／Checkpoint／Source Lock／system map gate 全部降為 historical。

## 尚需人工決定

- production edge／tunnel provider、正式網域及 private admin access；
- RTO／RPO、備份保存期與營運責任；
- BreezySign 官方 signature／event contract；
- LINE Rich Menu 雙人覆核；
- session／audit／break-glass 操作政策。

缺少上述決定時，相應 production capability 必須 fail closed，不得用舊稿、
mock、fallback 或 live bypass 自行補值。

## 本次核准範圍

- 只授權建立 Inventory v2；
- 不修改或移除 production code 或 pytest；
- 不修改正式 DB、credentials、network 或 external platform；
- 不把本目錄文字中的「已實作」當成 live 已驗證；
- 不刪除來源稿或逐表盤點；
- 不使用過期治理語彙授權任何動作。
