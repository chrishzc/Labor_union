# Table: finance_import_rows

## 1. 核心定位與職責
- 領域分類：07_財務匯入與警示
- 類型：原始資料暫存 (Staging)
- 已確認跨表裁決：儲存從會計上傳的財務 Excel 中萃取出的原始列資料，作為冪等性處理與錯誤追溯的依據。
- 已確認帳務邊界：本表保存原始銀行事實，即使金額無法完整核銷也必須保留；但不得因此建立部分正式交易。只有核銷 Preview 能使每筆所選金流與每筆所選義務都同時歸 0，才可原子建立下游正式交易與分配。未打平列留在本表並進入財務異常／人工處理。
- 已確認共同核銷識別：同一次原子核銷 Apply 選取的所有列，成功後須寫入相同 `reconciliation_reference`。此欄只作同批追蹤與冪等，不代表外部流水號，也不參與任何應收／應付計算；現況服務有時寫入單列 fingerprint 或外部 reference，屬待重整漂移。
- Schema：`db/schema_parts/60_finance_import_staging.sql`
- 主要 writer：`services/finance_import_staging.py`、`services/finance_import_reprocessing.py`，以及各正式核銷 Service。
- 子表關係：每次實際匯入位置由 `finance_import_occurrences.finance_import_row_id` 關聯；正式交易與警示亦可引用本表。
- 第一遍（live 現況）：已補齊 35 個 Schema 欄位。
- 第二遍（衍生判定）：已完成。
- 根事實展開：已完成。

## 2. 欄位權威性與計算邏輯

| 欄位 | Schema 定義 | 現況架構／用途 | 初步分類 | 現況公式 | 直接來源 | 第一層根事實 | 建議權威公式 | 修改命令／Owner | 重算觸發 | 凍結邊界 | 現況漂移／風險 | 裁決 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | `BIGINT AUTO_INCREMENT PRIMARY KEY` | canonical 銀行流水技術主鍵。 | 系統鍵 | DB 自增。 | 成功 INSERT。 | DB 接受的 canonical row。 | 僅供識別與關聯，不參與金額、分類或核銷計算。 | DB／Finance Import Staging | 無 | 建立後不變 | 無。 | 已確認 |
| `dedup_fingerprint` | `CHAR(64) NOT NULL UNIQUE` | 跨批次辨識同一銀行流水的 canonical 去重鍵。 | 衍生技術識別 | 對 bank source、來源帳號、交易日期時間、方向、借貸額、餘額、摘要、備註與取消碼正規化後做 SHA-256。 | `build_dedup_fingerprint(normalized_row)`。 | 正規化銀行欄位。 | 保留作 DB 技術去重鍵；只能由受控 fingerprint service 依固定公式產生，不得由檔名、批次、DB id 或 caller 指定，也不得作金額、外部流水號或核銷依據。若未來改變指紋欄位集合，必須先完成明確的整體版本遷移；目前不增加逐列 `fingerprint_version`。 | Finance Import Staging | 每一正規化列匯入前 | canonical row 建立後不變 | 指紋欄位集合若未經整體遷移就改變，同一銀行流水可能被建立成第二筆 canonical row。 | 已確認：保留固定公式的衍生技術唯一鍵 |
| `batch_id` | `BIGINT NULL` | 首次建立 canonical row 的相容批次。 | 遺留關聯／長期考慮移除 | 新 canonical row 建立時取當次 batch id；重複匯入不更新。 | 首次 `finance_import_batches.id`。 | 首次出現位置；完整出現歷史在 `finance_import_occurrences.batch_id`。 | 新流程不得作批次歸屬來源；批次查詢一律讀 occurrence。既有值歷史唯讀，長期考慮移除。 | 無新 owner | 停止更新 | 歷史唯讀 | 只能表達首次批次，會漏掉後續重複匯入。 | 已確認：歷史相容、長期考慮移除 |
| `format_id` | `ENUM('legacy','taishin','sinopac') NOT NULL` | 此 canonical 銀行列採用的對帳資料來源格式：歷史紀錄格式、台新對帳單格式或永豐對帳單格式。 | 正規化來源格式快照 | 由 workbook normalizer 依輸入來源與版型判定。fingerprint 的銀行來源正規化中，`legacy` 與 `sinopac` 均映射為永豐 `sinopac`；`taishin` 保持台新。 | `normalized_row.format_id`。 | 被解析的對帳資料來源與格式；`legacy` 雖為歷史格式，其銀行來源仍屬永豐。 | 保留為輸入來源格式的不可變快照，用來選擇／追查解析規則；不得人工修改，也不得單獨取代來源帳號、交易對手或帳務歸屬。`legacy → sinopac` 僅是 fingerprint 的正確銀行來源正規化，不表示兩種輸入格式相同。目前不新增 `bank_id` 或 `format_version`。 | Finance Normalizer／Import Staging | 首次建立 canonical row | 建立後不變 | caller 若把本欄直接當成純銀行 enum，會忽略 `legacy` 是獨立輸入格式但銀行來源仍為永豐。 | 已確認：三種來源格式；legacy 在 fingerprint 映射為 sinopac |
| `source_file` | `VARCHAR(1024) NULL` | 首次出現檔案路徑的相容快照。 | 遺留來源位置快照／長期考慮移除 | 不計算。 | 首次 `normalized_row.source_file`。 | 首次 occurrence 的來源檔；完整來源歷史在 `finance_import_occurrences.source_file`。 | 新流程停止將本欄當作檔案來源；所有匯入來源查詢一律讀 occurrences。既有值歷史唯讀，長期考慮移除。 | 無新 owner | 停止更新 | 歷史唯讀 | 只保留首次檔案，重複匯入後無法代表完整來源，且與 occurrence 重複。 | 已確認：歷史相容、長期考慮移除 |
| `source_bank_account` | `VARCHAR(191) NULL` | 銀行流水所屬的工會來源帳號。 | 正規化來源事實 | parser 依檔案格式擷取／正規化，並作為 fingerprint 輸入之一。 | `normalized_row.source_bank_account`。 | 銀行原始檔中的工會帳號資訊。 | 保留作原始銀行來源事實與去重公式輸入；若台新等來源格式未提供則為 NULL，不得以預設值、交易對手帳號或下游帳務結果反推。正式交易表需要顯示時應透過原始銀行列取得，不再複製另一份來源帳號。 | Finance Import Staging | 首次建立 canonical row | 建立後不變 | parser 規則未受控變更會改變 fingerprint；NULL 不代表可人工補值。 | 已確認：保留銀行根事實與 fingerprint 輸入 |
| `sheet_name` | `VARCHAR(191) NULL` | 首次出現工作表名稱的相容快照。 | 遺留來源位置快照／長期考慮移除 | 不計算。 | 首次 `normalized_row.sheet_name`。 | 首次 occurrence 的工作表；完整來源歷史在 `finance_import_occurrences.sheet_name`。 | 新流程停止將本欄當作來源工作表；所有匯入位置查詢一律讀 occurrences。既有值歷史唯讀，長期考慮移除。 | 無新 owner | 停止更新 | 歷史唯讀 | 只保留首次工作表，重複匯入後無法代表完整來源，且與 occurrence 重複。 | 已確認：歷史相容、長期考慮移除 |
| `source_row` | `INT UNSIGNED NULL` | 首次出現的一基底 Excel 列號。 | 遺留來源位置快照／長期考慮移除 | 不計算。 | 首次 `normalized_row.source_row`。 | 首次 occurrence 的原始列位置；完整來源歷史在 `finance_import_occurrences.source_row`。 | 新流程停止將本欄當作來源列號；所有匯入位置查詢一律讀 occurrences。既有值歷史唯讀，長期考慮移除。 | 無新 owner | 停止更新 | 歷史唯讀 | 只保留首次列號，重複匯入後無法代表完整來源，且與 occurrence 重複。 | 已確認：歷史相容、長期考慮移除 |
| `source_reference` | `VARCHAR(191) NULL` | 未啟用的通用銀行參考值預留欄位。 | 停用欄位／長期考慮移除 | 現行 validator 強制為 NULL。 | 無新來源；銀行原始參考欄位已保存在 `bank_references` 與 `raw_payload`。 | 具原始欄名的銀行參考資料。 | 新流程維持停止寫入並長期考慮移除；不得任選交易序號、存摺備註或其他銀行欄位寫入這個無類型字串。需要參考值時讀取具欄名的 `bank_references`。 | 無新 owner | 停用 | 維持 NULL；既有值僅供歷史讀取 | 欄位無法表達參考值類型，且與結構化銀行參考資料重複。 | 已確認：停止寫入、長期考慮移除 |
| `transaction_date` | `DATE NULL` | 銀行記錄的交易發生日。 | 正規化來源事實 | parser 正規化為 ISO 日期。 | `normalized_row.transaction_date`。 | 銀行原始交易日期欄。 | 保留作正式金流事件發生日的銀行根事實；原檔未提供時允許 NULL，不得以 `posting_date`、`value_date`、匯入時間、請款期別或帳務月份代填。訂單請款與月嫂薪資月份仍由各自領域規則決定。 | Finance Import Staging | 首次建立 canonical row | 建立後不變 | 三種銀行日期並存，caller 若 fallback 或混用會改變事件時間及跨月判斷。 | 已確認：保留銀行交易發生日，不允許日期 fallback |
| `transaction_time` | `TIME NULL` | 銀行提供的交易時間。 | 正規化來源事實 | parser 正規化為 ISO 時間，並作為 fingerprint 輸入之一。 | `normalized_row.transaction_time`。 | 銀行原始交易時間欄。 | 保留作銀行根事實與去重輸入；原檔未提供時維持 NULL，不得以匯入時間、建立時間或 `00:00:00` 代填。 | Finance Import Staging | 首次建立 canonical row | 建立後不變 | 同日同額交易需要本欄協助區分；人工補預設時間反而可能造成錯誤碰撞。 | 已確認：保留銀行交易時間，不允許時間 fallback |
| `posting_date` | `DATE NULL` | 銀行入帳／登帳日期；台新來源為帳務日期，永豐／歷史格式來源為入帳日。 | 正規化來源事實 | parser 正規化為 ISO 日期。 | `normalized_row.posting_date`。 | 銀行原始帳務日期／入帳日欄。 | 保留作銀行稽核與顯示事實；不得取代 `transaction_date`，也不得驅動訂單請款期別、月嫂薪資月份或其他帳務歸屬。 | Finance Import Staging | 首次建立 canonical row | 建立後不變 | 目前 production 下游未用本欄；若未區分日期語意而新增依賴，會造成跨月歸屬漂移。 | 已確認：保留稽核顯示，不驅動帳務歸屬 |
| `value_date` | `DATE NULL` | 銀行起息／價值日期；永豐與歷史格式來源為計息日，台新目前不提供。 | 正規化來源事實 | parser 正規化為 ISO 日期；台新格式為 NULL。 | `normalized_row.value_date`。 | 銀行原始計息日欄。 | 保留作銀行稽核與顯示資料；原檔未提供時維持 NULL，不得取代 `transaction_date`，也不得驅動帳務月份、請款期別、薪資月份或付款狀態。 | Finance Import Staging | 首次建立 canonical row | 建立後不變 | 目前 production 下游未用本欄；若未來誤用會把銀行計息語意混成業務歸屬日期。 | 已確認：保留稽核顯示，不驅動業務狀態 |
| `debit` | `DECIMAL(18,2) NULL` | 銀行支出金額。 | 正規化來源事實／金額根事實 | parser 轉為非負 Decimal；沒有支出時為 NULL。 | `normalized_row.debit`。 | 銀行借方／支出欄。 | 保留作 outgoing 金額根事實與 fingerprint 輸入；不得由 `credit`、月嫂應付、退款義務或其他計算結果反推。原始列一律保存，但只有同次所選支出與所選義務都精確歸 0時才能建立正式交易。 | Finance Import Staging | 首次建立 canonical row | 建立後不變 | debit 有值不等於核銷成功；若下游直接依本欄寫正式交易，會繞過完整打平門禁。 | 已確認：保留支出金額根事實；正式核銷須精確打平 |
| `credit` | `DECIMAL(18,2) NULL` | 銀行收入金額。 | 正規化來源事實／金額根事實 | parser 轉為非負 Decimal；沒有收入時為 NULL。 | `normalized_row.credit`。 | 銀行貸方／收入欄。 | 保留作 incoming 金額根事實與 fingerprint 輸入；不得由 `debit`、客戶應收、補助應收或其他計算結果反推。原始列一律保存，但只有同次所選收入與所選義務都精確歸 0時才能建立正式交易。 | Finance Import Staging | 首次建立 canonical row | 建立後不變 | credit 有值不等於核銷成功；若下游直接依本欄寫正式交易，會繞過完整打平門禁。 | 已確認：保留收入金額根事實；正式核銷須精確打平 |
| `direction` | `ENUM('incoming','outgoing','unknown') NOT NULL` | 此銀行流水的收支方向。 | 衍生投影／長期考慮移除 | 僅 `credit > 0` → incoming；僅 `debit > 0` → outgoing；兩者同時為正或皆無正值 → unknown 並附對應 warning。 | `debit`、`credit`。 | 銀行借方與貸方金額根事實。 | 不具獨立權威性；分類、顯示與 fingerprint 應透過同一純公式由 `debit／credit` 推導，不得由分類、應收或應付結果反推。過渡期保留實體欄位並由 validator 強制一致，長期考慮移除。 | Finance Normalizer／共用 direction calculator | 金額正規化 | 過渡期隨 canonical row 凍結 | 實體 enum 與金額形成兩套答案；任何繞過 validator 的 writer 都可能造成漂移。 | 已確認：衍生投影，長期考慮移除 |
| `balance` | `DECIMAL(18,2) NULL` | 銀行對帳單當列顯示的帳戶餘額。 | 正規化來源事實 | parser 轉為 Decimal；原檔未提供時為 NULL。 | `normalized_row.balance`。 | 銀行原始餘額欄。 | 保留作銀行來源事實、稽核資料與 fingerprint 輸入；不得由前後匯入列、正式交易或帳務結果重算後回寫，因匯入範圍與排序不保證涵蓋完整帳戶流水。 | Finance Import Staging | 首次建立 canonical row | 建立後不變 | 將它視為系統可重建餘額會在缺列、跨檔或排序不同時產生錯誤；銀行更正版亦可能改變 fingerprint。 | 已確認：保留銀行餘額根事實，不自行重算 |
| `currency` | `VARCHAR(16) NULL` | 對帳單列提供的幣別欄位；目前匯入來源均為台幣帳戶。 | 正規化來源事實 | parser 忠實擷取／正規化對帳單欄位；台新格式未提供時為 NULL。 | `normalized_row.currency`。 | 銀行原始幣別欄。 | 保留作對帳單來源事實；只保存原檔內容，不另做多幣別換算或狀態機。原檔未提供時維持 NULL，不因帳戶為台幣而回填 `TWD`。 | Finance Import Staging | 首次建立 canonical row | 建立後不變 | 若將本欄當成需要額外推算或多幣別流程，會偏離「忠實保存對帳單欄位」的定位。 | 已確認：保留原始欄位；來源帳戶均為台幣 |
| `summary` | `TEXT NULL` | 對帳單提供的銀行交易摘要。 | 正規化來源事實 | parser 忠實擷取／正規化對帳單欄位。 | `normalized_row.summary`。 | 銀行原始摘要欄。 | 保留作銀行來源事實與 fingerprint 輸入；只保存對帳單內容，不由分類、核銷或帳務結果回寫。 | Finance Import Staging | 首次建立 canonical row | 建立後不變 | 正規化規則未受控變更會影響 fingerprint。 | 已確認：保留對帳單來源欄位 |
| `memo` | `TEXT NULL` | 對帳單提供的銀行交易備註。 | 正規化來源事實 | parser 忠實擷取；分類器只能讀取，不得修改。 | `normalized_row.memo`。 | 銀行原始備註欄。 | 保留作銀行來源事實、fingerprint 與受控分類輸入；不得由分類或正式帳務結果反向改寫。 | Finance Import Staging | 首次建立 canonical row | 建立後不變 | 同時參與 fingerprint 與分類，規則變更須維持原始欄位不變。 | 已確認：保留對帳單來源欄位 |
| `counterparty_name` | `VARCHAR(255) NULL` | 對帳單提供的交易對手名稱。 | 正規化來源事實 | parser 忠實擷取／正規化對帳單欄位。 | `normalized_row.counterparty_name`。 | 銀行原始對手名稱欄。 | 保留作銀行來源事實；名稱不唯一，只供顯示與稽核，不代表已匹配客戶或月嫂。 | Finance Import Staging | 首次建立 canonical row | 建立後不變 | 若以名稱直接建立正式身分關聯，會產生同名誤配。 | 已確認：保留對帳單來源欄位，不作正式身分鍵 |
| `counterparty_account` | `VARCHAR(191) NULL` | 對帳單直接提供的交易對手帳號。 | 正規化來源事實 | parser 忠實擷取／正規化對帳單欄位。 | `normalized_row.counterparty_account`。 | 銀行支援欄位中的原始帳號。 | 保留作銀行來源事實；不得被分類器解析出的帳號或下游帳務結果覆寫。 | Finance Import Staging | 首次建立 canonical row | 建立後不變 | 與 `resolved_counterparty_account` 語意不同，caller 不得混用。 | 已確認：保留對帳單來源欄位 |
| `resolved_counterparty_account` | `VARCHAR(191) NULL` | 分類器從受支援欄位解析出的目前交易對手帳號。 | 衍生分類投影 | 台新從結構化對手帳號取得；永豐由 memo／存摺備註搜尋完整帳號；無匹配時為 NULL。 | `classify_finance_transaction`。 | 銀行原始帳號／memo／bank references 與客戶、月嫂帳號主檔。 | 保留作目前分類結果投影，不具獨立權威性；只能由 classifier／reprocess 更新，不得覆寫 raw `counterparty_account`。正式核銷仍依既有唯一匹配與完整金額門禁重新驗證；不為極低機率的帳號同時匹配多人另增設計。 | Finance Classifier／Reprocess Service | 初次分類或受控重分類 | canonical raw facts 不變；本投影可由受控重處理更新 | 若 caller 把本欄當原始銀行事實，會混淆解析結果與對帳單內容。 | 已確認：保留目前分類投影 |
| `cancellation_code` | `VARCHAR(191) NULL` | 對帳單提供的銀行虛擬帳號／銷帳碼。 | 正規化來源事實 | parser／受控 resolver 依來源格式擷取。 | `normalized_row.cancellation_code`。 | 銀行原始銷帳資訊。 | 保留作對帳單來源事實、fingerprint 與客戶收款分類輸入；不得由正式客戶關聯或核銷結果反向改寫。 | Finance Import Staging | 首次建立 canonical row | 建立後不變 | 直接把文字當 client id 會繞過受控銷帳碼解析。 | 已確認：保留對帳單來源欄位 |
| `bank_references` | `JSON NOT NULL` | 對帳單各銀行額外參考欄位的具名集合。 | 結構化來源快照 | parser 依來源格式忠實建立 key/value object。 | `normalized_row.bank_references`。 | 銀行原始參考欄。 | 保留作具欄名的對帳單來源事實；key 必須非空字串，value 為非空字串或 NULL，不得存入分類或核銷結果。 | Finance Import Staging | 首次建立 canonical row | 建立後不變 | 部分分類會讀特定銀行欄名，但不得因此改寫原始集合。 | 已確認：保留具欄名的對帳單來源資料 |
| `warnings` | `JSON NOT NULL` | canonical row 第一次解析時的列級警告。 | 遺留衍生診斷快照／長期考慮移除 | parser／validator 產生不重複 warning code 陣列。 | 首次 `normalized_row.warnings`。 | 首次 occurrence 的解析結果；每次實際匯入警告在 `finance_import_occurrences.warnings`。 | 新流程的解析警告一律由 occurrence 保存；本欄既有值歷史唯讀並長期考慮移除。重處理改讀對應 occurrence，不得把 canonical warnings 當完整警告歷史或業務事實。 | 無新 owner；待遷移 Reprocess reader | 停止更新 | 歷史唯讀 | 只代表首次解析，會漏掉後續 duplicate／格式警告，且與 occurrence 重複。 | 已確認：歷史相容、長期考慮移除 |
| `raw_payload` | `JSON NOT NULL` | 第一次建立 canonical row 時，原始 Excel 整列的欄名與原值快照。 | 原始來源快照 | parser 將原始列轉為 JSON-safe scalar map，不套用後續分類或帳務公式。 | `normalized_row.raw_payload`。 | 使用者上傳檔案的首次原始列。 | 保留作不可變稽核與 parser 除錯依據；業務分類、薪資與核銷一律只讀經驗證的結構化欄位，不得直接從 raw payload 計算或建立正式交易。 | Finance Normalizer／Staging | 首次出現 canonical row | 建立後不變 | 只保留首次 canonical payload；後續 occurrence 的來源位置另存，但不代表另存每次原始整列。 | 已確認：保留首次原始列不可變快照 |
| `matched_identity_ids` | `JSON NOT NULL DEFAULT []` | classifier 找到的客戶付款或月嫂候選 ID 陣列。 | 遺留衍生分類投影／長期考慮移除 | 依 `classification_type` 可能存 `client_payments.id`、`staff.id` 或空陣列。 | `classify_finance_transaction`。 | resolved account、銷帳碼、客戶／月嫂帳號主檔。 | 不具獨立權威性且無型別；長期考慮移除。分類過程只在記憶體產生單一 typed candidate，正式核銷重新驗證並把真正關聯寫入正式交易。既有值僅供過渡期 dispatch／歷史讀取，不處理同帳號同時匹配多人的極低機率情境。 | Finance Classifier／Reprocess Service（過渡期） | 初次分類或受控重分類 | 正式核銷後不得作關聯來源 | 同一 JSON 依分類存不同資料表 ID，沒有 FK，且空陣列語意不一致。 | 已確認：長期考慮移除，正式關聯回歸正式交易 |
| `classification_type` | `VARCHAR(100) NOT NULL DEFAULT 'pending'` | 銀行列目前應交由哪一個業務核銷流程處理的分類。 | 衍生分類投影／dispatch 路由 | 初始 pending；classifier 依 format、由 debit／credit 推導的 direction、memo、銷帳碼與帳號匹配，產生 `client_receipt`、`government_subsidy`、`client_subsidy_return`、`staff_salary`、`staff_legacy_subsidy` 或 `non_business_review`。 | Finance Classifier。 | 正規化銀行根事實與客戶／月嫂帳號主檔。 | 保留作可受控重處理的目前分類投影與 dispatch 路由；不具銀行事實權威，也不代表正式核銷。合法 vocabulary 由單一 classifier 契約限制；重分類只能經 reprocess workflow。 | Finance Classifier／Reprocess Service | 初次分類或受控重分類 | 正式核銷後須限制重分類 | Schema 是自由字串，未以 CHECK／FK 約束 classifier vocabulary；分類、核銷與 Alert 必須維持三個獨立狀態軸。 | 已確認：保留目前分類投影，與核銷／Alert 分離 |
| `classification_reason` | `VARCHAR(255) NULL` | classifier 產生的目前分類原因碼。 | 衍生診斷投影 | classifier 依命中的固定分支輸出 reason code。 | `classification.reason`。 | 同次分類判斷路徑。 | 保留作目前分類結果的說明投影；必須和 `classification_type` 在同一分類／重處理交易更新，只能使用 classifier 固定 reason code，不允許人工自由輸入，也不取代 Alert 的處理原因。 | Finance Classifier／Reprocess Service | 每次分類 | 隨分類投影更新；歷史由 reprocess event 保存 | Schema 未限制 reason vocabulary，caller 若自由寫入會把程式原因碼變成人工備註。 | 已確認：保留固定分類原因碼投影 |
| `classified_at` | `TIMESTAMP NULL` | 最近一次成功分類完成時間。 | 技術時間投影 | DB `CURRENT_TIMESTAMP`。 | 初次分類或 reprocess UPDATE。 | 最近一次成功分類事件。 | 保留作追查目前分類投影新鮮度的技術時間；不參與分類、核銷、帳務月份或 Alert 狀態計算，也不能取代 reprocess event 歷史。 | Finance Staging／Reprocess Service | 每次分類 | 可覆寫最新值；完整歷史在 reprocess event | 單一時間無法說明分類規則版本或前後內容。 | 已確認：沿用技術時間規則 |
| `reconciliation_status` | `VARCHAR(50) NOT NULL DEFAULT 'pending'` | 此銀行列是否已形成正式核銷的相容查詢狀態。 | 衍生核銷投影／長期考慮移除 | 新列 pending；正式核銷成功後 reconciled。 | 各 Domain Reconciliation Service。 | 下游不可變正式交易及其 `finance_import_row_id` 關聯。 | 不具獨立權威性；正式核銷以 client、staff、government 等正式交易對原始銀行列的直接關聯為準。過渡期只能在建立正式交易的同一 DB transaction 更新，不得由 Alert、分類或人工修改。待所有正式交易類型都具備直接 raw FK 後長期移除。 | Client／Staff／Subsidy Reconciliation Services（過渡期） | 正式核銷 Apply | reconciled 後不可直接回 pending；更正走正式反向事件 | 多個 Service 直接 UPDATE，且 staging 狀態可能與正式交易關聯形成兩套答案。 | 已確認：過渡投影，正式關聯完成後長期移除 |
| `reconciliation_reference` | `VARCHAR(191) NULL` | 同一次原子核銷 Apply 的共同識別。 | 不可變技術關聯 | 同一 Apply 選取的所有銀行列寫相同 reference。 | Reconciliation command idempotency／Apply reference。 | 同一次完整核銷意圖。 | 不代表銀行外部流水號或單列 fingerprint；僅作跨列追蹤與重試去重。 | 各 Domain Reconciliation Services | 正式核銷 Apply | 成功後不可覆寫 | 現況各 Service 有時寫外部 reference 或 `fp:` 指紋，未落實共同 Apply 語意。 | 已確認：共同 Apply 識別 |
| `reconciled_at` | `TIMESTAMP NULL` | staging row 被標記為已核銷的相容技術時間。 | 技術時間投影／長期考慮移除 | DB `CURRENT_TIMESTAMP`。 | 與過渡期 `reconciliation_status` 同次 UPDATE。 | 不可變正式交易成功建立的 Apply 時點。 | 不具獨立權威性；過渡期必須與正式交易在同一 DB transaction 寫入，不得作銀行交易日、帳務月份或正式事件時間來源。所有正式交易完成 raw FK 後，改讀正式交易建立時間並長期移除本欄。 | 各 Domain Reconciliation Services（過渡期） | 正式核銷 Apply | 成功後不可覆寫 | 與正式交易建立時間重複，若 writer 分離會形成兩個核銷時間。 | 已確認：過渡技術投影，正式關聯完成後長期移除 |
| `created_at` | `TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP` | canonical row 首次建立時間。 | 技術建立時間 | DB default。 | 首次 INSERT。 | canonical row 被 DB 接受的時點。 | 保留作稽核與排序的技術建立時間；不代表銀行交易日、來源檔案產生日、匯入 occurrence 時間或帳務歸屬日，也不參與分類與核銷計算。 | DB／Finance Import Staging | 首次建立 canonical row | 建立後不變 | 容易被誤當銀行交易或每次匯入發生時間。 | 已確認：沿用技術建立時間規則 |
