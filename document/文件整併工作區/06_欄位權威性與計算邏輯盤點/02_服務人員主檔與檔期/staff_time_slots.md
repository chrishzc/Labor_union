# `staff_time_slots` 欄位權威性與計算邏輯盤點

- 狀態：討論與裁決用工作清冊；不是 SSOT，不代表 CP-1、Task 或實作核准。
- 分類：`02_服務人員主檔與檔期`
- 總索引：[06_欄位權威性與計算邏輯盤點](../../06_欄位權威性與計算邏輯盤點.md)
- 第一遍（live 現況）：已完成
- 第二遍（衍生判定）：已完成
- 根事實展開：已完成
- 規格反查：已完成

- Schema：`db/schema.sql`
- 父表關係：`staff_id` → `staff.id`
- 子表關係：無已宣告子表
- 已確認跨表裁決：本 1:N 子表是「月嫂可承接案件時段方案」的單一事實來源 (SSOT)。它只供媒合資格判定，不改變訂單每日服務時數或薪資公式。

| 欄位 | Schema 定義 | 現況架構／用途 | 初步分類 | 現況公式 | 直接來源 | 第一層根事實 | 建議權威公式 | 修改命令／Owner | 重算觸發 | 凍結邊界 | 現況漂移／風險 | 裁決 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `staff_id` | `INT NOT NULL` | 指向 `staff` 的外鍵，複合主鍵之一。 | 關聯鍵 | 不計算。 | 匯入或 Admin 寫入。 | `staff.id`。 | 必須對應有效的月嫂 ID。 | HCM import／Data Browser | 寫入時 | 不變 | 無 | 已確認：SSOT 關聯 |
| `slot_name` | `VARCHAR(50) NOT NULL COMMENT '時段名稱'` | 月嫂可接受的承接時段方案（4小時_上午／4小時_下午／8小時／24小時／其他），複合主鍵之一。 | 來源事實／媒合資格 | 不計算；同一月嫂可有多個方案。 | 匯入或授權管理員維護。 | 月嫂明確表示可承接的時段方案集合。 | 將客戶 `service_time` 解析為服務時段後做涵蓋判定：例如 `8:00–15:00`（7 小時）可由 `8小時` 單一方案涵蓋，也可由 `4小時_上午` + `4小時_下午` 的組合涵蓋；符合只表示可列入候選，不改變 `orders.service_hours_per_day`。 | HCM import／月嫂資格維護 | 匯入、管理員修正、客戶服務時段變更 | 可變；只影響未確認媒合／後續候選評估 | live matcher 尚未實作時段比對，即使帶有 `filter_time` 參數也不會使用本子表。 | 已確認：媒合資格 SSOT；方案可組合涵蓋 |
| `custom_slot_detail` | `VARCHAR(100) NULL COMMENT '其他時段的補充說明'` | 其他時段的補充說明。 | 來源事實 | 不計算。 | 匯入或 Admin 寫入。 | 月嫂填寫之補充說明。 | 針對「其他」選項的文字說明；僅供人工參考。 | HCM import／Data Browser | 匯入、管理員修正 | 可變 | 自由文字無法用於系統自動過濾。 | 已確認 |
