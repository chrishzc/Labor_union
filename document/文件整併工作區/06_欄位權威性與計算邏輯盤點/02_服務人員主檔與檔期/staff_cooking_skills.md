# `staff_cooking_skills` 欄位權威性與計算邏輯盤點

- 狀態：討論與裁決用工作清冊；不是 SSOT，不代表 CP-1、Task 或實作核准。
- 分類：`02_服務人員主檔與檔期`
- 總索引：[06_欄位權威性與計算邏輯盤點](../../06_欄位權威性與計算邏輯盤點.md)
- 第一遍（live 現況）：已完成
- 第二遍（衍生判定）：已完成
- 根事實展開：已完成
- 規格反查：已完成

- Schema：`db/schema.sql`
- 父表關係：`staff_id` → `staff.id`
- 子表關係：無已宣告子表
- 已確認跨表裁決：本 1:N 子表是月嫂料理能力的單一事實來源 (SSOT)，取代舊有 `staff.special_skills` JSON。它是媒合加分條件，不是硬性淘汰條件：沒有對應技能者仍可列入候選。

| 欄位 | Schema 定義 | 現況架構／用途 | 初步分類 | 現況公式 | 直接來源 | 第一層根事實 | 建議權威公式 | 修改命令／Owner | 重算觸發 | 凍結邊界 | 現況漂移／風險 | 裁決 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `staff_id` | `INT NOT NULL` | 指向 `staff` 的外鍵，複合主鍵之一。 | 關聯鍵 | 不計算。 | 匯入或 Admin 寫入。 | `staff.id`。 | 必須對應有效的月嫂 ID。 | HCM import／Data Browser | 寫入時 | 不變 | 無 | 已確認：SSOT 關聯 |
| `skill_name` | `VARCHAR(50) NOT NULL COMMENT '料理類型'` | 月嫂料理類型（葷食／素食／其他），複合主鍵之一。 | 來源事實／媒合加分資格 | 不計算；同一月嫂可有多個值。 | 匯入或授權管理員維護。 | 月嫂明確表示的料理能力集合。 | 客戶需求命中本表任一技能時，提高候選分數；未命中不得排除該月嫂。取代遺留 `staff.special_skills` JSON。 | HCM import／月嫂資格維護 | 匯入、管理員修正、客戶需求變更 | 可變；只影響未確認媒合／後續候選排序 | live matcher 尚未讀取技能；Data Browser 目前只可改 legacy JSON，與本裁決相反。 | 已確認：媒合加分 SSOT |
| `custom_skill_detail` | `VARCHAR(100) NULL COMMENT '其他料理的補充說明'` | 其他料理的補充說明。 | 來源事實 | 不計算。 | 匯入或 Admin 寫入。 | 月嫂填寫之補充說明。 | 針對「其他」選項的文字說明；僅供人工參考，不參與自動媒合過濾。 | HCM import／Data Browser | 匯入、管理員修正 | 可變 | 自由文字無法用於系統自動過濾計算。 | 已確認 |
