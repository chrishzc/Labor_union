# `staff_weekly_rest` 欄位權威性與計算邏輯盤點

- 狀態：討論與裁決用工作清冊；不是 SSOT，不代表 CP-1、Task 或實作核准。
- 分類：`02_服務人員主檔與檔期`
- 總索引：[06_欄位權威性與計算邏輯盤點](../../06_欄位權威性與計算邏輯盤點.md)
- 第一遍（live 現況）：已完成
- 第二遍（衍生判定）：已完成
- 根事實展開：已完成
- 規格反查：已完成

- Schema：`db/schema.sql`
- 父表關係：`staff_id` → `staff.id`
- 子表關係：無已宣告子表
- 已確認跨表裁決：本 1:N 子表是「月嫂可接受的訂單固定排休方案」的單一事實來源 (SSOT)。同一月嫂可有多列；它只供媒合檢查訂單排休方案是否可接受，絕不定義月嫂本人的實際每週排休。正式排班唯一依 `orders` 的固定排休規則；舊 `staff.weekly_rest_days` 不得再參與排班。

| 欄位 | Schema 定義 | 現況架構／用途 | 初步分類 | 現況公式 | 直接來源 | 第一層根事實 | 建議權威公式 | 修改命令／Owner | 重算觸發 | 凍結邊界 | 現況漂移／風險 | 裁決 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `staff_id` | `INT NOT NULL` | 指向 `staff` 的外鍵，複合主鍵之一。 | 關聯鍵 | 不計算。 | 匯入或 Admin 寫入。 | `staff.id`。 | 必須對應有效的月嫂 ID。 | HCM import／Data Browser | 寫入時 | 不變 | 無 | 已確認：SSOT 關聯 |
| `rest_type` | `VARCHAR(50) NOT NULL COMMENT '放假類型'` | 月嫂可接受的訂單固定排休方案（連續服務／週休1日／週休2日／其他），複合主鍵之一。 | 來源事實／媒合資格 | 不計算；同一月嫂可有多個值。 | 匯入或受控管理員維護。 | 月嫂明確表示可承接的方案集合。 | 媒合時檢查 `orders` 的固定排休規則是否為本表一列；不得將多列合併成月嫂實際休假星期，也不得直接驅動 `staff_schedule`。 | HCM import／月嫂資格維護 | 匯入、管理員修正 | 可變；只影響未確認媒合／後續候選評估 | 現況有路徑把 legacy `staff.weekly_rest_days` 當正式排休日，與本裁決相反。 | 已確認：媒合資格 SSOT |
| `custom_rest_detail` | `VARCHAR(100) NULL COMMENT '其他週間服務的補充說明'` | 其他週間服務的補充說明。 | 來源事實 | 不計算。 | 匯入或 Admin 寫入。 | 月嫂填寫之補充說明。 | 針對「其他」選項的文字說明；僅供人工參考。 | HCM import／Data Browser | 匯入、管理員修正 | 可變 | 自由文字無法用於系統自動過濾。 | 已確認 |
