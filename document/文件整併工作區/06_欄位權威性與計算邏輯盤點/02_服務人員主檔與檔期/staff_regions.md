# `staff_regions` 欄位權威性與計算邏輯盤點

- 狀態：討論與裁決用工作清冊；不是 SSOT，不代表 CP-1、Task 或實作核准。
- 分類：`02_服務人員主檔與檔期`
- 總索引：[06_欄位權威性與計算邏輯盤點](../../06_欄位權威性與計算邏輯盤點.md)
- 第一遍（live 現況）：已完成
- 第二遍（衍生判定）：已完成
- 根事實展開：已完成
- 規格反查：已完成

- Schema：`db/schema.sql`
- 父表關係：`staff_id` → `staff.id`
- 子表關係：無已宣告子表
- 已確認跨表裁決：依據 Schema Drift 的裁決，本 1:N 子表已被確立為「月嫂可承接案件區域」的單一事實來源 (SSOT)，全面取代舊有 `staff.service_regions` 的 JSON 陣列。

| 欄位 | Schema 定義 | 現況架構／用途 | 初步分類 | 現況公式 | 直接來源 | 第一層根事實 | 建議權威公式 | 修改命令／Owner | 重算觸發 | 凍結邊界 | 現況漂移／風險 | 裁決 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `staff_id` | `INT NOT NULL` | 指向 `staff` 的外鍵，複合主鍵之一。 | 關聯鍵 | 不計算。 | 匯入或 Admin 寫入。 | `staff.id`。 | 必須對應有效的月嫂 ID。 | HCM import／Data Browser | 寫入時 | 不變 | 無 | 已確認：SSOT 關聯 |
| `region_name` | `VARCHAR(50) NOT NULL COMMENT '區域名稱'` | 月嫂可服務區域（北區／東區／香山區／新竹縣／苗栗縣／其他），複合主鍵之一。 | 來源事實／媒合資格 | 不計算；同一月嫂可有多個值。 | 匯入或授權管理員維護。 | 月嫂當前可接案區域；搬家等變更後的管理員更新。 | 月嫂可服務區域的唯一事實來源；媒合與過濾僅讀本子表集合，不讀／不合併 `staff.service_regions` 或 `staff.city`。必須提供授權管理員的可編輯操作。 | HCM import／月嫂資格維護 | 匯入、管理員修正（例如搬家） | 可變；更新後重新評估尚未確認的候選／媒合 | 現況子表在 Data Browser 唯讀，但遺留 JSON 可編輯且 matcher 會合併兩者，與本裁決相反。 | 已確認：可編輯的媒合資格 SSOT |
| `custom_region_detail` | `VARCHAR(100) NULL COMMENT '對應其他地區的補充說明'` | 其他地區的補充說明。 | 來源事實 | 不計算。 | 匯入或 Admin 寫入。 | 月嫂填寫之補充說明。 | 針對「其他」選項的文字說明；僅供人工參考。 | HCM import／Data Browser | 匯入、管理員修正 | 可變 | 自由文字無法用於系統自動過濾。 | 已確認 |
