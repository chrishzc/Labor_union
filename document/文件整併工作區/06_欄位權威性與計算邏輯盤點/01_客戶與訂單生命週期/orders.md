# `orders` 欄位權威性與計算邏輯盤點

- 狀態：討論與裁決用工作清冊；不是 SSOT，不代表 CP-1、Task 或實作核准。
- 分類：`01_客戶與訂單生命週期`
- 總索引：[06_欄位權威性與計算邏輯盤點](../../06_欄位權威性與計算邏輯盤點.md)
- 第一遍（live 現況）：已完成
- 第二遍（衍生判定）：已完成
- 根事實展開：已完成
- 規格反查：待盤點

- Schema：`db/schema.sql`, `db/schema_parts/105_order_service_time_terms.sql`, `db/schema_parts/106_order_lifecycle_control_facts.sql`
- 父表關係：`case_no` → `clients.case_no`; `client_id` → `clients.id`; `staff_id` → `staff.id`
- 子表關係：`assignment_schedule_leave_substitution_batches.case_no` → `orders.case_no`; `assignment_schedule_leave_substitution_events.case_no` → `orders.case_no`; `caregiver_matching_plans.case_no` → `orders.case_no`; `case_staff_assignments.case_no` → `orders.case_no`; `client_payment_transactions.case_no` → `orders.case_no`; `client_payments.case_no` → `orders.case_no`; `matching_records.case_no` → `orders.case_no`; `order_assignment_change_audits.case_no` → `orders.case_no`; `order_lifecycle_control_events.case_no` → `orders.case_no`; `order_lifecycle_control_state.case_no` → `orders.case_no`; `order_lifecycle_projection_outbox.case_no` → `orders.case_no`; `order_lifecycle_state_events.case_no` → `orders.case_no`; `payment_migration_reviews.case_no` → `orders.case_no`; `staff_monthly_settlement_details.case_no` → `orders.case_no`; `staff_payment_transactions.case_no` → `orders.case_no`; `staff_payments.case_no` → `orders.case_no`; `staff_schedule.case_no` → `orders.case_no`; `subsidy_claim_batch_items.case_no` → `orders.case_no`

| 欄位 | Schema 定義 | 現況架構／用途 | 初步分類 | 現況公式 | 直接來源 | 第一層根事實 | 建議權威公式 | 修改命令／Owner | 重算觸發 | 凍結邊界 | 現況漂移／風險 | 裁決 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `case_no` | `VARCHAR(50) NOT NULL PRIMARY KEY COMMENT '案件唯一識別碼；對應 clients.case_no'` | 案件跨表業務鍵；建立訂單、匯入與所有子表關聯使用 | 來源事實／識別 | 不計算 | 建案或匯入提供的案件編號 | 外部／人工建案事件的案件編號 | 原值正規化後保存，不由其他欄位推算 | 建案／Importer | 建案時一次 | 被子表引用後不可一般修改 | `clients.case_no` 與 `orders.case_no` 雙重保存且需保持一致 | 已確認 |
| `client_id` | `INT NOT NULL COMMENT '對應 clients.id'` | 指向客戶主檔；保留期間僅作可重建關聯快取，長期考慮移除 | 衍生投影／冗餘關聯 | 以案件對應的 client row 解析 | `clients.id`、`clients.case_no` | `orders.case_no` 與 `clients.case_no` | 由 `case_no` 唯一解析，不由前端任意指定；若無必要效能／外鍵需求則移除 | 建案 Service／Importer | 建案或合法主檔合併 | 訂單建立後固定 | 同時保存 `case_no` 與 `client_id`，可能發生不一致；已標記考慮移除 | 已確認 |
| `staff_id` | `INT NULL COMMENT '對應 staff.id (可為 NULL，代表尚未配對成功)'` | 舊單一月嫂便利欄位；不得作正式人力、薪資、檔期或排班 fallback，長期考慮移除 | 衍生投影／legacy | 現況由配對、指派或舊 writer 直接寫入，沒有一致公式 | `case_staff_assignments.staff_id/status` 或 legacy request | 正式 assignment ownership | 多月嫂案件不應具有單一權威值；查詢直接使用有效 assignments，舊畫面僅可顯示非權威摘要 | legacy assign／conversion writers | assignment aggregate 改變 | 不適用；應去權威化 | 與多筆 assignment 不可一一對應，已造成 fallback 與錯誤歸屬 | 已確認 |
| `status` | `ENUM('洽談中', '訂單成立', '服務中', '訂單完成', '訂單取消') DEFAULT '洽談中' COMMENT '專案狀態 (生命週期: 洽談中→訂單成立→服務中→訂單完成, 任何階段可→訂單取消)'` | 訂單生命週期持久化投影；新建案件預設「洽談中」，live 尚有多個直接 writer | 領域狀態／投影 | lifecycle evaluator 依取消 control、訂金、actual start、完成時刻與 blockers 評估；legacy writers 直接指定 | 付款事實、實際開始日、正式工作日、服務時段、control state、業務時間 | 外部付款事件、人工日期確認、assignment-owned 工作日、人工 control commands | 新建案件預設「洽談中」；後續只由唯一 lifecycle evaluator 推導與 persistence 寫入 | `OrderLifecycleApplicationService` 目標 owner；現況另有 payment／conversion／generic writers | 任一 lifecycle domain event | 事件歷史 append-only；projection 可重算 | SSOT owner 尚未完全接上 production callers | 已確認 |
| `cancel_reason` | `TEXT NULL COMMENT '當狀態變更為 訂單取消 時的取消原因說明'` | 目前有效取消事件的查詢投影；非取消狀態不得保留無來源值 | 衍生投影／稽核顯示 | 目前直接 request 寫入；新 persistence 從 active cancellation control 決定 | `order_lifecycle_control_events.reason/action` | 具權限取消命令中的人工原因 | 只投影目前有效 cancellation event 的 reason；原因更正以新 correction／replacement event 表達 | Cancellation Command／lifecycle persistence | 取消啟用、清除或 correction | 原 control event 不可改 | orders 欄位與 append-only control event 重複，可能漂移 | 已確認 |
| `line_group_id` | `VARCHAR(100) NULL COMMENT '三方服務 LINE 群組 ID'` | 目前 LINE 群組綁定；已發送訊息、任務與 webhook 保留各自歷史 group snapshot | 來源事實／外部識別 | 不計算 | LINE 建群／綁定結果 | LINE 平台回傳或具權限人員確認的 group id | 原值保存；改綁定必須留下原／新 group、原因、操作者與時間事件 | LINE integration command | 建群、重新綁定、失效 | 已發送事件不可回寫 | 目前直接放 orders，外部 lifecycle 與歷史未完整呈現 | 已確認 |
| `actual_start_date` | `DATE NULL COMMENT '實際生產服務開始日'` | 正式服務開始來源事實；未確認前僅能做規劃 Preview，不能建立正式工作日或實際財務投影 | 來源事實 | 不計算 | 具權限命令的確認日期 | 工會人員確認的實際開始事實 | 不計算，且禁止由 `start_date`、訂金日期、排班第一日或 UI 預設 fallback | Confirm／Correct Actual Start Command | 首次確認或合法更正 | 財務結算後更正需 adjustment path | 目前 UI／Service 會以 planned start 補值，混淆事實 | 已確認 |
| `actual_end_date` | `DATE NULL COMMENT '實際生產服務結束日'` | 已確認實際開始後的現況服務結束日；同時是有效排程與薪資投影的動態終點，不是結案後固定不變的歷史事實。 | 衍生投影 | `max(以 orders.actual_start_date 為起點之全案有效 assignment-owned 正式工作日)`。確認實際開始日後，先依服務天數與排班規則重建正式工作日；其後每次有效排班、請假、代班或服務天數變更均重算最大日期。 | `orders.actual_start_date`、`orders.service_days`、適用排班／休假規則、`staff_schedule.work_date/is_work_day/assignment_id`、`case_staff_assignments.status` | 工會確認的實際開始日，以及正式命令產生並持續調整的 assignment-owned 工作日。 | 未確認 `actual_start_date` 時不產生此值；確認後以實際開始日取代預計開始日為排程根點，並以目前有效正式工作日最大日期作為值。不得由 UI、`orders.start_date` 或單一 writer 直接覆寫。 | Schedule aggregate transaction | Confirm／Correct Actual Start、assignment／schedule／請假／代班／服務條款 Apply | 訂單投影在未結算期間持續可重建；付款／月結快照保存當時結果，後續差額走 adjustment／reversal。 | 現況有 `start + service_days`、休假循環及 schedule last date 等多套算法與前端 fallback，需收斂為上述單一投影。 | 已確認：實際開始後的動態排程／實際投影 |
| `staff_payment_due_date`（建議欄位名） | 現況 `orders` 不存在；待建，建議 `DATE NULL` | 工會對本訂單所有月嫂 assignment 的正式基本薪資應付日，也是基本薪資月結月份的訂單層 SSOT。 | 衍生日期／訂單帳務投影 | 訂單正式完成後，依客戶付款類型取次月或次次月 15 日。 | 訂單完成事實、完成時確定的 `actual_end_date`、客戶服務費是否為 0 且政府全額補助。 | 正式工作日與訂單狀態機完成事件、客戶付款政策。 | 客戶有服務費應付：完成日次月 15 日；客戶服務費為 0 且政府全額補助：完成日次次月 15 日。未完成訂單為 `NULL`。所有基本 assignment 直接使用本日期決定月結月份。 | Order Completion／Staff Payable Projection Service | 訂單正式完成；未 finalized 月結前合法付款類型更正 | 月結 finalized 後不回寫既有月結；後續差額進下一個開放月結 | 現況只有 `v_order_details.salary_payment_date_1` 衍生顯示與 `staff_payments.due_date` 重複實體欄；View 使用 planned `end_date` 與身分類型，公式不符已確認規則。 | 已確認採用訂單層 SSOT；欄位名待實作設計 |
| `contract_id` | `VARCHAR(100) NULL COMMENT '好好簽線上契約 ID'` | 外部 BreezySign 契約識別；簽署事件只進入契約 aggregate，不得越權改日期、排班或訂單狀態 | 來源事實／外部識別 | 不計算 | 契約建立或 webhook 的 provider id | 外部簽署平台事件 | 原值保存；更正保留原／新 ID、原因、操作者與 provider 證據；不可由簽署結果反推其他訂單事實 | Contract integration adapter | 契約建立、重發或 provider correction | 已簽事件需保留原 contract id | 舊 webhook 會越權改日期、status 與排班 | 已確認 |
| `service_days` | `INT DEFAULT 0 COMMENT '服務天數 (N)'` | 訂單服務條款；所有有效 assignment-owned 正式工作日的守恆目標 | 來源事實 | 不計算 | 合約／訂單人工確認 | 客戶與工會確認的服務條款 | 正式案件為正整數；0 僅容許草稿／歷史待補資料，不能建立正式排班、薪資或進入服務中 | Order Terms Command | 建案或合法條款變更 | 結算後不可一般覆寫 | Admin generic PATCH 與 UI 可改但未必重建全部投影 | 已確認 |
| `service_hours_per_day` | `INT DEFAULT 0 COMMENT '每日服務時數 (J)'` | 訂單每日服務條款；所有 assignment 時數公式根來源 | 來源事實（正式條款）／初始衍生 | 初始匯入：`clients.service_time` 的結束時間 − 開始時間；例 `8:00~15:00 = 7`。正式成立後不由薪資反推。 | 初始值：客戶希望服務時段；正式值：Order Terms Command。 | 客戶申請時段與後續正式訂單條款確認。 | 正式案件為正整數；0 僅容許草稿／歷史待補資料，不能建立正式排班或薪資；`assignment.actual_hours = official_work_day_count × service_hours_per_day`。客戶時段變更必須 Preview 後原子重建有效指派、正式工作日與薪資投影。 | Order Terms Command | 建案或合法條款變更 | 結算前可重建；付款／月結後以 adjustment／reversal 保存差額歷史。 | 現況 import 只抓 service_time 第一個數字，會把 `8:00~15:00` 錯寫為 8；generic writer 可改且下游 actual_hours 可能未同步。 | 已確認：時段差換算；修改走 Preview／重建 |
| `floor_fee` | `DECIMAL(10, 2) DEFAULT 0.00 COMMENT '樓層費用 (O)'` | 全案樓層費條款；assignment 分配僅為守恆投影 | 來源事實 | 不計算 | 合約／訂單人工確認 | 客戶與工會確認的樓層費 | 非負 Decimal 原值；所有有效 assignment allocation 合計必須等於本欄位，代班比例自原 assignment 轉移 | Order Terms Command | 建案或合法條款變更 | 結算前可重建；付款／月結後以追加 adjustment／reversal 保存差額歷史 | 下游 assignment allocation 是投影，需守恆 | 已確認 |
| `deposit_date` | `DATE NULL COMMENT '訂金收取日期'` | 付款事實的冗餘 legacy 投影；禁止直接寫入，長期考慮移除 | 衍生投影／legacy | 由訂金實收／核銷事件日期投影，現況也可被直接寫入 | `client_payment_transactions.occurred_at/status/type/stage`、allocation | 銀行交易與人工核銷事件 | 從目前有效訂金全額核銷結果投影，與 `client_payments.deposit_received_at` 一致；沖正／失效時清除或重算 | Payment Application Service | 訂金核銷、沖正、失效 | 交易 append-only；摘要可重建 | 與 `client_payments.deposit_received_at` 重複且命名不清 | 已確認；考慮移除 |
| `deposit_service_days` | `INT NULL COMMENT '訂金服務天數；NULL 表示歷史案件待人工補登'` | 客戶身分政策的冗餘投影；`orders` 保留期間不得手動輸入，長期考慮移除 | 衍生投影／legacy | `低收入戶`／`中低收入戶` → 0；`一般市民`／`非市民` → 5。 | `clients.identity_status`、固定訂金日數政策 | 客戶已確認身分資格與固定政策常數 | 同左；訂金義務成立時由 Payment aggregate 凍結當時身分與日數快照，之後身分更正以付款 correction 處理 | Payment Application Service | 身分資格確認／更正、訂金義務建立 | 已建立訂金義務後不回寫舊快照 | 原始身分類別可保留多值，但公式只能依受控政策映射，不得把名稱直接當公式分支。 | 已確認；考慮移除 |
| `start_date` | `DATE NULL COMMENT '預計/實際服務開始日 (AK)'` | 預計服務開始日；實際開始後仍保存作規劃與事實的比較，不得作 actual fallback | 來源事實 | 不計算 | UI／匯入提供的預計服務日 | 客戶與工會的規劃資訊 | 僅代表 planned start；可用於規劃模式 end date、候選檔期與預計服務日，不得建立正式工作日、薪資或實際結束日 | Order Planning Command | 建案或規劃修正 | 實際開始後仍保留原規劃值供比較 | Schema 註解與 caller 明確混用預計／實際 | 已確認 |
| `end_date` | `DATE NULL COMMENT '預計/實際服務結束日 (AL)'` | 預計服務結束日；不得被正式請假、實際出勤或 UI fallback 覆寫 | 衍生投影 | 自 `start_date` 向後數足 `service_days` 個合法規劃服務日；live 另有多套算法 | `start_date`、規劃服務日／非服務日、`service_days` | 規劃開始日、服務天數與規劃休假選擇 | 僅為 planned end projection，與 actual end 分離 | Planning Schedule Service | 規劃日期或休假變更 | 無；可重建 | 與 actual_end_date 一起被 legacy writer 同值更新 | 已確認 |
| `custom_rest_dates` | `JSON NULL COMMENT '排定/自訂休假日期 JSON 陣列 (如 ["2026-07-05", "2026-07-12"])'` | legacy 全案休假 JSON；新 Calendar 不讀取，月曆雖 SELECT 但未參與計算；長期規劃移除 | 衍生投影／legacy | legacy `save_order_rest_dates` 直接寫 JSON、兩個 end date 並刪除／重建 schedule | 使用者輸入 JSON；正式模型則來自 leave events／schedule | 請假命令的日期與 outcome 事件 | 正式 actual 流程不以此欄位為權威；移除 Data Browser generic PATCH 與 legacy writer production 副作用後淘汰 | Legacy rest-date writer／未來 Planning Service | 規劃休假或正式 leave Apply | 正式事件建立後不可用 JSON 覆蓋 | 與正式事件模型重複，且無 assignment ownership／lineage；目前仍可經 generic PATCH 修改 | 已確認；長期規劃移除 |
| `created_at` | `TIMESTAMP DEFAULT CURRENT_TIMESTAMP` | DB 建立列的技術稽核時間，不替代建案、訂單成立、契約簽署或服務開始等業務時間 | 稽核／系統事實 | DB current timestamp | DB insert 時刻 | 資料庫接受建案的時間 | DB 生成；API、UI 與匯入 payload 不得指定 | Repository | INSERT | 不可修改 | 與業務建案時間可能不同，需分清 | 已確認 |
| `updated_at` | `TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 任意 `UPDATE orders` 造成的技術時間；不得作版本、事件順序或業務日期 | 衍生系統投影 | DB current timestamp on update | 任一 UPDATE | DB 實際寫入事件 | 僅作技術資訊；並行控制使用 lifecycle version／expected version／Preview fingerprint，業務歷史使用 append-only event 時間 | DB | 任一 UPDATE | 可變 | 不能作 lifecycle version 或業務時間 | 已確認 |
| `service_start_time` | `TIME NULL COMMENT ''案件統一每日服務開始時間；既有案件待明確補登` | 每日服務開始時間；不作為薪資或訂單狀態的權威依據。 | 遺留設定／待移除 | 不計算 | 無；不得由排班、瀏覽器時區或其他欄位推算。 | 無 | 長期考慮移除。若仍暫存，僅能作為非權威顯示設定；不可驅動薪資、排班有效性或訂單狀態。 | 禁止新的業務命令依賴或寫入；移除前盤點既有讀寫端。 | 無 | 無 | 若被用於自動完成、跨日判斷或衝突檢查，會形成未受狀態機治理的隱性權威。 | 已確認：長期考慮移除 |
| `service_end_time` | `TIME NULL COMMENT ''案件統一每日服務結束時間；既有案件待明確補登` | 每日服務結束時間；不作為薪資、排班或訂單狀態的權威依據。 | 遺留設定／待移除 | 不計算 | 無；不得由排班、瀏覽器時區、開始時間或其他欄位推算。 | 無 | 長期考慮移除。若仍暫存，僅能作為非權威顯示設定；不可驅動薪資、排班有效性或訂單狀態。 | 禁止新的業務命令依賴或寫入；移除前盤點既有讀寫端。 | 無 | 無 | 若被用於自動完成、跨日判斷或衝突檢查，會形成未受狀態機治理的隱性權威。 | 已確認：長期考慮移除 |
| `service_end_day_offset` | `TINYINT UNSIGNED NULL COMMENT ''0=服務日當日結束，1=次日結束；不得由時間大小推測` | 指定服務結束落在同日或次日；不作為薪資、排班或訂單狀態的權威依據。 | 遺留設定／待移除 | 不計算 | 無；不得由起訖時間、排班或其他欄位推算。 | 無 | 長期考慮移除。若仍暫存，僅能作為非權威顯示設定；不可驅動薪資、排班有效性或訂單狀態。 | 禁止新的業務命令依賴或寫入；移除前盤點既有讀寫端。 | 無 | 無 | 與起訖時間欄位共同形成重複、未受狀態機治理的時段權威。 | 已確認：長期考慮移除 |
| `lifecycle_version` | `BIGINT UNSIGNED NOT NULL DEFAULT 0 COMMENT ''ORD-01 aggregate revision；每個非 replay command 恰遞增一次` | 訂單生命週期 optimistic concurrency token。 | 衍生並行控制狀態 | 每個成功且非 replay lifecycle command 加一。 | 前一版本與成功 command acceptance。 | lifecycle command 受理順序。 | 保留；`previous + 1`，exact replay 不增加。UI／caller 必須送出 `expected_version`，Service 只在目前版本相等時更新；版本衝突要求刷新。不參與服務日期、薪資或訂單領域狀態公式，也不具獨立業務權威。 | Lifecycle Persistence | 成功且非 replay 的 lifecycle command | append-only state event 綁定版本 | live 行事曆與財務 UI 已使用；legacy direct writer 若繞過版本檢查，仍會破壞並行控制。 | 已確認：保留衍生並行控制 token |
