# `clients` 欄位權威性與計算邏輯盤點

- 狀態：討論與裁決用工作清冊；不是 SSOT，不代表 CP-1、Task 或實作核准。
- 分類：`01_客戶與訂單生命週期`
- 總索引：[06_欄位權威性與計算邏輯盤點](../../06_欄位權威性與計算邏輯盤點.md)
- 第一遍（live 現況）：已完成
- 第二遍（衍生判定）：待盤點
- 根事實展開：待盤點
- 規格反查：待盤點

- Schema：`db/schema.sql`
- 父表關係：無已宣告外鍵
- 子表關係：`line_confirmation_requests.client_id` → `clients.id`; `orders.case_no` → `clients.case_no`; `orders.client_id` → `clients.id`

| 欄位 | Schema 定義 | 現況架構／用途 | 初步分類 | 現況公式 | 直接來源 | 第一層根事實 | 建議權威公式 | 修改命令／Owner | 重算觸發 | 凍結邊界 | 現況漂移／風險 | 裁決 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | `INT AUTO_INCREMENT PRIMARY KEY` | 客戶列技術主鍵；供資料庫關聯使用，非業務案件識別。 | 技術識別／關聯鍵 | DB 自增，無業務公式。 | DB INSERT。 | 資料庫列建立。 | 不得被 UI 或業務命令修改；`orders.client_id` 與 LINE review 以此建立技術關聯。業務識別仍以不可變 `case_no` 為準。 | DB／Repository | INSERT | 不變。 | `orders.client_id` 與 `case_no` 雙重關聯屬既知 legacy；不應把本技術 id 暴露為業務規則來源。 | 已確認：技術主鍵 |
| `seq_num` | `INT COMMENT '項次'` | HCM 匯入列項次。 | 遺留匯入欄位／待移除 | 不計算。 | 匯入來源。 | 原始 HCM 檔的列順序。 | 長期考慮移除；不作客戶、訂單、收款、排班或薪資的識別／計算來源。 | HCM import | 匯入 | 無 | 未發現業務讀取或關聯用途。 | 已確認：長期考慮移除 |
| `reject_reason` | `TEXT COMMENT '不符合原因'` | HCM 原始資料的「不符合原因」殘留。 | 遺留匯入欄位／待移除 | 不計算。 | HCM 匯入／Data Browser。 | 原始 HCM 報名審核資料。 | 長期考慮移除；對應的「案件狀態」未存於 clients，故不可作客戶、訂單取消或狀態機權威。若需留存證據，應歸屬匯入紀錄。 | import／admin edit | 匯入、人工修正 | 無 | 單獨保存原因沒有完整業務語意。 | 已確認：長期考慮移除 |
| `case_no` | `VARCHAR(50) UNIQUE COMMENT '查詢序號(案件編號) - 去重唯一識別碼'` | 客戶／訂單主關聯識別。 | 來源事實／識別 | 不計算。 | HCM 匯入／建案。 | 原始建案識別。 | 建立後不可任意改動；必須與 `orders.case_no` 同案。更正只能走受稽核的資料遷移，不可單列 UPDATE。 | import／case creation | 建案 | 子表建立後不可一般修改 | `orders.client_id` 與雙向 case_no 關聯形成重複關聯，已另將 orders.client_id 列長期考慮移除。 | 已確認（沿用 case_no 規則） |
| `created_at` | `DATETIME COMMENT '報名時間(建檔)'` | HCM／外部來源記載的客戶申請建檔時間。 | 來源事實／稽核 | 不計算。 | 匯入資料。 | 原始客戶申請／建檔事件時間。 | 保留原值；可用於申請先後與原始資料追查，但不是訂單成立、實際開始或狀態機時間。 | HCM import | 匯入 | 匯入後不應一般覆寫；更正應保留來源證據。 | 與 `db_created_at` 的 DB 技術時間不同。 | 已確認 |
| `ip_address` | `VARCHAR(45) COMMENT 'IP位址'` | 原始報名 IP 稽核資料。 | 敏感遺留資料／待移除 | 不計算。 | 匯入／Data Browser。 | 原始報名來源資料。 | 長期考慮移除；無客戶、訂單、排班、薪資或狀態機權威。若有明確資安／爭議追查需求，應另定保存政策與受限稽核位置。 | import／admin edit | 匯入、人工修正 | 無 | 現況無業務讀取；存於客戶主檔增加敏感資料暴露面。 | 已確認：長期考慮移除 |
| `name` | `VARCHAR(100) COMMENT '姓名'` | 客戶姓名。 | 來源事實 | 不計算。 | HCM 匯入；Data Browser 直接修改。 | 客戶登錄資料與授權的 Data Browser 客資更正。 | 姓名可由 Data Browser 直接修改；訂單只能讀取／引用，不能回寫。 | HCM import／Data Browser | 匯入、授權客資修正 | 訂單成立後仍可依法更正客資；更正應有 Data Browser 稽核。 | 現況 order assignment synchronization 會直接 UPDATE clients.name，屬應移除的反向回寫漂移。 | 已確認 |
| `gender` | `VARCHAR(10) COMMENT '性別'` | 客戶基本資料。 | 來源事實 | 不計算。 | HCM 匯入／Data Browser。 | 客戶登錄資料與授權客資更正。 | 保留為可更正的客戶基本資料；不參與訂單狀態、排班、薪資或補助金額計算。 | HCM import／Data Browser | 匯入、授權客資修正 | 可變；更正應有 Data Browser 稽核。 | 現況字彙限男／女，是否擴充屬資料政策另議。 | 已確認 |
| `phone` | `VARCHAR(20) COMMENT '行動電話'` | 客戶聯絡電話。 | 來源事實 | 不計算。 | HCM 匯入／Data Browser。 | 客戶登錄資料與授權客資更正。 | 保留為可更正的聯絡資料；不可作案件識別、LINE 綁定權威或訂單／薪資計算來源。 | HCM import／Data Browser | 匯入、授權客資修正 | 可變；更正應有 Data Browser 稽核。 | 匯入驗證為 09 開頭十碼；不可假設電話唯一或永久不變。 | 已確認 |
| `city` | `VARCHAR(50) COMMENT '縣市'` | 客戶服務地的行政區基本資料。 | 來源事實 | 不計算。 | HCM 匯入／Data Browser。 | 客戶登錄資料與授權客資更正。 | 保留為可更正服務地客資；媒合可讀取以縮小候選，不能取代排班可用性或正式服務範圍判定。 | HCM import／Data Browser | 匯入、授權客資修正 | 可變；更正應有 Data Browser 稽核。 | 匯入字彙需受縣市清單驗證；不可由 city 直接作媒合核准。 | 已確認 |
| `address` | `VARCHAR(255) COMMENT '地址'` | 客戶實際服務地址。 | 來源事實 | 不計算。 | HCM 匯入／Data Browser。 | 客戶登錄資料與授權客資更正。 | 保留為可更正服務地客資；媒合可參考，正式指派與排班仍須經可用性／衝突檢查，不能僅憑地址成立。 | HCM import／Data Browser | 匯入、授權客資修正 | 可變；更正應有 Data Browser 稽核。 | 個資；不參與薪資或訂單狀態計算。 | 已確認 |
| `identity_status` | `VARCHAR(100) COMMENT '身分資格'` | 客戶身分資格；保留歷史身分類別，供受控政策映射使用。 | 來源事實／受控資格 | 不計算。 | HCM 匯入／資格認定；Form UI 為唯讀。 | 客戶資格認定資料。 | 官方值保留 `低收入戶`、`中低收入戶`、`一般市民`、`非市民`；不得由 Data Browser 任意改寫。計算映射：低收入戶／中低收入戶 → 補助市民政策（訂金天數 0）；一般市民／非市民 → 5 天。所有收費／補助計算必須先套用此映射，不能要求原始欄位改存政策名稱。 | eligibility/import owner | 資格認定或受控匯入 | 訂金 obligation 建立時凍結當時資格／天數 snapshot；後續更正走付款更正，不回寫已結算事實。 | 身分類別與計算政策分類不同，禁止把原始身份名稱當作公式分支。live Order Amount Calculator 目前只接受 `補助市民` 等政策名稱，未先映射低收入戶／中低收入戶，會使合法原始資格無法計算。 | 已確認 |
| `service_time` | `VARCHAR(100) COMMENT '服務時間'` | 客戶希望服務時段，例如 `8:00~15:00`。 | 來源事實／訂單條款輸入 | `service_hours_per_day = 結束時間 − 開始時間`；例 `8:00~15:00 = 7` 小時。 | HCM 匯入／Data Browser。 | 客戶申請或後續確認的明確開始／結束時段。 | 建案／匯入時將合法時段換算為 `orders.service_hours_per_day`。既有正式案件修改時，Data Browser 僅作 Order Terms Command 入口，必須 Preview 後原子重建有效指派、正式工作日與薪資投影；不得直接 PATCH。 | HCM import／Order Terms Command | 匯入、受理客戶變更 | 結算前可重建；付款／月結後依 adjustment／reversal 邊界處理，不覆寫歷史快照。 | 現況 import 以 regex 抓第一個數字，`8:00~15:00` 錯寫成 8 而非 7；Data Browser 可改但不會同步訂單。 | 已確認：時段差換算；修改走 Preview／重建 |
| `due_month` | `VARCHAR(100) COMMENT '預產期/預計服務開始月份'` | 客戶預產期；僅為客戶情況資料，不是訂單服務起日。 | 來源事實／客戶情況 | 不計算；不得推導或覆寫任何 `orders` 日期。 | HCM 匯入／Data Browser。 | 客戶或其家屬提供的預產期。 | 可由 Data Browser 更正；僅供查詢或溝通，不得觸發訂單條款、排班、正式工作日或薪資投影重建。 | HCM import／Data Browser | 匯入、客資更正 | 歷史更正保留資料修正軌跡；不影響既有結算。 | 匯入驗證 YYYY/MM/DD，schema 卻是 VARCHAR；名稱混入「預計服務開始月份」而容易被誤當訂單條款，應改名或清楚標示預產期。 | 已確認：預產期，不驅動訂單 |
| `service_start_date` | `VARCHAR(100) COMMENT '預計服務日期'` | 客戶提出的預計服務開始日；是尚未確認實際開始前的訂單規劃條款輸入。 | 來源事實／訂單規劃條款輸入 | 未填入實際開始日前：作為 `orders.start_date` 的來源，並據此產生預排行事曆與 `end_date` 規劃投影。`orders.actual_start_date` 一經工會確認，正式排程根點切換為實際開始日，`actual_end_date` 隨有效正式工作日持續重算，並驅動有效指派與薪資投影。 | HCM 匯入／Data Browser。 | 客戶申請或後續確認的預計服務開始日。 | 既有案件改期時，Data Browser 僅作 Order Planning Command 入口，必須 Preview 後重建未結算期間的預排行事曆、有效指派與薪資投影；不得直接 PATCH。實際開始日由獨立 Confirm／Correct Actual Start Command 確認。 | HCM import／Order Planning Command／Confirm Actual Start Command | 匯入、受理改期、確認實際開始 | 結算前可重建；付款／月結後以 adjustment／reversal 處理，不覆寫歷史快照。 | schema 是 VARCHAR；generic Data Browser 可直接改，且現況未必同步訂單與下游投影。 | 已確認：規劃起點；實際開始後切換 |
| `notes` | `TEXT COMMENT '其他事項'` | 客戶申請其他說明。 | 客資說明 | 不計算。 | HCM 匯入／Data Browser。 | 客戶登錄資料與授權客資更正。 | 可更正客資說明；不作訂單、排班、薪資、補助或狀態機來源。 | HCM import／Data Browser | 匯入、授權客資修正 | 可變；更正應有 Data Browser 稽核。 | 不得以自由文字取代受控業務欄位。 | 已依普通客資基準記錄 |
| `service_days` | `INT COMMENT '希望服務天數'` | 客戶提出的希望服務天數；是 `orders.service_days` 正式條款的輸入來源。 | 來源事實／訂單條款輸入 | 正式值不由下游薪資或已排日數反推；初始建案將此值帶入 `orders.service_days`。後續 `actual_end_date`、有效指派與薪資投影均以正式訂單條款為根。 | HCM 匯入／Data Browser。 | 客戶申請或後續確認的服務天數。 | 既有案件變更時，Data Browser 僅作 Order Terms Command 入口，必須 Preview 後原子重建未結算期間的有效指派、正式工作日、`actual_end_date` 與薪資投影；不得直接 PATCH。 | HCM import／Order Terms Command | 匯入、受理客戶變更 | 結算前可重建；付款／月結後以 adjustment／reversal 處理，不覆寫歷史快照。 | generic Data Browser 可直接改，且不會保證同步 `orders.service_days` 與下游投影。 | 已確認：服務天數條款；修改走 Preview／重建 |
| `residence_type` | `VARCHAR(100) COMMENT '居住型態'` | 客戶居住型態。 | 客資來源事實 | 不計算。 | HCM 匯入／Data Browser。 | 客戶登錄資料與授權客資更正。 | 可更正客資；不作訂單、排班、薪資、補助或狀態機來源。 | HCM import／Data Browser | 匯入、授權客資修正 | 可變；更正應有 Data Browser 稽核。 | 現況字彙受匯入驗證限制。 | 已依普通客資基準記錄 |
| `delivery_type` | `VARCHAR(100) COMMENT '生產方式'` | 客戶申請生產方式。 | 客資來源事實 | 不計算。 | HCM 匯入／Data Browser。 | 客戶登錄資料與授權客資更正。 | 可更正客資；不作訂單、排班、薪資、補助或狀態機來源。 | HCM import／Data Browser | 匯入、授權客資修正 | 可變；更正應有 Data Browser 稽核。 | 不可取代正式醫療／服務評估紀錄。 | 已依普通客資基準記錄 |
| `service_type` | `VARCHAR(100) COMMENT '服務方式'` | 遺留輸入欄位；其正式業務名稱為「固定排休規則」，是訂單排班條款而非一般客資。 | 來源事實／訂單條款輸入；長期應自 `clients` 移交至訂單條款模型。 | 合法值僅 `週休1日`、`週休2日`、`連續服務`；分別對應每週排除週日、週六日、無固定排休。此規則與實際開始日、服務天數、單日請假／國定假日共同推導正式工作日、`actual_end_date`、有效指派及薪資投影。 | HCM 匯入／受控 Order Terms Command。 | 客戶與工會確認的固定排休安排。 | 初始建案可由此帶入；既有案件變更時，Data Browser 僅作 Order Terms Command 入口，必須 Preview 後重建未結算期間的正式工作日、有效指派、`actual_end_date` 與薪資投影；不得 generic PATCH。 | HCM import／Order Terms Command | 匯入、受理排休條款變更 | 結算前可重建；付款／月結後以 adjustment／reversal 處理，不覆寫歷史快照。 | 現況 import、部分行事曆試算與訂單頁讀取本欄位；Data Browser 卻可直接改。backend Data Browser schema 錯列 `24小時`、`9小時`、`4小時`、`其他`。更嚴重的是 assignment rest-date Apply 路徑硬編碼 `週休1日`，未讀訂單條款，會使實際排班與 `actual_end_date` 漂移，必須移除。 | 已確認：固定排休規則；修改走 Preview／重建 |
| `baby_info` | `VARCHAR(255) COMMENT '寶寶資訊'` | 客戶申請嬰兒資訊。 | 客資來源事實 | 不計算。 | HCM 匯入／Data Browser。 | 客戶登錄資料與授權客資更正。 | 可更正客資；不作訂單、排班、薪資、補助或狀態機來源。 | HCM import／Data Browser | 匯入、授權客資修正 | 可變；更正應有 Data Browser 稽核。 | 個資；不應被自由文字解析成計算規則。 | 已依普通客資基準記錄 |
| `line_id` | `VARCHAR(100) COMMENT 'LINE ID'` | 客戶提供的 LINE 帳號／聯絡文字；不是平台身分識別。 | 來源事實／基本聯絡資料 | 不計算，且不得推導 `line_user_id`。 | HCM 匯入／Data Browser。 | 客戶提供或人工核對的聯絡帳號文字。 | 可由 Data Browser 更正；僅供人工聯絡與查詢，不得用於通知授權、帳號綁定、訂單狀態或薪資計算。 | HCM import／Data Browser | 匯入、客資更正 | 不影響歷史結算。 | 與 `line_user_id` 語意不同；live search 未發現它參與 API、通知或計算，只在匯入與假資料使用。 | 已確認：一般聯絡資料 |
| `line_user_id` | `VARCHAR(100) COMMENT 'LINE 平台用戶唯一識別碼 (Webhook 取得)'` | 客戶在 LINE 平台的綁定身分；用於依案件客戶角色發送訊息，不參與訂單或薪資計算。 | 來源事實／外部帳號綁定 | 不計算；通知以案件收件者角色選取客戶或月嫂的綁定值。 | LINE webhook／人工審核綁定。 | 平台 webhook 所得帳號與人工審核的綁定結果。 | 僅能經 LINE review bind／rebind command 變更；不可 generic PATCH。每個角色內避免同一帳號重複綁定，但同一平台帳號可同時存在於 `clients` 與 `staff`：此人同時是客戶與月嫂時，系統依通知的案件角色送達正確一方。 | LINE review service | webhook 申請、核准綁定／重綁 | 綁定／重綁事件應可稽核；不影響財務結算。 | 現況 clients 端缺 DB unique，服務層已有同角色重複檢查；跨 clients／staff 的 anomaly 僅供觀測，不能視為違規。 | 已確認：跨角色重複允許 |
| `admin_notes` | `TEXT COMMENT '管理者註記事項'` | 管理員客資註記。 | 管理註記 | 不計算。 | Data Browser／管理端。 | 授權管理員輸入。 | 僅作管理註記；不作訂單、排班、薪資、補助或狀態機來源。 | Data Browser／管理端 | 人工註記 | 可變；更正應有 Data Browser 稽核。 | 不得以自由文字取代受控業務欄位。 | 已依普通客資基準記錄 |
| `db_created_at` | `TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '資料庫匯入時間'` | 本列第一次寫入資料庫的技術時間。 | 技術中繼資料 | DB `CURRENT_TIMESTAMP` 於 INSERT 時寫入。 | DB。 | INSERT 執行時間。 | 僅供技術稽核與除錯；不得當作客戶報名、訂單成立或狀態機事件時間。 | DB | INSERT | 不變。 | 與外部申請時間 `created_at` 完全不同。 | 已確認：技術時間 |
| `db_updated_at` | `TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '資料庫更新時間'` | 本列最近一次任何 UPDATE 的技術時間。 | 技術中繼資料 | DB `ON UPDATE CURRENT_TIMESTAMP`。 | DB。 | 最近一次列更新執行時間。 | 僅供技術除錯；任何欄位更新皆會變動，不能表示某項客資、條款、綁定或狀態的業務生效時間。 | DB | 任一 UPDATE | 可隨更新變動。 | 不等於業務變更事件時間，不能作狀態機或薪資判斷來源。 | 已確認：技術時間 |
