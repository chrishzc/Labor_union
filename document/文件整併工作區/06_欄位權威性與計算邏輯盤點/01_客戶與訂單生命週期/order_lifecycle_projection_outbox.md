# `order_lifecycle_projection_outbox` 欄位權威性與計算邏輯盤點

- 狀態：討論與裁決用工作清冊；不是 SSOT，不代表 CP-1、Task 或實作核准。
- 分類：`01_客戶與訂單生命週期`
- 總索引：[06_欄位權威性與計算邏輯盤點](../../06_欄位權威性與計算邏輯盤點.md)
- 第一遍（live 現況）：已完成
- 第二遍（衍生判定）：已完成
- 根事實展開：已完成
- 規格反查：待盤點

- Schema：`db/schema_parts/106_order_lifecycle_control_facts.sql`
- 父表關係：`case_no` → `orders.case_no`; `lifecycle_event_id` → `order_lifecycle_state_events.id`
- 子表關係：無已宣告子表

| 欄位 | Schema 定義 | 現況架構／用途 | 初步分類 | 現況公式 | 直接來源 | 第一層根事實 | 建議權威公式 | 修改命令／Owner | 重算觸發 | 凍結邊界 | 現況漂移／風險 | 裁決 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | `BIGINT UNSIGNED NOT NULL AUTO_INCREMENT` | outbox 工作項目的技術主鍵。 | 稽核／識別 | DB 自增。 | 成功 INSERT。 | DB 接受 persistence 寫入。 | DB 生成；不參與警示內容、訂單狀態、排班或薪資計算。 | lifecycle persistence | INSERT | 建立後不可修改 | 技術主鍵，不列業務裁決。 | 不列業務裁決（已確認） |
| `case_no` | `VARCHAR(50) NOT NULL` | 將投影工作項目關聯所屬訂單；亦構成系統警示的 case key。 | 稽核／關聯 | 不計算。 | lifecycle persistence 由本次狀態命令帶入。 | 已確認不可變的 `orders.case_no`。 | 保存所屬訂單關聯；必須存在且與 `lifecycle_event_id` 所屬事件相同，不得由 caller 任意指定。 | lifecycle persistence | 每一個 projection intent | 建立後不可修改 | FK 僅各自驗證存在，應由 persistence 保證兩者同案。 | 已確認（沿用 case_no 關聯規則） |
| `lifecycle_event_id` | `BIGINT UNSIGNED NOT NULL` | 指向同交易剛建立的狀態事件，供追溯投影起因。 | 稽核／關聯 | 取 INSERT `order_lifecycle_state_events` 的 `lastrowid`。 | lifecycle persistence。 | 本次不可變的 `order_lifecycle_state_events` 紀錄。 | 保存投影起因；必須與同列 `case_no` 屬同一訂單。worker 不讀取它作警示內容或業務判斷。 | lifecycle persistence | 每一個 projection intent | 建立後不可修改 | FK 指向狀態事件但不單獨保證和 case_no 同案，需 persistence 保證。 | 已確認（技術關聯） |
| `intent_key` | `VARCHAR(191) NOT NULL` | 同一訂單的投影意圖去重鍵。 | 衍生技術識別 | `lifecycle:` + canonical JSON `{command_idempotency_key, scope, alert_code, action}` 的 SHA-256。 | lifecycle application 產生的 intent。 | 原命令冪等鍵、evaluator 輸出的 `scope`／`alert_code`，以及由 active 狀態衍生的 `action`。 | 僅作重試去重；由受控輸入計算，不可由 caller 指定，不參與訂單狀態、薪資或其他業務計算。 | lifecycle application／persistence | projection state materialize | 建立後不可修改 | DB 對 `(case_no, intent_key)` 唯一，防止重複副作用。 | 已確認 |
| `scope` | `ENUM('enter_service', 'auto_complete') NOT NULL` | blocker 警示所屬狀態機範圍；與 `alert_code` 組成完整投影語意鍵。 | 衍生領域決策輸出 | 不計算；限列舉值。 | lifecycle evaluator 的 projection state。 | 狀態機依根事實決定各 transition scope 的 blocker 是否 active。 | 必須保留：即使移除 `human_hold`，如 `legacy_cancellation_requires_review` 仍可存在兩個 scope，不能只由 code 推得。僅由 evaluator 產生；outbox 不得反向改變狀態。 | lifecycle evaluator | 每一個 projection state | 建立後不可修改 | worker 必須以 `(scope, alert_code)` 對公開 registry 驗證；human_hold 投影列為長期移除範圍。 | 已確認（人類暫停理由已更新） |
| `alert_code` | `VARCHAR(191) NOT NULL` | 要開啟或解除的受控 blocker 警示類型；須與 `scope` 配對。 | 衍生領域決策輸出 | 不計算；必須在 projection registry。 | lifecycle evaluator 的 projection state。 | 狀態機依付款、實際開案、正式指派、控制命令等根事實得出的 blocker 類型。 | 僅由 evaluator 依根事實輸出；outbox 不得自行造出 code，亦不得反向改變訂單狀態。 | lifecycle evaluator | 每一個 projection state | 建立後不可修改 | 若非受控 registry 成員，worker 必須 permanent fail。 | 已確認 |
| `action` | `ENUM('open', 'resolve') NOT NULL` | 對系統警示的目標副作用。 | 衍生技術投影 | active blocker → `open`；inactive blocker → `resolve`。 | lifecycle persistence 由 evaluator projection state materialize。 | lifecycle evaluator 依根事實得出的該 `(scope, alert_code)` active／inactive 結果。 | 完全由 blocker 有效性衍生；outbox 僅套用警示投影，不得反向變更 blocker、根事實或訂單狀態。 | lifecycle persistence | projection state materialize | 建立後不可修改 | 允許 caller 直接指定會使警示與狀態機漂移。 | 已確認 |
| `payload_hash` | `CHAR(64) CHARACTER SET ascii COLLATE ascii_bin NOT NULL` | payload 的衍生完整性檢查值。 | 衍生完整性檢查／待移除 | SHA-256(canonical UTF-8 `payload_snapshot`)。 | lifecycle persistence `_intent_storage`。 | `payload_snapshot` 的 canonical JSON；其內容根源為 evaluator 的 blocker projection state。 | 長期考慮移除。非安全簽章、非業務來源；若 payload 縮為固定規格，worker 直接驗證內容即可。 | lifecycle persistence | intent materialize | 建立後不應修改 | 能改 DB 者可連 payload 與 hash 一起改；目前僅能偵測不一致。 | 已確認：長期考慮移除 |
| `payload_snapshot` | `JSON NOT NULL` | worker 執行警示投影的衍生 payload；目前唯一內容為 `reason_code`。 | 衍生技術投影／待移除 | canonical JSON `{ "reason_code": "<scope>.<alert_code>" }`。 | evaluator projection state payload。 | 同列 `scope` 與 `alert_code`。 | 長期考慮移除；worker 直接由 `scope` 與 `alert_code` 組成 reason code，不可被當成目前領域事實。 | lifecycle evaluator／persistence | intent materialize | 建立後不應修改 | 以 JSON 保存可直接推導的一個字串，增加欄位、hash 與驗證複雜度。 | 已確認：長期考慮移除 |
| `status` | `ENUM( 'pending', 'processing', 'projected', 'failed' ) NOT NULL DEFAULT 'pending'` | outbox worker 的技術處理生命週期。 | 技術處理狀態 | INSERT=`pending`；claim→`processing`；成功→`projected`；錯誤→`failed`。 | projector worker 與 DB default。 | 是否已成功投影該固定工作項目的技術事實。 | outbox 保留時必須保留，用於可靠重試與互斥處理；不等於訂單狀態，不得影響 lifecycle evaluator。 | alert projector worker | claim、成功、失敗 | `projected` 完成後不得回寫為待處理；重送應建立另一個唯一 intent。 | DB CHECK 限制搭配的時間／錯誤欄位形狀。 | 已確認 |
| `attempt_count` | `INT UNSIGNED NOT NULL DEFAULT 0` | 已 claim 的投影嘗試次數。 | 技術重試計數 | 每次成功 claim 加 1；最多 5 次。 | alert projector worker。 | worker 對固定 outbox 工作項目的實際 claim 次數。 | 僅服務重試上限與退避計算；不是業務事件次數，不得影響訂單狀態或狀態機。 | alert projector worker | claim | `projected` 後固定；不得任意回寫降低以繞過上限。 | 重試上限屬技術營運設定，非領域規則。 | 已確認 |
| `next_attempt_at_utc` | `DATETIME(6) NULL` | failed 工作項目的下次可重試 UTC 時刻。 | 技術重試排程 | 依嘗試次數使用 60／300／1800／7200 秒退避；claim 或成功時清空。 | alert projector worker。 | worker 對固定 outbox 工作項目的失敗時刻與技術退避設定。 | 僅決定 worker 何時可再重試；必須是 UTC，不得用於業務日期、薪資或訂單狀態。 | alert projector worker | 可重試失敗、claim、成功 | 成功投影後保持 NULL；不得用來重開已完成工作。 | 將其換算為台灣日期會錯置技術排程與業務時間。 | 已確認 |
| `locked_at_utc` | `DATETIME(6) NULL` | worker 成功 claim 後的持久化 claim／CAS 時刻。 | 技術互斥控制 | claim 時為 worker 的 UTC now；完成時清空。 | alert projector worker。 | worker 對固定 outbox 工作項目的成功 claim 時刻。 | 僅用於 worker 互斥與 CAS 驗證；`processing` 時必須有值，完成時清空；不是業務鎖、排班鎖或訂單鎖。 | alert projector worker | claim、成功 | 不得由業務命令改寫。 | worker 重啟或多實例時仍須由 DB 持久資料協調。 | 已確認 |
| `projected_at_utc` | `DATETIME(6) NULL` | 成功投影至系統警示的 UTC 完成時刻。 | 衍生技術稽核／待移除 | `status='projected'` 時的最後一次更新時間。 | alert projector worker。 | `status` 轉為 `projected` 的技術更新；若 projected 後禁止更新，等同 `updated_at`。 | 長期考慮移除；以 `status` 與 projected 後不可再更新的 `updated_at` 表示成功投影，不代表訂單狀態生效時刻。 | alert projector worker | 成功投影 | projected 後不得再更新該列。 | 重複保存成功時間增加欄位與 CHECK 複雜度。 | 已確認：長期考慮移除 |
| `last_error` | `VARCHAR(1000) NULL` | 最近一次投影失敗的維運診斷文字。 | 技術診斷資料 | 失敗時為截斷 1000 字的例外訊息；成功時清空。 | alert projector worker。 | worker 執行固定 outbox 工作項目時的最近一次技術失敗。 | 僅保存維運診斷，不是業務 reason 或使用者可見訊息。不得再存 claim／CAS token；互斥識別另由技術機制處理。 | alert projector worker | 失敗、成功 | projected 後不得再以新錯誤覆寫。 | 現況 claim 時暫存 `claim:` SHA-256 token，與欄位語意衝突，列為需修正的實作漂移。 | 已確認 |
| `created_at` | `TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6)` | DB 成功建立 outbox 工作項目的技術稽核時間。 | 稽核／系統事實 | DB current timestamp。 | INSERT。 | DB transaction commit path。 | 僅作稽核與排程觀察；不得由 caller 指定或修改，不等於訂單狀態、警示生效或業務日期。 | Repository | INSERT | 建立後不可修改 | 不得拿來推導 lifecycle decision。 | 已確認 |
| `updated_at` | `TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6)` | 任一 outbox 技術更新的 DB 時間。 | 衍生系統投影 | DB ON UPDATE current timestamp。 | 任一 UPDATE。 | DB 實際寫入事件。 | 僅反映最後技術寫入；不可作業務事件時間、重試依據或訂單狀態判斷。 | DB | claim、成功、失敗等 UPDATE | 可變；`projected` 後應禁止再更新。 | 需與 `projected_at_utc` 的長期移除方案一併保證 projected 後不可再寫。 | 已確認 |
