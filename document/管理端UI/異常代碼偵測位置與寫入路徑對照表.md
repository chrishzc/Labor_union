# 異常代碼偵測位置與寫入路徑對照表

> **建立時間**：2026-08-09
> **適用範圍**：異常警示中心（`ui/pages/06_finance_alerts.py`）所讀取的 canonical 異常註冊表 `anomaly_current_alerts`
> **對應程式**：`domains/anomalies/registry.py`（異常定義目錄）＋ 各 `subsystems/anomalies/*.py`／`infrastructure/mysql/*_anomaly_source.py`（實際偵測規則）

本表列出目前 `default_anomaly_registry()` 註冊的全部異常代碼，說明每個代碼的偵測時機（事件觸發同步偵測，或背景迴圈定期重新掃描）與資料寫入路徑（直接寫進 canonical，或先經過中繼表再由背景迴圈投影）。

## 判斷原則

- **事件觸發型**：正式流程以 durable outbox 隔離業務交易與異常投影；歷史 HCM CLI 是已知相容例外，仍同步投影，但重跑既有案件時會補投影未成功的驗證異常，不把同步例外誤寫成通則。
- **狀態掃描型**：偵測本身就是背景迴圈定期主動重新計算現況（例如「現在有沒有月嫂檔期重疊」），沒有依附任何業務交易 → 直接寫進 canonical，不需要中繼表這層緩衝。

## 對照表

| 代碼 | 代碼說明 | 偵測位置 | 寫進哪裡 |
|---|---|---|---|
| ORDER-001 | 訂單未配對月嫂－資訊-1未發送 | 背景迴圈每 60 秒整批重新掃描（process_reminder_anomaly_source.py） | 直接寫進 canonical anomaly_current_alerts，無中繼表 |
| ORDER-002 | 訂單未配對月嫂－資訊-2未發送 | 同上 | 同上 |
| ORDER-003 | 已發資訊-1但候選人未回覆 | 同上 | 同上 |
| ORDER-004 | 已發資訊-2但候選人未回覆 | 同上 | 同上 |
| BECLASS-001 | 客戶尚未填寫 BeClass 問卷 | 同上 | 同上 |
| DOC-SEND-001 | 履歷尚未發送給客戶 | 同上 | 同上 |
| RECEIVABLE-001 | 客戶應收帳款逾期未收齊 | 同上 | 同上 |
| CLIENTPAYABLE-001 | 應付客戶款項逾期未付 | 同上 | 同上 |
| RETURN-001 | 補助款應退還客戶逾期未退 | 同上 | 同上 |
| SUBSIDYADVANCE-001 | 政府補助墊付款項待核對 | 同上 | 同上 |
| SCHEDULE-001 | 服務檔期跨國定假日，行政尚未決策放假與否 | 同上 | 同上 |
| SCHEDULE-002 | 月嫂服務中途更換人員，需人工複核財務拆分 | 同上 | 同上 |
| SCHEDULE-003 | 月嫂檔期重疊/雙重預約 | 同上 | 同上 |
| SCHEDULE-005 | 國定假日休假偏好衝突 | 同上 | 同上 |
| LINE-001 | 客戶尚未綁定 LINE | 同上 | 同上 |
| LINE-002 | 月嫂群組任務逾期未回覆 | 同上 | 同上 |
| LINE-004 | LINE 帳號重複綁定衝突 | 同上 | 同上 |
| LINE-005 | 服務人員尚未綁定 LINE | 同上 | 同上 |
| IMPORT-001 | BeClass 匯入欄位驗證失敗 | 匯入當下同步偵測（import_staff_beclass.py／import_client_beclass.py） | 先寫進中繼 beclass_import_review_outbox，再由背景迴圈投影進 canonical |
| IMPORT-003 | 跨表整合去重/關聯衝突 | 同上 | 同上 |
| IMPORT-004 | HCM 匯入欄位驗證失敗 | 歷史 HCM CLI 匯入當下同步偵測；既有案件重跑會補投影 | 直接寫 canonical；屬相容例外，投影失敗時該列維持 review_required，允許重跑修復 |
| IMPORT-006 | 銀行對帳匯入完整性異常 | 初次批次完成時同交易投影；正式 historical reprocess 由既有 finance_import_outbox 事件重播 | 僅寫 canonical anomaly_current_alerts；不再雙寫 system_alerts，也不以每分鐘全表掃描刷新 |
| finance_import_manual_review | 銀行對帳需人工分類 | 正式財務匯入流程（ingestion.py）匯入當下寫進 finance_import_outbox；人工更正/退款覆核操作也會寫入 | 背景迴圈近即時消化，直接寫進 canonical（FINANCE_OCCURRENCE 投影模式） |
| CLIENTREFUND-001 | 客戶退款案件下游異常 | 同上 | 同上 |
| PAYOUT-001 | 服務人員應付款逾期未匯 | 背景迴圈分頁式重新掃描（staff_payables_anomaly_source.py） | 直接寫進 canonical，無中繼表 |
| PAYOUT-002 | 服務人員應付款異常變更 | 同上 | 同上 |
| PAYOUT-003 | 服務人員銀行帳戶資料異常 | 同上 | 同上 |
| GOVSUB-001 | 政府補助收款無唯一批次 | 背景迴圈分頁式重新掃描（government_subsidy_anomaly_source.py） | 直接寫進 canonical |
| GOVSUB-002 | 政府補助收款分配歧義 | 同上 | 同上 |
| GOVSUB-003 | 政府補助批次完整性異常 | 背景迴圈分頁式重新掃描（government_subsidy_integrity_anomaly_source.py） | 直接寫進 canonical |
| GOVSUB-004 | 政府補助沖正異常 | 背景迴圈分頁式重新掃描（government_subsidy_reversal_anomaly_source.py） | 直接寫進 canonical |
| GOVSUB-005 | 政府補助請款資料漂移 | 背景迴圈分頁式重新掃描（government_subsidy_assignment_drift_anomaly_source.py） | 直接寫進 canonical |
| SCHEDULE-006 | 服務天數與實際排班天數不平衡 | 背景迴圈分頁式重新掃描（scheduling_coverage_anomaly_consumer.py） | 直接寫進 canonical |
