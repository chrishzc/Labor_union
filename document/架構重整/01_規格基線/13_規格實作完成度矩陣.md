# 規格實作完成度矩陣

> 稽核日期：2026-08-04  
> 本文件逐項區分「規格已確認」、「production wiring 已存在」、「測試證據已存在」與
> 「release gate 已關閉」。`partial`、`missing` 或 `external-gate` 不得因其他測試全綠
> 而改標為完成。
>
> **2026-08-04 formal baseline reset：** 本矩陣 2026-08-03 的長篇 live／UI／integration
> 敘述保留為歷史線索，不再單獨構成現況驗收。新的可重跑入口是
> `scripts/validate_formal_architecture_baseline.py`，其 receipt 為
> `evidence/formal_baseline_v1.json`。它以現況 source 產生 683 個 writer finding、
> fingerprint 以該 receipt 為準，並驗證
> `client_payments.subsidy_refund_*` 的 runtime caller 為 0、legacy
> `subsidy_return_reconciliation` 已不存在且無 runtime caller、必要 API routes、schema parts
> 與關鍵測試 source。所有 writer finding 仍是 `blocked`，不能由 count 或舊矩陣文字推論
> 為可移除或 production-ready。

## 0. 2026-08-04 可驗收基線

| Scope | 現況狀態 | 可重跑證據 | 未關閉 gate |
|---|---|---|---|
| Writer Inventory | `evidence-only` | `writer_inventory_v3_candidate.*` | 697 findings 全數仍為 blocked，尚未完成逐項 disposition／caller／replacement receipt；`client_payments.subsidy_refund_*` 已明確標為 legacy projection、runtime caller=0、非 Client Finance SSOT。舊 v1 baseline 的掃描根仍包含已退役 `services`、未涵蓋 `domains/subsystems`，且未能覆蓋 canonical durable-job repository；只供歷史追溯，不得作現況驗收。 |
| Python regression baseline | `proven-at-2026-08-04` | `.venv\Scripts\python.exe -m pytest -q --basetemp C:\tmp\labor_union_pytest_full_20260804_escalated` | `830 passed, 5 skipped`；預設 Windows user temp ACL 會產生 setup-only `PermissionError`，不得把該執行環境錯誤誤判為業務 regression。 |
| `subsidy_refund_*` | `proven-boundary` | runtime boundary test、formal baseline runtime caller=0 | 欄位仍在 legacy `client_payments` projection；不得成為 Client Finance、Finance Import 或 UI SSOT。 |
| legacy `subsidy_return_reconciliation` | `retired` | formal baseline path absent＋caller=0 | 已移除原三個 legacy writer；不得重新建立或以 compatibility adapter 復活。 |
| 一般客戶退款 | `partial` | partial allocation、退匯 target／replay focused tests；真實台新 Excel→manual correction→原退款 ledger→銀行退匯入款→`refund_reversal`→原應付重開的隔離 MySQL E2E；2026-08-04 新隔離重跑已驗一般退款 `1 passed in 13.82s`、退匯與不等額不自動打平 `2 passed in 24.78s`。退款退回覆核的 schema／typed API／outbox／`CLIENTREFUND-001` projector 有 disposable MySQL E2E `1 passed in 9.66s`：建立 review 不會先寫 `refund_reversal`，且只有同一 bank row＋同一原 refund 的正式 `refund_reversal` 存在後才 auto-resolve。案件頁只顯示 case-owned obligation，未綁定案件的銀行列只能在 Finance Import correction 以理由、證據、義務與 Preview／Apply 操作。 | 尚缺外部付款失敗根事實、完整 UI 資料流程／Loading trace 與 17 類 Global scenario。候選辨識的真實資料正確率／誤配率與重匯驗收仍未完成；目前沒有可再次隔離使用的真實對帳單。 |
| 客戶補助退還／墊付 recovery | `partial` | advance／recovery／outbox focused tests；真實台新 Excel＋隔離 MySQL 已驗正式 ledger、claim-item allocation、第一季首月結案的墊付、Government outbox exact recovery、不產生第二筆 payout 與 mismatch anomaly；2026-08-04 新隔離重跑 `1 passed in 14.62s`。 | 尚缺可發布 migration release、補助墊付 task queue、完整 UI 資料流程／Loading trace。 |
| Finance Import historical reprocess | `partial` | typed Preview／Apply、append-only receipt／outbox schema、focused tests；既有真實台新 Excel 紀錄可重現現行 classifier 覆蓋不足的 trace | 尚缺可重播的候選辨識／重匯偵測實作與經驗證的 historical owner selection／case evidence contract。現階段沒有可再次隔離使用的真實對帳單，故不得宣稱新候選規則已通過真實資料驗收；待取得樣本後必須完成完整 owner Apply E2E、Streamlit 操作入口與可發布 migration release。 |
| Global G01–G17 | `partial` | `evidence/global_e2e_manifest.json` | G04、G06、G07、G08、G09、G10、G11、G12、G16、G17 已以 disposable MySQL traceable scenario 驗收；G01、G02、G03、G13、G14 為 partial，G05、G15 尚缺可追溯 Global E2E。 |
>
> 2026-08-03 最新驗證：非整合回歸為 `3273 passed, 2 skipped, 74 deselected`；
> 使用只具 `lu_test_%` 權限的 `lu_test_runner` 於隔離 MySQL 8.4 容器完成
> 73 個 integration scenarios。剩餘 2 個 preserve-data／recovery gate 需要明確的
> 隔離 source 環境檔，不能把正式 `.env` 與另一個測試容器混用。最新 API／Streamlit 重啟後，
> 六個主頁及 Orders、Scheduling、Anomalies、LINE 各工作區均完成實際
> Query／Preview 巡檢，未出現 traceback 或 HTTP 500。
> Finance Import 批次 Manifest／待確認資料／歷史 Run 也已用實際 Streamlit
> 驗證不存在批次會顯示 typed 404，而非 500 或空資料猜測。
> 實際 UI 另完成王洋／案件 115000028 的出勤天數精算 Preview，後端回傳
> 20 個服務日、0 個休假日與 2026-08-08 預計完工日；2026-08 應付帳款匯出亦已
> 實際產生並歸檔 XLSX。Calendar 無效日期改為 fail closed，精算案件選單只顯示
> 所選月嫂當月正式 assignment，避免把錯案或錯誤日期當成可操作事實。
> Finance Import 人工修正 UI 亦已實際選擇 `government_subsidy` 與指定批次，
> 不存在 row 由 API 回 typed `409 finance_import_row_not_found`，未產生 HTTP 500
> 或正式帳務寫入。
> 異常中心的 Government Subsidy、Staff Payables、Scheduling owning actions
> 已能帶入批次／銀行列／原入款、月嫂或案件識別並導向既有 Query／Preview UI；
> 導向契約、目標 widget 與政府補助關聯批次 Query 共 31 個聚焦測試通過。
> 實際 Streamlit 另重跑客戶收款、Payroll、月嫂付款、2026-08 應付帳款、
> 補助清冊、案件人力配置、表單、資料瀏覽、異常及 LINE 狀態頁；後端日誌均為
> 2xx 或預期的 typed 4xx，未出現 HTTP 500。
> Orders／Scheduling 的 orders、staff 參考清單已改為 15 秒 defensive-copy
> 短快取，且案件人力配置直接重用 Scheduling shell 的參考資料。實際重啟後，
> Orders 連續工作區切換由約 2.6～7.3 秒降為 0.55～0.58 秒，檔期投影與配對中心
> 約 0.57 秒，案件人力配置由約 7.1 秒降為約 3.1 秒；後者仍保留不可快取的
> authoritative bootstrap-status 查詢。
> 服務人員月曆、月嫂配對中心與案件人力配置已改讀正式 bounded Orders
> summary Query；選定出勤案件後才另查固定排休條款，且只接受
> `週休1日`、`週休2日`、`連續服務` 三個 SSOT 值。實際瀏覽器以楊華／
> 案件 115000040 完成出勤 Preview：15 個服務日、17 個日曆日、2 個休假日、
> 預計完工日 2026-08-06；API 日誌未再出現 Calendar 讀取舊全量 `/orders`。
> GOVSUB-001～005、SCHEDULE-006、PAYOUT-001～003 source projector 已接入
> production background worker；異常層聚焦回歸 `137 passed`，新增來源與
> Finance Import 收斂鏈的隔離 MySQL 合併回歸 `38 passed`。實際重啟後 worker
> 無錯誤，異常中心 Query 為 HTTP 200。
> 目前 50 筆 legacy orders 尚未 Apply 新架構 bootstrap，UI 會以 Preview gate 明確呈現，
> 這是資料準備狀態而不是以 fallback 隱藏。

## 1. 狀態定義

| 狀態 | 定義 |
|---|---|
| `proven` | production 入口、正式 owner、持久化與相應分層測試均有可重現證據 |
| `partial` | 部分層級已完成，但正式入口、交易、持久化或指定層級驗收仍缺一項以上 |
| `missing` | 規格已確認，但尚無符合新架構的 production capability |
| `conflict` | live 實作繞過或違反已確認 SSOT／交易邊界 |
| `decision-required` | 現有規格不足以唯一決定 owner 或 public contract，需人工裁決 |
| `external-gate` | 必須在專用部署或全隔離環境取得證據，不能以 unit test 推定 |

## 2. Orders／Scheduling／Payroll

| Capability | 狀態 | 目前證據／缺口 |
|---|---|---|
| Orders Terms Preview／Apply | `proven` | 已有 pure candidate、outer workflow、MySQL repository、typed API／client／薄 panel、replay／stale／rollback 與真實 MySQL 測試。 |
| Contract-completed root event | `proven` | typed Query／Preview／Apply、不可變契約事件、Orders lifecycle 與 Client Finance 三期義務共用 outer UoW；API／薄 UI、exact replay、stale、rollback 與隔離 MySQL 測試存在。 |
| Actual Start Preview／Apply | `proven` | 已綁定目前 deposit settlement identity，原子重建 Scheduling、Client Finance、Payroll 與 lifecycle。 |
| Cancellation Preview／Apply | `proven` | 已由逐日服務日期＋月嫂根事實重建；完整履約後固定 blocker 且零寫入；Global candidate 驗收已有兩個核心案例。 |
| Controlled Reopen | `proven` | `ReopenEligibility`、Preview／Apply、不可變 reopen event、receipt、typed API／薄 UI 與真實 MySQL replay／rollback 測試存在。 |
| Orders Query read model | `partial` | 正式 bounded summary Query 具固定 11 個列表欄位、`page_size ≤ 200`、`case_no` keyset cursor、有效 assignment 月嫂聚合、representation ETag／304、typed errors 與 Streamlit 短快取；Module／Repository／Router／Client／UI 及隔離 MySQL keyset／EXPLAIN 測試存在。Orders 主頁、服務人員月曆、月嫂配對中心與案件人力配置均已接 summary Query；出勤選案另以 typed selected-detail Query 讀取固定排休 SSOT，合法值僅 `週休1日`／`週休2日`／`連續服務`。Form Management 等其他 legacy list caller、超過單頁 200 筆時的 Scheduling UI 分頁與完整單案 detail 投影仍待完成。 |
| Availability Query | `partial` | live availability／matching services 可查詢，unexpected failure 與已知 case／stage blocker 已回 typed error；尚未完整收斂成規格的 Scheduling Query port。 |
| Waiting Deposit Lock commands | `proven` | acquire 與 release 均有 read-only Preview、fingerprint、typed Apply、replay／stale／conflict／blocker、唯一後端 owner 與薄 UI；服務區間加固定 7 日緩衝同時進入 availability Query。舊 acquire／release／conversion mutation routes 已回 410，聚焦 Module／Service／Router／Client／UI 共 100 個測試通過。 |
| First Assignment bootstrap | `proven` | Case Import 原子建立 Client Finance／Payroll accounts、付款條款、rate policy 與 Scheduling generation-0；第一個正式 assignment 必須轉換同案 waiting-deposit lock，並重驗契約完成、服務時間與正式訂金核銷，不允許由 generation-0 直接繞過。隔離 MySQL 8.4 E2E 已證明 20 個 assignment-owned 服務日、lock conversion、Finance／Payroll／Orders 版本與 replay transaction。 |
| Assignment Plan Preview／Apply | `proven` | 新 Domain／Subsystem／MySQL／API／UI 與 replay、mutex、stale、rollback 測試已存在。 |
| Leave／Substitution Preview／Apply | `proven` | 已使用 assignment-owned exact service dates、取消舊 assignment 並建立新 generation，跨域重算已接入。 |
| Calendar Read | `proven` | canonical current projection、MySQL repository、bounded typed API／client／薄 panel 與隔離 MySQL 測試存在。 |
| Assignment status projector | `proven` | current projection 依第一個／最後一個正式服務時刻推導 `planned`／`active`／`completed`，不依賴 legacy DB status。 |
| Payroll root-fact calculation | `proven` | 薪資只消費有效 assignment-owned 正式服務日與 rate snapshot；不 fallback 至 `planned_hours` 或 `orders.staff_id`。 |
| Payroll adjustment | `proven` | typed Preview／Apply、整數守恆、allocation、API／UI 與分層測試存在。 |
| Standalone Payroll rebuild | `proven` | workflow、MySQL repository、typed API／client／薄 UI、同月多訂單加總、replay／rollback 與隔離 MySQL 測試存在。 |
| Payroll／Scheduling legacy writer exit | `partial` | 正式配對 UI 已統一使用 1～4 人 Matching Plan；legacy match-record create、資訊發送、意願、履歷與 single-staff writers 均為 typed 410 Gone。人工 `actual_hours` 覆寫／付款前確認服務已改為 fail-closed compatibility shim，既有 adjustment 歷史只讀；Data Browser Orders writer、daily schedule 與 `fix_schedule_conflicts --repair` 也已關閉或改為唯讀。仍須逐一證明其他 inventory `legacy_active` caller 已改接 typed adapter 或 Gone。 |

## 3. Client Finance

| Capability | 狀態 | 目前證據／缺口 |
|---|---|---|
| Payment terms／三期 obligation planning | `proven` | Case bootstrap 建立 account／付款條款；Contract Completion Preview 顯示三期日數、金額、到期日並以同一 outer UoW 建立首次 obligations；Terms Apply 後續重算未核銷義務。2026-08-04 已恢復 Orders Terms canonical workflow，隔離 MySQL 證明同一 Apply 依序寫 Scheduling、Client Finance、Payroll、Lifecycle、Orders receipt，並以相同 idempotency key replay 而不重寫；service-day 數量變更若缺安全重分配計畫會明確阻擋，不會臆造排班。 |
| Client receipt exact Preview／Apply | `proven` | MySQL repository、outer UoW、ledger、M:N allocation、exact-zero projection、typed API／UI 與真實 MySQL 測試完整。 |
| Immutable client ledger schema | `proven` | receipt／refund／adjustment／reversal、reversal reference、allocations 與 append-only triggers 已存在。 |
| Client refund Preview／Apply | `partial` | 單案 refund obligation、immutable ledger、Preview／Apply、API、partial allocation；`client_refund_return` 以 Finance Import manual correction 委派 borrowed Client Finance UoW，鎖定銀行入款、原 refund ledger 與 allocation，append `refund_reversal`、重開原應付且有 replay；真實台新 Excel＋隔離 MySQL 已驗證。案件頁在 legacy client-payment summary 缺席時仍讀取 canonical 一般退款與補助退還，但僅顯示本案 obligation；未關聯 case 的銀行列不可直接配對。Finance Import 工作區已有 typed correction Preview／Apply、理由與證據欄位，退款退匯另保留原退款 ledger identity。尚缺 refund failure、canonical anomalies、全域待辦、完整 UI 資料流程／Loading trace 與一般退款／subsidy-return 匯出分流。詳見 `16_Staff_Payables與Client_Refund正式規格.md`。 |
| Client adjustment Preview／Apply | `proven` | `client_only`／`client_and_staff` scope 已明確分離；client-only 只驗 Client Finance version，寫不可變調整與客戶義務／outbox，不建立 Payroll side effect。typed API／client／薄 UI、exact replay、stale、rollback 與隔離 MySQL 測試存在。 |
| Client reversal Preview／Apply | `proven` | dedicated Preview／Apply 已驗 target、金額上限與禁止 reversal-of-reversal，並具 API／UI、immutable ledger、replay／rollback 與隔離 MySQL 測試。 |
| Client subsidy return／union advance | `partial` | `subsidy_return` 是獨立 payable；Finance Import posting 會在第一個 claim-quarter 月結案、結案月＋2 曆月 15 日且尚無 receipt allocation 時寫 `subsidy_advance`，否則寫 `subsidy_return`。後續 Government allocation 只會建立 immutable recovery；amount mismatch 則建立人工審核 anomaly。隔離 MySQL 已驗 exact recovery 不新增第二筆 payout，以及 mismatch 不自動打平。`client_payments.subsidy_refund_*` 是無 runtime caller 的 legacy projection，非 SSOT。尚缺 task queue／UI 操作入口、真實 Excel 到補助分支的全鏈路與 preserve-data release。 |
| Client Finance stable errors | `proven` | Client Finance workflows 與跨 Domain version gate 已統一輸出 `idempotency_conflict`／`client_finance_candidate_stale`；API 邊界保留 `idempotency_mismatch`、`client_finance_version_conflict`、`financial_adjustment_candidate_stale` 與 `stale_preview` 的相容映射。Module、Subsystem、Router 共 81 個聚焦測試通過，實際 Streamlit 客戶收款 Query 已載入且後端為 HTTP 200。 |

## 4. Staff Payables／Accounts Payable Export

| Capability | 狀態 | 目前證據／缺口 |
|---|---|---|
| Staff payout exact Preview／Apply | `proven` | typed API／UI、immutable payout event、obligation links、projection、outbox、replay／rollback 與真實 MySQL 主路徑測試存在。 |
| Staff return／reversal | `proven` | Domain／Subsystem／repository／API／UI 與隔離 MySQL E2E 存在；已驗 immutable event／link、projection 重開、exact replay、outbox／receipt 及中途失敗整筆 rollback。 |
| 同月同月嫂合併匯出 | `proven` | read-only snapshot 依 staff＋日期合併；completed／anomaly 排除；不建立月結 entity。 |
| XLSX archive | `proven` | 使用者下載 bytes 與系統 archive bytes 相同；固定保存至 `downloads/accounts_payable_archive/YYYY`、不覆蓋並驗證 hash。 |
| PAYOUT-001／002／003 canonical anomaly | `proven` | canonical registry 已固定 source Domain、fingerprint、severity、projection kind、顯示根事實及 owning action routing；bounded root-fact sources、caller-owned cursors、active／inactive recovery、borrowed UoW 與 production worker wiring 均有 Module／MySQL 測試。 |
| Legacy payout writers exit | `partial` | legacy `staff_payments/transaction` 已固定回 410，正式 UI 只走 Staff Payables Preview／Apply；舊 Finance Import apply 與 historical reprocess apply 已 fail closed，production caller 只保留 dry-run 診斷。仍須從 inventory 逐一關閉或移除殘留 legacy writer code，才能標為 `proven`。 |

## 5. Government Subsidy

| Capability | 狀態 | 目前證據／缺口 |
|---|---|---|
| Claim Planning Preview／Apply | `proven` | 有效 assignment＋正式服務日×每日時數是 hours 根事實；一般市民 300、補助市民 350 由後端推導，非資格排除。Domain／Subsystem／MySQL repository、typed API／client／薄 UI 與隔離 MySQL replay／rollback 測試存在。 |
| Submission／Approval | `proven` | 送件與逐 item 核准皆保存不可變事件；requested snapshot 不覆寫，approval 以 CAS 更新投影，具 Preview fingerprint、冪等 receipt、outbox 與 append-only triggers。 |
| Receipt／allocation／reversal | `proven` | 正式 ledger、M:N allocation、同批次與 exact-total invariant、退匯重開 outstanding、typed API／UI、borrowed UoW 與隔離 MySQL 測試存在。 |
| Preserve-data release wiring | `proven` | additive `134_government_subsidy_claim_workflow.sql` 已納入 release manifest 與 owned-object descriptors；無 DROP TABLE／TRUNCATE。 |
| GOVSUB-001～005 canonical definitions／projectors | `proven` | 五類 definition、fingerprint roots、severity、display fields 與 owning action 已固定；缺批次／部分入款歧義、批次帳與 ledger 完整性、政府沖正及 frozen claim item 對 assignment 正式服務根事實漂移均由 bounded source projector 推導。每類皆具 deterministic event、active／inactive recovery、borrowed UoW、隔離 MySQL 測試與 production worker 獨立游標；UI 能帶入批次、銀行列與原入款至 owning Domain Query／Preview。 |

## 6. Finance Import

| Capability | 狀態 | 目前證據／缺口 |
|---|---|---|
| Existing-batch Preview／Apply | `proven` | typed workflow、fresh lock、fingerprint、owning-Domain resolver、outer UoW、audit／outbox／receipt、API／薄 UI、replay／rollback 與隔離 MySQL tests 存在。 |
| Source Intake／Normalization | `proven` | production ingestion 保存 canonical bank facts、batch contract、initial classification events、receipt 與 outbox；CLI 預設只入庫待確認，不直接正式入帳。 |
| Batch contract／initial classification events | `proven` | ingestion 原子建立 batch contract、不可變 classification event、root-fact outbox 與 replay receipt；隔離 MySQL 已驗證。 |
| Manual correction＋posting：client receipt | `proven` | correction Preview／Apply 透過 Client Finance borrowed UoW 寫正式 ledger；異常中心會帶入 canonical row 到 correction Preview 工作區。 |
| Manual correction＋posting：staff payout | `proven` | correction Preview／Apply 透過 Staff Payables borrowed UoW，保留 exact-zero、replay、rollback 與 typed error。 |
| Manual correction：client subsidy return | `partial` | correction 與正常 dispatch 都以銀行 row identity 委派 Client Finance；一般 refund 與 subsidy return 的 classification、obligation、API 與 UI 均隔離，無唯一 owner 時維持人工處理而不猜測。當唯一補助義務具 claim link 時，posting 會以 allocation 事實選擇 normal return 或 union advance；真實台新 Excel→墊付→Government allocation outbox→recovery E2E 已通過，尚缺 UI task queue／Loading trace。 |
| Manual correction：government subsidy | `proven` | correction Domain 以 `government_subsidy` 獨立 owner 驗證單一選定批次與銀行整數金額完全相等；Repository 從 approved／partially-paid batch 的 outstanding 根事實載入並鎖定，production composite 以 borrowed UoW 委派 Government Subsidy Receipt Preview／Apply。唯一核准批次仍可自動 resolve；人工選批次、owner mismatch、typed fail-closed 與薄 UI 均有分層測試及實際瀏覽器證據。 |
| Historical Reprocess Preview／Apply | `partial` | legacy reprocess `--apply` 仍 fail closed；canonical typed workflow 以同一 plan 執行 preview／apply、expected-version、fingerprint、idempotency、outer UoW、owning-Domain dispatch、append-only classification event／run／receipt／outbox，並已掛載 authenticated API。`139_finance_import_historical_reprocess.sql` 為 additive schema contract。尚缺 isolated MySQL 與真實格式 Excel E2E、UI、release manifest。詳見 `27_Finance_Import_Historical_Reprocess_Completion_Decision_Package.md`。 |
| Query Batch／Run／Manifest／review scan | `proven` | authenticated Query 均有上限 100 與 cursor；Batch 區分正式 contract 與 legacy 稽核資料，Manifest、待確認 row scan 與歷史 Run 只回 privacy-safe read model，不含帳號、姓名、memo 或 raw payload。API、typed client、薄 UI 與 29 個聚焦測試存在；實際 Streamlit 已驗證不存在正式批次 fail closed 為 typed 404。 |
| Legacy `--apply` shutdown | `proven` | Finance ingestion CLI 預設及相容 `--apply` 都只保存銀行根事實與待確認批次；historical reprocess `--apply` 明確拒絕。正式帳務只能走 typed Preview／Apply。 |
| 真實格式 Excel＋全隔離 MySQL Global E2E | `partial` | 真實台新 Excel 已驗 ingestion→canonical root fact→historical reprocess fail-closed、人工一般退款 correction→ledger/allocation→Anomalies resolution、退款退匯入款→`refund_reversal`→原應付重開且 replay，以及同一筆補助 payout 的 correction→`subsidy_advance`→Government allocation outbox→recovery（不產生第二筆 payout）。仍缺其餘跨 Domain 與 17 類 Global trace。 |

## 7. Anomalies

| Capability | 狀態 | 目前證據／缺口 |
|---|---|---|
| Registry／fingerprint／workflow reducer | `proven` | open／claimed／resolved／reopen reducer、MySQL projection、API query／claim／resolve 與薄 UI 已存在。 |
| Source outbox／scan → canonical projector | `proven` | production background worker 消費 Finance Import classification／dispatch outbox 與 BeClass review outbox，並以獨立 bounded cursors 掃描 Staff Payables、Scheduling 與 GOVSUB-001～005 根事實；單一來源失敗不阻塞其他來源，成功 reconciliation／recovery 以 inactive root fact 自動解除舊警示。 |
| Immutable finance occurrence recorder | `proven` | projector 以 source event identity／version 保存不可變 occurrence、current projection、checkpoint 與 receipt；重送不重複 occurrence。 |
| Human-assisted recovery actions | `proven` | Finance Import recovery action 能把 row identity 與必要輸入導向 correction Preview；Case Import、Government Subsidy、Staff Payables 與 Scheduling 也會依 root snapshot／source identity 導向既有 owning Domain Query／Preview UI。缺少必要 identity 時 fail closed，不猜測對象；根事實重掃與 failed projector retry 亦有 UI。 |
| Claim／resolve 不修改根事實 | `proven` | workflow 只更新處理進度與事件，不繞過 Domain blocker。 |
| Scan／Retry Projector | `proven` | canonical typed API 提供 bounded root-fact scan 與 failed-only outbox retry；Finance Import manual-review、Staff Payables、Scheduling 與 GOVSUB-001～005 sources 已接 MySQL port及 worker，unsupported definition／missing schema fail closed，具 replay／lock／active-to-inactive／薄 UI 測試。 |
| BeClass invalid-row review／repair | `proven` | 客戶與月嫂匯入的缺欄、格式錯誤與身分衝突會先保存 privacy-safe root fact，不再把不完整資料寫入正式表；已有 Query／Preview／Apply、typed API／薄 UI、outbox→`IMPORT-001/003` canonical projection、replay／stale／rollback 與聚焦測試。隔離 MySQL 8.4 另證明 invalid row→異常開啟→正式補正→異常自動解除的完整持久化鏈。 |
| BeClass isolated MySQL E2E | `proven` | `lu_test_runner` 只具 `lu_test_%` schema 權限，安全 fixture 已驗 grant；production schema 依 Finance Import→Anomalies→BeClass FK 順序安裝，真實 MySQL E2E 通過且 disposable database 已清理。 |

## 8. Global／Migration／Performance／Release

| Capability | 狀態 | 目前證據／缺口 |
|---|---|---|
| Shared contracts | `proven` | MoneyNTD、BusinessClock、typed errors、fingerprint、idempotency、UoW、outbox、bounded payload／cache／job policy Module 已存在。 |
| Global business suite | `partial` | G04、G06、G07、G08、G09、G10、G11、G12、G16、G17 已以 `global_e2e_manifest.json` 對應隔離 MySQL test：G04 證明完整服務後取消被拒且不改寫月嫂薪資；G06 證明退款與沖正不會改寫 Orders service-data lock；G07 證明 API 回應遺失後以同一 idempotency key 重送只會建立、執行一次 Finance Import command；G08 證明 manual correction 的 classification、ledger、reconciliation、alert、outbox、version 或 receipt 任一 persistence seam 失敗都不留 partial commit；G09 證明 Finance Import panel、UI client／HTTP router 與 direct typed application 取得同一 Preview，不存在 UI business fallback；G10 證明 anomaly projector failure/recovery 不會更動已提交退款事實；G11 證明 ordinary review replay 只進入一次 Finance anomaly、未產生 `IMPORT-006`；G12 證明 correction/post injected failure 全交易回滾與同 key 精確 retry；G16 證明 worker crash 後 lease recovery、重送與通知遺失不會重複 Finance Import Apply；G17 證明 UI response timeout 後保持 pending、重送同 key、查詢 queued 並只在 durable worker 成功後顯示成功。G01、G02、G03、G13、G14 仍為 partial；G05、G15 尚無可追溯 Global E2E。各 Domain 的隔離 MySQL 不能替代其餘 Global E2E。17 項逐項缺口、依賴與隔離規則已固定於 `28_Global_E2E_Acceptance_Gap_Package.md`。 |
| Writer inventory | `partial` | 2026-08-04 fresh v3 candidate scan 已產生 697 個 findings、9 個 unresolved，固定 `path + symbol + operation + table + fingerprint`，scan fingerprint 為 `a090829f30ef6fcbfe066a8d3a25e32379cb4458e9ad82025e704a9300ad8e54`；candidate evidence SHA-256 為 `e95e73ecf164c74f29f0f5d301f8a7c7a5694558dea2d335fdd22768dcb4a3e4`。deterministic validator 已通過。manifest 明定 `client_payments.subsidy_refund_*` 是 legacy projection、runtime caller=0、非 Client Finance SSOT。所有 finding 仍為 `effective_disposition=blocked`、`approved_to_remove=false`，尚未構成移除裁決。v1／早期 formal baseline count 僅是歷史追溯，不得再作驗收依據；其 owner coverage 無法涵蓋 current `background_job_repository.py`。下一步須完成逐項 owner／transaction boundary／high-risk disposition，並以 deterministic validator 固定 evidence。詳見 `evidence/writer_inventory_v3/`。 |
| Preserve-data runner contracts | `partial` | manifest、分類、journal、fingerprint、crash recovery 與 restart/read-smoke modules 已存在；public runner CLI 的 direct import path 已修復，`--help` 與無連線 invalid-input subprocess 驗證通過，且 27 項 migration-focused 測試通過。CLI 已提供 `complete-restart` 並以 bounded candidate runtime/read-smoke port 執行，但尚未綁定 read-only source principal、maintenance token 或 interrupted-switch recovery；未發現 dedicated runner test suite。詳見 `29_Preserve_Data_Runner_Completion_Decision_Package.md`。 |
| 全隔離 preserve-data hard rehearsal | `external-gate` | 尚未用專用、可丟棄 source DB 完成 source→backup→candidate→migration→switch→restart/read-smoke；禁止改用目前 `mysql_db`／`union_db` 補證據。 |
| Query cache／telemetry／durable job adapters | `partial` | API 已回 `Server-Timing`／response-time 並對 mutation 設 `no-store`；Holiday read projection 使用 defensive-copy TTL cache、成功後 invalidation、hit／miss／load telemetry 與 concurrent same-key single-flight。`finance_import_batch_apply` 與 `finance_import_correction_apply` 已使用 versioned envelope、MySQL lease/retry queue、獨立 `scripts/run_durable_job_worker.py`、typed job status；2026-08-04 新隔離 E2E 證明 correction command 由新連線 worker 一次性寫入 ledger／receipt。其餘 Assignment Plan、Payroll Rebuild、Staff Payout、Government Subsidy 仍由 FastAPI `BackgroundTasks` 在 API process 內執行，不得拿它們作 durable worker evidence。worker supervision、UI bounded polling、取消 API 與 preserve-data migration runner 仍未完成。詳見 `26_Durable_Job_Completion_Decision_Package.md`。 |
| Bounded Query／conditional GET | `partial` | Orders 主頁已使用正式 summary endpoint，具上限 200、穩定 keyset cursor、50 筆 UI 分頁、ETag／weak ETag／304、`private, no-cache` 與 15 秒 UI short cache；隔離 MySQL 證明 `page_size+1`、有效 assignment 聚合、排除 cancelled、不得讀 `orders.staff_id` 及 PRIMARY range plan。Scheduling 三個訂單清單 caller 已遷移至 summary endpoint，服務人員月曆再以單案 typed Query 讀固定排休條款。Staff、Form Management 等 legacy list endpoint，以及 Scheduling 超過 200 筆的 UI 分頁仍待逐頁完成。 |
| UI skeleton／supersession／single-flight | `partial` | Orders／Scheduling 高成本工作區已 lazy-load；Scheduling 四個工作區與 LINE 八個工作區都只渲染目前選取項。Orders 與 Calendar 的 staff／orders／holiday 參考 Query 使用 15 秒短 TTL cache，案件人力配置重用 Scheduling shell 的同一份資料，避免每次 widget rerun 重取；focused cache／shell 回歸 23 項通過。spinner、明確空狀態與部分 stable idempotency 已有。實際瀏覽器巡檢已覆蓋六個主頁、bootstrap Preview、核銷、Payroll、付款、2026-08 應付帳款與 XLSX 歸檔、補助、Finance Import 指定政府補助批次修正、BeClass 修正、檔期與七日緩衝、配對、出勤精算及請假 typed blocker；Orders warm switch 約 0.55～0.58 秒，Scheduling projection／matching 約 0.57 秒。集中 skeleton、request supersession、pending-state 與 job polling 尚未完整接入。 |
| HTTP/2／3、compression、deployment latency | `external-gate` | 本機 API 已啟用 `minimum_size=1024`、level 5 的 archive-aware gzip；大 JSON、typed errors 與 streaming bytes 均通過，XLSX 明確排除重壓縮，無 `Accept-Encoding` 時完全 bypass。HTTP/2／3、TLS reverse proxy 與實際部署 latency 仍需部署環境證據。 |
| ADAD runtime removal | `proven` | root rules 明定停用；active skill 不存在、Git hooks 只有 sample、歷史資料僅忽略保留，沒有 active gate。 |
| README／release metadata | `proven` | README 與 `pyproject.toml` 均為 `0.2.2`；`start.bat` 維持 non-destructive redirect。先前矩陣所記 `pyproject.toml` `0.2.1` 已是 stale evidence。 |

## 9. 下一個關閉順序

1. 當前基線：writer inventory v3 candidate 已 fresh scan／blocked reconciliation、
   source digest 與 evidence artifacts；不得把 candidate finding 升為
   `approved_to_remove` 或據此直接移除 production code。
2. 未來另立並人工核准 removal Work Package 後，才可逐類改接已確認
   `retire_candidate`，最終證明 `retire_candidate = 0`、`approved_to_remove = 0`，
   且所有 canonical／migrated writer 具有唯一 owner。不得要求所有 SQL finding 歸零。
3. 未來另立並人工核准相應 Work Package 後，才可補齊 Global 業務場景；
   preserve-data hard rehearsal、部署效能 evidence 及其 pytest／DB／network side
   effects 必須依各自 scope 另行核准，不會由前一步自動啟用。

## 10. 待裁決架構問題

### GOV-OWN-001：Government Subsidy 正式 owner（已裁決）

2026-08-02 人工確認新增獨立 `Government Subsidy Domain`。它擁有申請批次、政府
入款／退匯 ledger、allocation、reversal 與 typed Preview／Apply；Finance Import 只擁有
銀行根事實與 classification，再以 borrowed outer UoW 委派本 Domain。

新 owner production migration、typed API／UI 與隔離 MySQL 驗證均已完成；仍須遵守：

- 不得 fallback 到 legacy writer；
- Finance Import 無唯一批次或 allocation 時必須 fail closed 並轉人工 Preview；
- 政府補助與 Client Finance／Staff Payables ledger 不得互相抵銷。
