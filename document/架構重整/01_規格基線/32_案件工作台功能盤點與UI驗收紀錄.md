# 案件工作台功能盤點與 UI 驗收紀錄

本文件是使用者於 2026-09-10 要求保存的工作紀錄，供 UI 修改、功能修復及人工驗收續作使用；不是新的業務規格，也不以現有實作反向改寫正式 owner 語意。四階段完成後保留為本次調整紀錄。

## 最新功能階段狀態（2026-09-10；覆蓋下方歷次執行狀態）

### 其他功能逐項 Browser 操作（2026-09-10 19:37）

**尚非全功能驗收通過。** 本次繼續實際按鈕操作，不把入口開啟、查詢成功或 client mock 視為真實業務 Apply 完成。未修改既有案件、付款、匯入資料、帳號或儲存資料；未寄送 LINE。

| 操作項目 | 結果 | 驗證邊界 |
|---|---|---|
| 客戶銀行核銷更正 | PASS（Browser＋client mock） | 空案件禁止查詢；選款項、原因、預覽、確認受理、查詢結果至核銷收據；受理後輸入及重複確認鎖定 |
| 月嫂銀行核銷更正 | PASS（Browser＋client mock） | 切換月嫂付款、選月嫂、查詢、選未付款、預覽、確認、讀取完成收據；零餘額款項未出現 |
| 服務完工 | 發現問題後修復，PASS（Browser＋client mock） | 原成功後舊 Apply 仍存在，父頁回讀尚未成功即顯示完成。改為收據到達後清空預覽、禁止舊 Apply，等待父頁回呼成功才顯示完成；正常與故意回讀失敗兩條路皆實際重按。失敗時顯示「案件狀態尚未回讀確認」且無舊確認按鈕 |
| 取消案件 | PASS（真實查詢／Preview）；Apply NOT_RUN | OPS96-SCHED-C-RETEST2 讀取與取消影響成功，0 日／0 時、洽談中→訂單取消；未填原因／未勾核對不能確認。帳務影響仍有 deposit／no_finance_change 英文，保留待改善 |
| 實際開始日 | 業務 Preview BLOCKED；原因提示修復後 Browser PASS | API 409 `scheduling_assignments_required`。原籠統訊息改成尚未建立正式指派、先完成服務安排及本次未改日期；同案件重新查詢與預覽確認中文提示，未放寬後端規則 |
| 服務前換人 R-01 | BLOCKED（此測試案件） | 選情境後 API 503；typed blocker 為 `replacement_willingness_set_incomplete`，loader 要求候選人的完整意願事件。未造資料、未取消檢查，UI 仍為籠統提示，不能宣稱换人流程通過 |
| 四類工作簿匯入 | PASS（入口及空檔防呆）；檔案／Apply NOT_RUN | HCM、客戶、月嫂、歷史訂單逐一切換；正確標題、選檔入口、未選檔預覽 disabled |
| 六類資料瀏覽 | PASS（真實唯讀） | 訂單、客戶、月嫂、BeClass、HCM、銀行資料 API 均 200；搜尋指定測試案只得該案，詳情抽屜開關、清除搜尋、下一頁 25→50 筆實測 |
| 帳務中心查詢 | PASS（真實唯讀） | 客戶收款、月嫂應付款、應付帳款、契約收款帳戶 API 200；銀行匯入未選檔時上傳與預覽 disabled。未改帳戶、未付款 |
| 跨訂單補助／結案 | PASS（真實唯讀） | 補助全部 90 筆；已入款 28 篩選成功、草稿 0 顯示無符合結果；結案彙總開啟與 API 200 |
| 歷史服務天數 | PASS（查詢與已完工案 Preview）；Apply NOT_RUN | 服務中合成案 CX0905123327A 預覽 422 `historical_order_lifecycle_transition_invalid`，錯誤提示仍過於籠統；改查已服務完成案 CX0905123631A，填 2 天後預覽成功，客戶應收及月嫂應付各 4,800 元，未確認建立帳務 |
| 異常審核 | PASS（唯讀重查） | 重查及匯入待檢查 API 200；LINE 異常處理按使用者指示不驗證後端 |
| 帳號權限／儲存管理 | BLOCKED（模式登入權限） | 兩者 API 403，分別明示唯一 root／root 才能使用；未冒用高權限，未建立帳號或刪除資料 |
| 季度／年度補助報表 | PASS（真實查詢與匯出按鈕） | 切換兩種範圍，各查詢及按匯出，畫面均顯示 XLSX 已產生並開始下載。季度查詢及 export 200、年度查詢 200；本次未開啟下載的 XLSX 核對所有儲存格 |

本次 client mock 以正式 React 元件搭配暫存 `remaining.html` 操作頁，指定 client 使用記憶體資料，其他 fetch 一律拒絕；不是完整 API／DB 端到端證據。完工回读故障由 `fail-readback=1` 注入。

追加月嫂操作（隔離 `staff.html`，正式 StaffPage＋記憶體 client mock）：

- 六項偏好：北區改東區、填說明、預覽，原因空白時確認 disabled；填原因後確認儲存，顯示東區及「已儲存並重新查詢六大接案能力」，mock Query→Preview→Apply→Query 順序可見。PASS；不是後端寫入證據。
- 暫停接案：新增原因、预覽新增、套用新增，清單顯示 2026-10-01 起無期限暫停，顯示最新期間已觀察。PASS。
- 取消期間：填原因、預覽取消、套用取消，回讀已取消；重複預覽取消 disabled 並有業務原因。PASS。
- 長假：切換長假、預覽、套用，回讀 2026-10-01～2026-12-31 生效中。PASS。
- 結束暫停：BLOCKED（Browser 日期輸入能力），不是已證實的程式缺陷。輸入日期在 DOM 可見，但 React 仍視為空白；另建無業務 `date-probe.html`、只有受控日期欄與事件顯示，實測同樣只有 input 事件、onChange 未更新日期。沒有為了通過而修改正式頁面事件處理或偽造狀態。
- Mock 頁未提供完整資格／摘要 client，對應區塊會顯示 mock 安全閘錯誤，不將此判為正式資料查詢失敗；本次只驗收上述人工偏好與期間流程。

追加其餘流程（皆正式元件＋client mock，不接既有 DB）：

| 流程 | Browser 結果 |
|---|---|
| 週報手動數值 | PASS；填指定週推廣 3、詢問 7，儲存後重新載入仍為 3／7；另一週維持 12／8 |
| 未服務取消 | PASS；讀取、預覽、填原因、勾確認、Apply、Query 回讀取消成功；已取消時預覽 disabled，舊確認消失 |
| 候選新增 | PASS；只有完整承接人選顯示可選，未選時加入 disabled；選取、加入、按 receipt 對應 ID 回讀同一月嫂，不觸發資訊寄送 |
| 多月嫂完整方案 | PASS；兩段組合查詢、建立與正式方案回讀成功，舊建立按鈕 disabled；切 3 段且無完整組合時只有空結果說明，無建立按鈕 |
| 正式推薦 | PASS；只願意且 active 的人選可建立；正式方案回讀、補登分段意願、履歷 mock 入列、客戶接受、等待訂金鎖 Preview／Apply／回讀皆完成。履歷不冒充實際投遞，鎖定後不可另建方案 |
| 客戶拒絕推薦 | PASS；另一次初始情境操作至拒絕，顯示拒絕狀態，不提供等待訂金鎖入口 |
| 正式開工成功路徑 | PASS；mock 已有正式服務資料，沿用查詢預填開始日，Preview、填原因、Apply、同案件日期及版本回讀成功；未測手動修改空日期欄（上述工具限制） |
| 退役／復職 | PASS；mock 填生效時間與原因，分別 Preview→Apply→Query，先回讀已退役，再改辦復職回讀在職；未觸發真實 LINE role revocation |
| 人工日期表與正式排班 | PASS；人工快照原因與勾確認、客戶／月嫂個別確認、排班原因与勾確認、Apply／工作查詢／正式指派／父頁回讀。僅單方確認時無排班入口 |

排班故障分支實測再發現問題並修復：父頁回讀失敗時原先仍顯示完成且可再次 Apply。`MatchingScheduleAndAssignmentActions` 改為收到 job 後清空舊 preview、鎖定新 Preview／Apply，父頁回讀成功後才標完成；工作已成功但尚未回讀時仍可查原結果，查詢失敗有受控錯誤。Browser 重測先顯示回讀失敗及「重新查詢正式排班結果」，無完成提示與舊確認；重查成功後父頁顯示正式指派，紀錄僅一次 Apply、兩次原工作查詢。新增既有 owner 回歸覆蓋同一路徑。

修復集中於完工／正式排班成功與失敗呈現及實際開始日阻擋原因，不更動 owner、日期計算或帳務規則。完工、日期、排班 focused tests 共 12 項 PASS；最終 build、architecture closure、diff check PASS（build 僅保留 bundle 大小警告）。尚待逐項操作：最終契約簽回與封存、工作簿檔案匯入；日期輸入、root 權限與真實 LINE 限制另列。上述一律維持 Browser NOT_RUN，不以既有單元測試或本次入口檢查升格。

### 授權後 migration 與 UI 重測（2026-09-10 18:26；覆蓋下方缺表阻塞）

**本機缺表與空媒合方案回讀問題已修復，相關實際 UI 重測 PASS。** 這不把尚未實際點擊的其他業務 Apply 升格為通過，整體功能驗收邊界仍依逐項紀錄。

- 使用者先授權本機補表，再明確授權隔離驗證 DB。建立 `lu_test_ui_weekly_fresh_20260910`（完整 canonical fresh schema）與 `lu_test_ui_weekly_candidate_20260910`（原資料庫備份還原及 1035 升級）；未重設或替換既有 DB，沒有環境切換或業務 seed。兩個驗證 DB 保留供追溯。
- 真實引擎驗證由既有 migration／collector／qualification builder 執行；fresh 物件清單檢查 PASS，candidate 全部來源資料保留及 descriptor exact PASS。合格收據：`validation/receipts/PROV-UI-WEEKLY-20260910-local-additive-qualification-1035.json`。
- 本機更新器只執行一個 `CREATE TABLE weekly_report_metrics`。初次 postcheck 因「不存在」與「新建但空表」指紋不同而停止；唯讀逐表核對確認筆數差異為空、指紋差異僅新表。修正 `_local_verify_backup_rows`：只允許明確 absent baseline 且前後皆零筆的新表正規化；既有表及任何筆數變動仍拒絕，未改寫原始備份或測試 golden。
- 維護鎖內重新驗證原始 dump、backup identity、完整既有資料指紋、descriptor 及已執行 statement journal，完成收據對帳；未重跑 DDL、未另建備份冒充原 baseline。更新器 `--require-current` 確認 current。
- 備份、原始計畫、candidate 操作及本機 journal／完成收據存於 `scratch/ui_weekly_migration_20260910/`。dump 限本機保存且檔案權限 0600，目錄 0700、Git ignored；發布收據不包含 raw 業務資料。
- API 以原 local_bypass／lifespan off 設定重啟。實際 Browser：週報重試與重新載入 200；案件受理、補助案件、服務中三分頁切換成功；按「下載營運報表 XLSX」後 export 200，畫面顯示已產生並開始下載。未按「儲存此週」。
- 實際 Browser：原 `OPS96-SCHED-C-RETEST2` 的「讀取正式指派與排班」不再因 active plan 404 整塊失敗；顯示無有效方案與「尚未建立、5 天 × 8 小時」正式摘要。非 404 錯誤仍保留失敗路徑，不偽造可排班方案。

| DB gate | 結果 | 證據 |
|---|---|---|
| Scope／change inventory | PASS | 本次兩次人工授權；僅 schema-only 1035，無既有資料刪改 |
| Static release／descriptor | PASS | canonical assembly、manifest hash／descriptor；唯讀計畫僅 1035 absent |
| Read-only plan | PASS | `scratch/ui_weekly_migration_20260910/local-plan.json`；qualification exact |
| Engine verification | PASS | 上述已發布 qualification；fresh 完整物件及 preserve-data candidate 複驗 |
| Developer acceptance | PASS | 原備份＋本機 completed journal；current 檢查、API 重啟與週報 UI 查詢／下載 |

驗證：migration builder／collector／entrypoint 46 項 PASS；空表保留及既有 additive runner 59 項 PASS；週報 backend 13 項 PASS；指派／條款 frontend 7 項 PASS、報表 frontend 9 項 PASS；build、diff check、architecture closure PASS。未 commit／push／部署或真實 LINE。

### 實際瀏覽器按鈕補驗（2026-09-10 17:36；最新裁定）

#### 使用者授權補表後的安全預檢（2026-09-10）

使用者已授權對本機 `lu_test_nonfinance_20260906` 套用既有保留資料 migration；範圍仍不包含新建資料庫、reset、seed、正式環境或繞過安全門。執行 `APP_ENV=development .venv/bin/python -m scripts.update_local_database` 的唯讀計畫後，結果 **DB_CHANGE_NOT_READY**。尚未執行 DDL、備份建立、Apply 或資料寫入。

| DB gate | 狀態 | 證據 |
|---|---|---|
| Scope | PASS | 使用者本次補表授權，目標限定本機既有測試 DB |
| Change inventory | PASS | `1035_weekly_report_metrics.sql` 僅 CREATE TABLE，無 backfill／seed／刪改既有資料 |
| Static release | PASS | 更新器成功解析 latest `labor-union-weekly-report-metrics-2026-09-09-v1` 與 SQL／descriptor；fingerprint `8cf69bb5ed5bd2733bee48f049fb1c90aeb393a93d9b5a77c42cc78f51f15f0f` |
| Descriptor／state | PASS | 唯讀計畫中前置 artifacts 均 exact，1035 為 absent，無其他待套項目 |
| Read-only plan | BLOCKED | 更新器 exit 2，`qualification_missing`；沒有符合本版的有效 qualification receipt |
| Engine verification | BLOCKED | `validation/receipts` 搜尋未找到本版或 1035 收據；不能以 mock 取代 fresh-bootstrap 與 preserve-data 真實引擎證據 |
| Developer acceptance | NOT_RUN | 未進 Apply；不直接執行 SQL 旁路 runner，不偽造 qualification |

需先取得建立隔離驗證用暫存資料庫的範圍授權，才能依既有規範補做真實引擎驗證與合格收據，之後才可備份並套用目標本機 DB。既有缺表及 UI 未通過項目仍保留，不宣稱完成。

**整體尚未通過；不得以以下舊有 mock 回歸 PASS 宣稱全部 UI 功能已驗收。** 使用者要求親自操作 UI 後，新增下列直接點擊證據。僅使用本機模式登入與隔離 mock，未送出正式業務 Apply。

| 實際操作 | 結果 | 證據與限制 |
|---|---|---|
| 進件條款：服務天數 5 改 6，檢查變更 | BLOCKED，錯誤說明已修復並 UI 重測 PASS | `scheduling_segments_required`；既有 domain 拒絕無正式區段案件改開始日／天數。原英文拒絕改為中文原因及未儲存說明，未放寬規則。追加既有回歸測試後 4 項 PASS |
| 精算日期、取消一天、補選另一天 | PASS（互動與查詢） | 4/5 天時確認 disabled，5/5 天時啟用；未按正式確認 |
| 正式指派與排班回讀 | 未通過易用性檢查 | assignment-plan 200，但 active matching plan 404，畫面只顯示「找不到要求的資源」；空方案與正式排班資訊的顯示仍待改善 |
| 洽談中案件完工確認入口 | PASS（防呆） | 說明須先實際開工且進入服務中，未開放完成預覽 |
| 契約欄位預覽／讀取本案資料 | PASS（查詢） | 主要欄位顯示且缺少簽約條件明確提示；非完整契約完成驗收 |
| 資訊－1／－2 預覽、取消、確認寄送 | PASS（瀏覽器＋client mock） | 載入正式 React 元件；資訊二取消不入列，分別確認後僅相應資訊成為等待發送。mock 不連 BeClass、不寄 LINE；不宣稱實際投遞 |
| 人工記錄願意 | PASS（瀏覽器＋client mock） | 寫入記憶體 mock 後回讀「願意承接」，重複願意按鈕 disabled |
| 客戶／月嫂契約下載按鈕 | PASS（瀏覽器＋client mock） | 各按鈕取得先前以真實模板生成的合成 PDF：484379／428711 bytes，畫面各顯示正確檔名下載完成。不是本次 UI 觸發後端重新產生或正式封存驗證 |
| 月嫂搜尋、詳情、偏好與接案狀態分頁 | PASS（入口／唯讀） | 搜尋後摘要卡開啟該人詳情；偏好編輯與預覽成功，未填原因時確認儲存 disabled；不可服務期間查詢 API 200。未送出偏好、長假或人事 Apply |
| 營運週報載入、重試、服務中分頁 | FAILED／環境修復 BLOCKED | GET `/api/v1/operations-reports/weekly` 反覆 500。強制 READ ONLY 診斷確認目前本機 `lu_test_nonfinance_20260906` 缺 `weekly_report_metrics`。已有 `db/schema_parts/1035_weekly_report_metrics.sql` 保留資料新增表 migration，但未獲本次 schema 變更授權，未執行；不可用空資料 fallback 隱藏錯誤 |

- 本次只修正條款拒絕訊息及追加對應測試，build PASS；未修改後端業務規則。
- 隔離 mock 操作頁位於暫存目錄，使用正式前端元件並替換指定 client；禁止其他業務 API，不接資料庫。只屬元件瀏覽器驗證，不冒充整套 API 端到端。
- 其餘尚未逐一點擊的業務 Apply、最終簽回、帳務核銷等為 NOT_RUN（實際瀏覽器）；先前單元／整合 mock 測試證據不升格為瀏覽器證據。
- 下一步：取得本機既有 migration 的明確授權後依 preserve-data 執行門評估與補齊，重測週報；繼續完成剩餘操作驗證及空方案回讀呈現修復。禁止新建／重設／seed DB、正式資料或 LINE 副作用。

### 前次 mock／PDF 階段結論（保留歷史，以下非最新整體裁定）

使用者最新明確限定：使用 mock data，只驗證欄位顯示及轉換 PDF，不要求真實資料寫入整合。**本次 mock 顯示與兩種 PDF 轉檔驗證 PASS，可交使用者驗收**；真實案件缺地址／收款設定不是本次 blocker，不再要求補資料或指定銀行流水。沿用已完成的程式修復、前後端隔離測試與逐頁 PDF 檢查證據，不以另跑真實交易作為完成門檻。原附件外部連結快取已找回完整 19 組項目與內容；現制由客戶先墊款，月嫂只有整筆應付，已移除舊制補助／自費拆分。

### 最後全範圍補驗（2026-09-10 17:22）

使用者再次要求確認整個功能修復範圍，而非僅 PDF。已完成本次盘點與已修改頁面的 mock 功能回歸；**自行驗證 PASS，使用者功能驗收仍待進行**。此處不宣稱全部 repository 或真實端到端交易都通過。

- 初次補驗有 53 項前端失敗；逐項區分已移除 UI 的過期定位與實際遺漏，不恢復小狀態篩選、舊抽屜、技術來源或匯入頁異常清單。
- 實際修復：結案／取消案件補回結算摘要且不展示十三階段；歷史資料保留月嫂姓名與不可用提示，與正式派案隔離；案件刷新失敗時保留表單並禁止舊資料操作，提供重新整理；正式階段前進不自動切走正在操作的工作群組；移除帳務尚未接線的過期說明。
- 後端補驗 **426 項 PASS**：營運階段、媒合協調、契約完整 integration roots。與先前 144 項部分重疊，不加總；此前候選專用寄送與查詢測試仍有效。
- 前端合併補驗 **52 個檔案、299 項 PASS**：進件／條款、候選查詢與意願、推薦、多月嫂、契約、日期／排班／完工、異動、歷史重啟、回讀保護、帳務更正、月嫂抽屜、週報／補助、資料匯入／瀏覽、帳號、異常及 LINE 頁面。全為 mock transport，未送出真實業務寫入。
- 追加既有條款測試中的「回讀後階段前進仍保留表單」斷言，3 項 PASS；與上述 299 重疊。
- 最後 build、git diff --check、architecture closure PASS。瀏覽器實際核對案件工作區及帳務導引畫面；先前兩種 PDF 逐頁證據沿用。既有 build 大型 chunk 警告未作無關優化；部分測試仍有 act 警告，未以此冒充功能失敗或抹去。
- 沒有新增 DB、seed、migration、正式資料寫入、真實 LINE、commit、push 或部署。使用者先前通過的是 UI；本輪完成的是自行功能驗證，不能代替使用者最終功能驗收。

### 本次已實作

- 初步候選查詢與正式分段查詢分流。原截圖案件 `115990909` 實際點擊查詢已成功回傳人選；畫面區分檢查預計期間的日曆日與正式服務天數。
- 候選資訊－1／－2 各自查閱完整命名投影，無正式 assignment 也能預覽。每日服務時段、服務方式、寶寶資訊接回既有來源；未知報酬及付款日期標示待確認。
- 候選專屬預覽綁定案件、人選、資訊種類、收件身分及全文；確認寄送 fresh-read 比對後，把同一全文交給既有 durable LINE queue。保留投遞狀態，不把 queued 當送達；本次沒有建立真實寄送工作或發送 LINE。
- 客戶未簽 PDF 準備入口接回既有 accepted-plan、XLSX 套值與受控 PDF 引擎。不要求先有 commitment，不建立正式排班。重播比對穩定欄位摘要，不依賴 XLSX ZIP 時間戳；準備後下載使用回傳的精確文件版本，不因舊畫面尚無文件而跳過。
- 契約實際入帳日期讀取既有 Client Finance 有效入帳及 allocation，未入帳留白；不再拿 due date 代填。未取得條件欄位時清除舊公式，避免未定金額被算成 0。
- PDF 轉換前檢查外部工作簿公式；缺舊來源時明確阻擋，不啟動 Office 引擎、不把缺漏契約顯示為可簽署。主要欄位預覽的 ready-to-print 同步此限制。
- 客戶契約 19 組靜態服務內容由原附件目錄快取精確還原；B39 使用日數投影，月嫂 D13 清除無標題的旧雇主自費值。新封存 XLSX 不攜帶原附件的其他歷史訂單快取。服務人員契約只列整筆應付，詢問資訊不再顯示兩種資金來源拆分。
- 本機 PDF 引擎使用隔離字型設定，修正中文缺字；未安裝字型或更動系統設定。客戶契約長段落加高列高，實際轉檔逐頁檢查。
- 銀行流水的人工核對列接回既有 Finance Import correction：選案件／月嫂及款項 → 預覽逐筆核銷金額 → 明確確認 → 查閱 durable job 收據。只能處理既有列提供的可更正操作，不新增任意標記已付款。
- 已驗收的月嫂抽屜入口保留；接案偏好、長假、暫停、結束暫停的回歸測試改由此入口操作，並驗證 stale、錯誤、未知結果及回讀。未恢復使用者已要求移除的外層分頁。

### 驗證證據與限制

| 驗證 | 結果 | 邊界 |
|---|---|---|
| 後端 focused tests | PASS，144 項 | 11 個檔案：資訊、候選查詢／sender、階段投影、契約 API／欄位／日期／準備／renderer；隔離 fake adapters，未建 DB；包含新制單筆應付回歸 |
| 前端最新 focused tests | PASS，72 項 | 11 個檔案：獨立資訊預覽／確認寄送、契約準備與下載、帳務更正、月嫂詳情／偏好／不可服務期間等；含 mock transport，不等同真實核銷；與先前 73 項有重疊，不加總 |
| 前端 build | PASS | 仍有既有大型 chunk 警告；帳務元件測試有 React act 警告，不隱瞞 |
| 本機模式登入、原失敗案件候選查詢 | PASS | 修正驗收程序仍執行舊程式導致新路由 404；重新啟動本機 backend/frontend，未改正式認證設定 |
| 既有兩位候選案件資訊－1／－2 預覽 | PASS | `OPS96-SCHED-C-RETEST2` 實際頁面分別載入；未按確認寄送 |
| 本機合成案件契約投影 | PASS（查詢），資料門檻 BLOCKED | 強制 READ ONLY transaction 查詢既有 `lu_test_nonfinance_20260906`；至少一個 accepted-plan 案可投影預計日期／Client Finance 金額，但缺服務地址及目前收款帳戶設定；未補造或寫入 |
| 兩份真實模板 PDF | PASS（合成欄位轉檔及逐頁檢查） | 客戶 3 頁、月嫂 2 頁，中文可讀、外部公式清除；客戶 19 組內容與原快取精確比對通過。非真實案件完成簽署或封存下載驗收 |
| 架構歸屬與 diff 空白檢查 | PASS | closure 以本次起始 HEAD 為比較基準，未提交既有成果均保留 |
| 真實 LINE、正式資料寫入、部署 | NOT_RUN | 依使用者邊界，不建立／重設／seed 資料庫；沒有 commit、push |

### 本次排除範圍（不是阻塞或下一批修復）

1. 先前缺「目錄」及報酬分拆兩項均已解決，不能再要求使用者提供旧工作簿或拆分規則。
2. 本機已查閱契約案例缺收款目的帳戶設定／部分地址；不能為讓 PDF 通過而填入虛構帳戶。真實案件 PDF 準備、封存與下載須在資料齊全且獲授權的目標完成。
3. 實際帳務核銷、契約最終簽回會寫入資料；本輪僅執行隔離測試與唯讀查閱，未自行變更既有測試 DB，也未啟用背景工作。LINE 真實投遞依指示排除。

上述真實寫入鏈不在本次驗收範圍，不要求使用者提供資料或另行授權。本次未建立或修改資料庫、未發送 LINE。下方歷次盤點保留為歷史證據，與本節衝突時以本節及最新人工裁決為準。

## 人工指示與執行邊界

- 順序：完成盤點 → 修改 UI 並交使用者驗收 → 修復功能並自行驗證 → 交使用者功能驗收。各階段為停止點，不以零碎發現結束任務。
- 重用既有後端、API、元件、模板；確認真的缺失才新增，不重複實作。
- 資訊－1 與資訊－2 可分開詢問；没有 BeClass 不得一律阻止初步接案意願詢問。
- 案件處理與訂單／服務資料分開。保留分步導引，但不能只准操作當前投影階段。
- 候選池簡化為一份人選清單，不把系統內部狀態當作五種日常操作分類。技術資料來源不呈現給工會人員。
- 月曆多人甘特圖設計維持；其他既有未提交成果保留。
- 不建立測試資料庫、不重置或重新 seed。LINE 真實後端投遞驗證依使用者指示跳過。模式登入供畫面驗收。
- 本輪無 commit、push、production、migration、部署或真實訊息發送授權。

## 盤點基準

- Branch：`codex/ui-ux-improvements`；HEAD：`aa583769476b61b15ebe0f7406d6a10c87b98db9`。包含既有 dirty 修改，HEAD 並非目前畫面的完整快照。
- 舊版參考：`1c1aa14c^:ui_react/src/pages/OrdersPage.tsx`。
- 原始附件：`document/管理端UI/表格需求模板/所需表格.xlsx`（資訊－1、資訊－2、客戶契約）及 `服務人員契約.xlsx`（工作表1）。附件為欄位與用途 evidence，舊條款及公式不能自行升格現行規則。
- 已完成 source/template/入口靜態盤點；沒有執行功能測試，不能以檔案或介面存在宣稱可用。

## 十三階段盤點與處理方向

| 階段 | 既有能力與缺口 | 處理方向 |
|---|---|---|
| 1 進件 | `OrderIntakeRepairPanel`、`OrderTermsMutationPanel` 已有；UI 僅當前階段可操作 | 分清待補資料與可辦事項 |
| 2 候選 | 查詢、加入人選已有；五種分類其實是 pending/building/blocked/ready/unavailable 流程投影 | 一份清單；載入、失敗就地呈現；不改後端 enum |
| 3 詢問 | `candidate_contact_pool_client.sendInformation` 及後端存在，畫面未接；傳送只有標題及日期 | 重用傳送機制、專用資訊投影，補內容；不可直接接上不完整訊息 |
| 4 意願 | 人工記錄與 LINE 回覆機制存在 | 同一清單顯示待回覆／願意／不願意，資訊1/2獨立紀錄 |
| 5 推薦 | `OrderFormalRecommendationPanel`：方案、履歷、客戶接受／拒絕、假日協調與等待訂金鎖 | 保留現有規則，整理順序及文字 |
| 6 契約準備 | 模板、套值預覽、PDF、送交外部平台紀錄存在；完整預覽未接 current UI | 重用，不重建引擎 |
| 7 簽署完成 | 完成回報、最終 PDF staging/preview/apply 存在 | 清楚分開送出、簽署、文件確認 |
| 8 訂金 | 正常收款由 Finance Import 銀行流水核銷 | 導向帳務，不新增任意標記已付 |
| 9 日期 | `OrderServiceDatesPanel` 已有精算及確認 | 保留，區分預計／正式日期 |
| 10 服務 | `OrderAssignmentPlanPanel`、正式排班及服務前換人存在；換人重複入口 | 統一異動入口，保留合法操作條件 |
| 11 完工 | `OrderServiceCompletionActions` preview/apply 已有 | 保留並呈現具體未完成條件 |
| 12 客戶結算 | 正常核銷鏈存在；人工核對清單只有顯示，更正 client 未接畫面 | 重用 `finance_import_correction_client`；不把歷史補登當正常核銷 |
| 13 月嫂結算 | 正常付款走銀行流水，歷史付款另有專用元件 | 分清正常／歷史；共用帳務入口，不重建 writer |

## 四份表格逐項差異

### 資訊－1

- `db/templates/tpl_info_01.json` 已有案件、月嫂、客戶、起訖、每日時數、天數、地址、單價、服務薪資、發薪日、樓層費、特殊休假及備註。
- 原表服務方式、寶寶資訊、補助／自費分列、兩個付款日與總計未完整對應。時段不能只用每日小時數替代。
- `subsystems/orders/order_information.py` 與 `infrastructure/mysql/order_information_repository.py` 查詢綁定 assignment；未指派就找不到目標。薪資依賴正式應付資料，不能直接拿來當候選詢問內容。
- `candidate_contact_pool_workflow.py::_send_information_in_transaction` 目前訊息只有資訊種類及服務日期；另一條 `matching_line_cards.py` 也只有少量基本資訊，均非原表完整內容。

### 資訊－2

- `tpl_info_02.json` 與 `domains/case_import/order_information.py` 已有十五項命名投影：飲食、中藥／蛋奶素、酒、油、過敏、特殊照護、餐點喜忌、工具、洗澡水、哺乳、三節計費、胎數、樓層、停車、其他寶寶。
- 加上姓名、電話、案號、地址及樓層費；原表食材清單沒有納入目前模板。
- 無 current UI caller；同樣受到 assignment 目標限制。資訊－2 未齊不得連帶鎖住資訊－1。

### 客戶契約

- `db/templates/contracts/contract_client_copy.xlsx`、同名 JSON、`full_contract_preview.py` 與 PDF renderer 已存在。
- 原表與模板的非空儲存格比對發現 40 處文字／公式差異，多數為舊公式大小寫差異；不代表 40 個功能缺陷。
- 原表部分入帳日期映射成 due date，需裁決應呈現應繳日還是實際入帳日，不能混用。
- 舊外部公式仍有未被 param mappings 取代者；尚未驗證產出 PDF 的完整性，不可宣稱已完成套值。

### 月嫂契約

- `db/templates/contracts/服務人員契約.xlsx` 與提供附件的儲存格內容比對一致（非視覺／列印驗證）。
- JSON 將補助、自費金額及付款日期四格設定為 not_applicable；renderer 會清空。這是原表與目前帳務模型的語意差異，不得自行分拆、補零或抹除需求。
- 契約模板、套值、下載、簽署紀錄應重用；既有 `ContractSigningActions` 亦有封存文件下載能力，不代表全部操作適合直接放回畫面。

## 必須留待功能修復的事項

1. 無 BeClass 的初步詢問：候選查詢 `requires_cooking is None` 拒絕；傳送前 `_require_full_coverage` 又沿用該檢查。不是 UI 文案能修好。
2. 候選／正式指派兩種使用時機與資訊預覽目標：需在現有 owner 契約下修正，不虛構 assignment。
3. 完整資訊預覽與傳送內容必須一致；不能以排入工作等同實際送達。
4. 契約金額分列、付款日期語意與未取代外部公式須逐項解決；業務衝突請人工裁決。
5. Finance Import correction 前端接線與正常／歷史入口區分，沿用原 Query/Preview/Apply。
6. 上述皆尚未 functional verification；資料及外部 effect 邊界不得因本文件擴張。

## UI 階段驗收範圍

- 案件處理獨立工作畫面，與資料及異動分開。
- 以可理解的工作群組承接十三階段；不新增後端狀態、不自動推進案件。
- 候選只一份清單；資訊1/2分開展示與預覽入口；未完成接線／內容的傳送不能假裝成功。
- 契約與文件入口、帳務導引可被找到；重複換人入口合併。
- 以實際瀏覽器畫面檢查桌面及窄畫面，僅操作查閱／導航，不執行業務 mutation。
- 編譯與靜態檢查可執行；功能測試、資料寫入、真實傳送留待 UI 人工驗收後。

## 階段狀態

| 階段 | 狀態 | 說明 |
|---|---|---|
| 盤點 | passed | 靜態盤點完成，不代表功能通過 |
| UI 修改 | passed | 六組工作、獨立資料及異動頁、資訊1/2預覽、契約欄位預覽與帳務導引已實作；不代表功能修復完成 |
| 桌面畫面檢查 | passed | 實際本機瀏覽器檢查看板、六組工作、兩份資訊、兩種契約欄位版面、資料與異動分頁；只查閱／導航 |
| 窄畫面檢查 | not_run | 已加入單欄樣式，尚未實際調整瀏覽器尺寸驗證，不宣稱手機版通過 |
| 編譯／靜態／架構歸屬檢查 | passed | npm run build、git diff --check、scoped architecture closure 通過；編譯仍有大型 chunk 警告 |
| 使用者 UI 驗收 | passed | 使用者已確認待辦看板及最後修訂的月嫂名冊 UI 通過 |
| 功能修復及自行驗證 | passed（本次 mock 範圍） | 最後補驗已完成，詳見文件頂部；不含真實投遞與帳務寫入 |
| 使用者功能驗收 | not_run | 自行驗證完成後交付 |

## UI 階段交付紀錄（2026-09-10）

### 功能續修：初步候選查詢（2026-09-10）

- 人工確認：初步詢問使用預計服務期間，未知報酬／付款日顯示待確認；契約收付日期取實際收付日，未收付留白，不能以應收／應付期限替代。此項裁決已完成，不應再次以未裁決為由停止。
- 初步查詢新增 purpose-specific 入口，重用既有檔期算法；正式分段查詢仍要求 official service dates。沒有 BeClass／下廚需求等資料時保留未知，不阻擋初步詢問；已填需求的啟用篩選仍有效。
- 候選池加入／寄送／人工確認所用 fresh availability 改為預計期間檢查，仍拒絕占用及不可服務日；不重套查詢偏好、不新增正式指派或排班。
- 獨立回歸驗證：後端 9 項 passed，涵蓋無正式日期、未知下廚、既有占用、已知下廚要求、候選池 fresh-check、API 身分與單人限制、自訂服務地區；前端 transport 2 項驗證詢問／正式路由分流與錯案回應拒絕。全部為隔離測試，不建立資料庫，不代表實機流程已通過。
- 本批前端 2 項 passed；`npm run build`、`git diff --check`、scoped architecture closure passed。編譯仍有既有大型 chunk 警告；本批瀏覽器實機查詢／寫入驗證 not_run。
- 未完成：資訊－1／－2 完整內容仍須接入 candidate-specific 投影與預覽／寄送一致性；現有 sender 只有標題與期間，UI 最終寄送保持停用。客戶 PDF 準備、實際收付日期套值、帳務 correction 仍待修復。不得將本批候選查詢修復報成整體功能階段完成。

### 功能階段首次執行（UI 全部獲本輪人工確認後）

- 修正 `segmented_availability_query.py`：下廚需求未知時，只有啟用下廚篩選才拒絕；停用時維持原始 NULL 與 false 的未匹配結果，不寫入預設值、不繞過休假／占用限制。這不是完整「無 BeClass 即可寄送」修復；聯絡池 fresh coverage 仍沿用預設篩選，且正式服務日期限制仍在。
- 新增 canonical matching-coordination unit test `unit/test_unknown_cooking_filter.py`，三項測試 passed：未知值停用篩選可查詢、啟用仍拒絕、休假不可被繞過。
- 契約 UI 針對既有 accepted-plan-required／session-facts-unavailable code 提供缺少方案／契約資料的說明，不再一律指示版本重查；原 unknown-outcome／stale 防護不變。
- 前端測試沿用已驗收新按鈕名稱與移除稽核區塊的規則；Staff 詳情保留 canonical relation detail，但不得出現在摘要卡片。契約 actions 18 項、Staff profile 1 項共 19 項 passed，涵蓋 mock PDF 下載對象、準備、簽回、衝突與未知結果回讀；不是實機產生／下載 PDF 驗證。
- 後端既有 `test_full_contract_preview.py` 與 `test_contract_external_signing_api.py` 合計 74 項 passed；採 fake repository／TestClient，不連真實 LINE 或建立 DB。三批合計 96 項 passed。
- 新確認的未解邊界：`_required_service_dates` 對沒有正式日期精算的案件一律拒絕，而候選池加入／傳送皆共用此查詢；既有 Orders 資訊投影要求真實 assignment，薪資讀正式應付，不能將它改名當成候選預估報酬。需要確認初步詢問允許的預計日期／未定欄位顯示，再在 owner 下分清詢問與正式媒合，不偽造 assignment。
- 客戶契約的現行準備入口未如月嫂契約提供同等 current API；尚未以新 public entrypoint 或替代簽約文件繞過。完整資訊1/2送出內容、客戶 PDF 準備、契約金額／入帳日期及 Finance correction 接線皆未完成。
- 已詢問契約應顯示實際收付日或預定日期，待人工回覆。接下來還需確認初步詢問可使用預計日期，尚未確定的報酬／付款日是否標「待確認」。業務語意未確定前不改正式日期、Payroll／應付或模板套值。
- 整體階段未完成；不可交付成完整功能驗收通過。沒有 production、DB mutation、migration、seed、真實 LINE、commit／push。

### 月嫂名冊修訂與驗收邊界

- 使用者後續指定移除名冊上方三個分頁與重複獨立工作台；已移除外層分頁狀態與兩個重複工作台呈現，只保留點開月嫂後的資格、接案偏好、長假／暫停及任職管理小分頁。名冊頁顯示搜尋與摘要卡片；桌面確認外層分頁消失、抽屜小分頁保留，編譯與靜態檢查 passed，功能寫入 not_run。
- 使用者確認待辦看板 UI 通過，不重做已通過畫面；尚未進入功能修復階段。
- 名冊改為等寬摘要卡片，顯示姓名、編號、電話、學歷；移除原先僅選中人員卡片展開接案偏好的不一致排版。完整偏好摘要改放既有詳情抽屜。
- 點卡片或「查看詳情」開啟既有月嫂抽屜，每次從資格主檔開始；沿用原資格、個人資料、接案偏好及接案狀態操作，不改後端。完整身分／銀行資訊不放卡片。
- 本輪來源修改限 `ui_react/src/pages/StaffPage.tsx` 與 `StaffPage.css`；保留工作區其他既有成果。
- 桌面摘要版面與開啟合成人員詳情已檢查；編譯、差異格式與架構歸屬檢查 passed。業務寫入／功能回歸測試 not_run，窄畫面 not_run；月嫂名冊人工 UI 驗收待使用者確認。

### 第二輪 UI 修訂（使用者採用精簡方案）

- 移除所有十三階段的小狀態篩選列及其前端查詢選項；後端子狀態、計數與業務規則不變。
- 移除契約頁選用稽核、技術來源、版本／證據詳情區塊，保留內部驗證及原始紀錄。移除跨階段「待確認事項」總表，僅於所選工作群組內顯示該組阻擋／提醒；既有操作錯誤仍保留，不隱藏失敗。
- 每位候選月嫂卡片新增資訊－1／－2各自的寄送入口，展開指定人選與資訊種類確認區。最終寄送明確停用；候選專屬套值及後端阻擋留待功能修復。資訊－1文案明示不以 BeClass 完成為前提，不代表後端已修復。
- 契約工作預設顯示「下載與簽回」；兩種契約下載卡片直接可見。可下載時沿用既有 document version 下載；無文件或查詢尚未確認時停用且解釋。移除舊位置的重複下載按鈕；既有準備、送簽及簽回操作不另造 API。
- 桌面查閱驗證：既有兩位候選案件的兩種寄送確認區均可展開；契約兩種下載入口及未確認文件空態可見；稽核／技術／總表區塊已移除。契約查詢仍有既有衝突訊息，未當成功、不冒稱 PDF 下載已驗證。
- 靜態編譯與架構歸屬檢查通過；本輪沒有執行寄送、產生 PDF、下載實檔、簽回、回覆寫入或其他功能驗證。窄畫面仍 not_run；停止於再次 UI 驗收。

- 主要整合：`ui_react/src/components/OrderWorkbenchV2Drawer.tsx`、同名 CSS、`ui_react/src/pages/OrderWorkbenchV2Page.tsx`。保留既有十三階段，前端以進件、候選與詢問、推薦、契約、服務、帳務六組導覽，切換不變更進度。
- 候選單一清單：`OrderCandidateContactStatusPanel.tsx` 自動讀取既有人選；分別展示資訊1/2的聯絡紀錄，人工電話回覆表單收在個別人選下。看板候選階段不再顯示五種子狀態篩選。
- `OrderInformationSheets.tsx` 與 `api/orders/order_information_client.ts` 重用既有訂單資訊查詢。未有正式指派時只顯示欄位版面，不虛構人選或套值；資訊1/2各自有預覽，傳送入口明確停用並標示待修復。
- `OrderContractPreview.tsx` 與 `api/orders/contract_full_preview_client.ts` 重用既有零寫入契約預覽。此次是主要欄位版面，不是完整契約／列印驗收；既有送簽與簽回入口另外保留。完整套值與 PDF 仍待功能階段驗證。
- 服務前換人導向同一案件異動入口；帳務導引重用 Finance 頁。客戶收款帶案件搜尋條件；月嫂付款仍需在帳務頁選擇月嫂，不冒稱已依本案篩選。
- 瀏覽器查閱案件 `115990909`（未有候選／正式指派）、`OPS96-SCHED-C-RETEST2`（兩位既有候選），確認空態及有人選清單均有呈現。未執行儲存、建立方案、記錄回覆、送簽或 LINE 傳送。
- 保留原有 dirty 成果，未修改後端、資料庫及模板，未新增測試資料庫，未 commit／push。後續從「使用者 UI 驗收」接續，不重做已完成盤點；若有 UI 意見，先限縮修訂 UI，再進入功能修復。
