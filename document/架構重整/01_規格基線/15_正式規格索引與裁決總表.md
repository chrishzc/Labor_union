# 正式規格索引與裁決總表

## 1. 文件狀態

- 狀態：`approved-architecture-baseline`
- 收斂日期：2026-08-03
- 人工核准日期：2026-08-03
- 2026-08-11 executable finance amendment：`approved`
- 2026-08-11 Contract Signing／簽約前服務承諾／正常驗收資料鏈：`approved`
- 2026-08-13 Staff Matching Preferences、Case Import 下廚條款與 Scheduling 不可服務期間：`approved`
- 2026-08-13 ADR-001 `IMPORT-ENTRY-02`：HCM 日常 Web upload；Client BeClass／Staff 現行
  LIFF typed API；BeClass scripts 保留為 restricted historical import：`approved`
- 2026-08-14 WP77 合併裁決：Staff restricted historical source在 identity、姓名一致且來源報名時間
  嚴格較新時，可覆寫姓名、銀行帳戶與關聯集合；來源空值與受保護 root fields 不覆寫；
  HCM以案件編號防重；新案件若IP位址與姓名同時命中既有Client，視為疑似同一客戶重複申請，
  不載入並通知公會人工確認；只有IP相同但姓名不同不阻擋。HCM與Client BeClass可獨立匯入，
  缺少對方時建立 partial formal case 並標記 warning，待兩方唯一配對後再綁定並補入料理條款：`approved`
- 2026-08-13 WP80 歷史訂單裁決：只以 `case_no + 客戶姓名` 精確匹配既有 Order；0／1／2
  採納為歷史 asserted status，日期永遠可空且 Excel serial 依 workbook date system 轉換；
  雙月嫂缺每人獨立區間時只留配對 evidence，不猜 assignment／服務日／薪資：`approved`
- 2026-08-13 資料匯入中心過渡裁決：管理端以單一「資料匯入中心」排列 HCM、Client BeClass、
  Staff BeClass、歷史訂單與銀行流水等獨立上傳卡；LIFF 穩定前，Client／Staff BeClass 可暫由
  authenticated Web upload 進入各自 typed intake，明示為 temporary entry，LIFF end-to-end 驗收後
  必須移除。HCM 與帳務固定由 Web upload，不得回退 File Watcher／browser SQL：`approved`
- 2026-08-25 資料中心入口規劃：管理端「資料匯入」規劃改名為「資料中心」，內含 `NAS 檔案`、
  `資料匯入`、`數據瀏覽` 三個分頁；NAS 介面只先固定為資料夾與其中檔案名稱的簡單投影，不另列
  層級、用途、案件／人員、版本、大小、更新時間或異常狀態，也不暴露實體路徑或提供原地編輯。
  舊數據瀏覽 URL 應保留為相容深連結並開啟資料中心的數據瀏覽分頁，既有匯入與去敏瀏覽功能不得
  因整併而減少。版型與互動待使用者另行設計，本次不實作：`approved-plan`
- 2026-08-14 訂單狀態與月嫂歷史配對過渡裁決：舊 `historical_orders` 僅保留 source lineage；
  匯入中心以 Orders typed Preview／Apply 補既有 Order 的 status、月嫂配對 evidence 與可空的實際
  服務日期。多月嫂缺個別區間不得猜 assignment／日期／薪資；日常 UI 不得呼叫 legacy CLI：`approved`
- 2026-08-14 WP92 匯入採用與 warning lane recovery：Historical Orders 精確配對且可解析的歷史值
  直接寫入，不比較 current value 或 source time，也不產生 `current_conflict`；無法採用的欄位建立
  typed warning。現階段只授權 import scripts、lane Preview／Apply 與正式寫入；warning center UI、
  typed Query、人工轉態及 WarningReferral 留待後續 Work Package：`approved`。2026-08-15
  WP94 已承接 typed Query 與人工轉態；WarningReferral 仍須 owner-specific Work Package：`approved`
- 2026-08-15 HCM 異常中心補充裁決：未達 HCM 最低 import 條件的來源列只保留
  source review／receipt／outbox 稽核，不投影異常中心，因此不存在 `HCM-CASE-001`。缺漏與
  格式錯誤保持通用 logical code，由 `field_path` 組成人可讀文字，例如「缺少身分證」。
  未登錄 issue 屬 projection contract 故障：整筆回滾、去敏記錄、最多 3 次且間隔 1 秒後 dead-letter，
  不得預設映射成任一業務警示：`approved`
- 2026-08-15 WP95 HCM owner 裁決：異常中心只提供警示、追蹤與 owning 業面跳轉；HCM 缺漏、格式
  錯誤或同案修正版由完整修正來源重新走 owner typed Preview／Apply，不在警示中心或 Streamlit
  編輯欄位。owner commit 後另由 root predicate 自動解除；typed backend 供未來 React 使用：`approved`
- 2026-08-15 Scheduling LINE 請假裁決：已綁定月嫂的 LIFF 請假只建立 Scheduling request evidence；
  管理受理不是正式核准，不得猜案件、服務日或代班人。只有既有 Leave/Substitution Apply 的 canonical
  receipt 已提交且 staff 關聯唯一時可結案並排入 durable LINE delivery task；通知失敗不回滾：`approved`
- 2026-08-15 WP91 Staff 退役：保留 Staff、BeClass、Orders、Payroll 與歷史配對；退役後排除所有新
  Matching。已確認的未來服務保留，未確認媒合失效；復職使用獨立 typed command 且不恢復過期
  媒合資料；第一階段不新增外部通知：`approved`
- 2026-08-11 退款單、應付明細與帳單後核對邊界：`approved`；此最新人工裁決優先於
  既有任何把會計匯款排程或執行納入系統的文字。
- 2026-08-11 Finance Amendment cutover：不要求獨立的正式 production migration procedure
  或 runbook；release record 只須保存目標、revision、digest、operator、時窗與備份／回復責任。
- 2026-08-16 Access Control 本機驗證 closeout：TOTP、帳號中心、security-alert outbox 與本機
  schema upgrade 已完成；production cutover、external security alert sink 與 target-specific 驗收移交
  `25` 與其 proposed Work Package，未取得 target 時一律不得部署：`user-deferred-2026-08-16`。
- 2026-08-21 LINE four-module specification freeze：M1 Alternative A、M2 deterministic Phase 1、M3
  Scheduling Matching Coordination Phase A–D、M4 runtime target／Customer Service escalation ownership：
  `approved-for-specification-freeze`。後續人工已核准 M1-A、M2-A、M3-A～D、M4-A exact production
  implementation slice；provider、deployment、production DB、未另行核准 schema 與 external side effect
  仍未授權。裁決 identity 與 current status 以本節 11 及正式規格／待辦清單為準。
- 2026-08-22 React Phase 3D-H Anomalies public detail／recovery hardening：closed typed evidence、timeline、
  source bindings 與 recovery snapshot 已完成；未知／缺漏／額外／malformed projection fail closed，Query
  零寫入，focused regression `43 passed`。只解鎖 Phase 3D-R 唯讀 React 接線；Claim／Resolve、warning
  transition、owner repair、DB/schema 與 provider side effect 仍不在此完成範圍：`completed`。
- 2026-08-22 React Phase 3D-R Anomaly detail／recovery query：既有 Drawer 已接 typed detail、timeline、
  evidence 與 recovery metadata 的唯讀顯示；detail GET 200，recovery GET 404 時局部 fail closed 為
  unavailable，Claim／Resolve／warning transition 維持 disabled。build與focused驗證已完成；full suite／lint
  與 positive recovery browser 未宣稱，0 DB/schema/mutation：`completed-read-only`。
- 2026-08-23 Case Import workbook policy decision：HCM duplicate identity採`review_only`，HCM Current採
  `WHOLE_WORKBOOK + archive_required`；Client BeClass、Staff Historical、Historical Orders採
  `ROW_ATOMIC_RESUMABLE + archive_required`。archive-first failure固定0 Domain／DB write；row-atomic
  family必須具durable running/progress/resume/terminal receipts。static inventory結果為
  `DB_SCOPE_REQUIRED`；本裁決只完成specification freeze，不授權production code、schema／DB、React Apply、
  entry switch或production host：`approved-policy-decision`。
- 目的：把 `document/文件整併工作區` 的來源保存、狀態機候選與逐表盤點，
  收斂為唯一可人工確認的 `Global → Domain → Subsystem → Module` 規格入口。
- 生效結果：本文件與 `16`～`20` 的既有條款已經人工整體確認，成為後續 Work Package 的
  業務與架構依據；2026-08-11 寫入 `06`、`14`、`16`、`20` 的追償結清、staff payout
  difference、government overpayment disposition 與 typed recovery dispatcher 條款，已於
  2026-08-11 人工整體確認，授權 production code／schema 實作。
- 第 `21` 份規格收斂 2026-08-10 已確認的月嫂先簽、訂金先行核銷、客戶簽回、Contract
  Completion、commitment exact conversion 與 validation dataset 邊界；其可執行範圍由
  已完成並封存的 [`56_Contract_Signing_and_UI_Validation_Work_Package.md`](../04_已完成與上線封存/work_packages/56_Contract_Signing_and_UI_Validation_Work_Package.md) 約束。
- 2026-08-03 的原始核准只授權建立 Inventory v2；不授權 production code、pytest、schema、
  正式資料庫、資料、外部平台、移除、部署、Git stage／commit／push 或 release mutation。

## 2. 權威順序

1. 人工最新明確裁決。
2. 已經人工整體確認的本目錄正式規格。
3. `document/文件整併工作區` 的來源追溯、無損稿與逐表盤點。
4. live schema、API、Service、SQL writer、production caller、UI 與測試，僅作現況證據。
5. legacy `system_map*.md`／`system_map*.yaml` 與 `history/adad/`，僅作歷史證據。

live 現況與正式規格不一致時，記為 `live-drift`，不得反過來把繞過路徑升格為規格。

### 2.1 補助季度撥款與工會墊付裁決（2026-08-04）

政府補助一季申請一次並以整季合計款入帳；Government Subsidy 先把入帳款 allocation 到
claim items。客戶補助退還是工會對客戶的獨立應付，不能把政府入帳當成客戶退款。

客戶在 claim quarter 第一個曆月實際結案時，補助退款日固定為結案月份加兩個曆月的
15 日；該日仍無相關政府 receipt allocation 時，工會執行墊付。未清償的客戶補助
退還必須進入 Accounts Payable Query／Export，並與月嫂應付款明確區分。後續政府入帳
只能核銷既有的工會墊款，不得再次付款給客戶。任何金額或對應不唯一均為 anomaly；
`client_payments.subsidy_refund_*` 是 legacy projection drift，不是正式模型。

## 3. 狀態標記

| 標記 | 定義 |
|---|---|
| `confirmed-inherited` | 已由較新的人工決策或已確認架構明確決定，本次只去重與搬移 |
| `consolidated-decision` | 依較新的已確認原則消除舊文件矛盾，已隨本套文件整體核准 |
| `historical` | 僅保存來源與遷移脈絡，不得形成新依賴 |
| `live-drift` | production 現況違反正式方向，必須另立 Work Package 退出 |
| `human-decision-required` | 現有證據不足以唯一決定，不能由 Agent 猜測 |
| `out-of-scope` | 不屬本次架構收斂，不得順帶實作 |

## 4. Global 架構

### 4.1 Global 責任

Global 擁有：

- Domain ownership 排他性與跨 Domain 不變量；
- command envelope、actor、correlation、fingerprint、idempotency 與 typed error；
- 管理端FastAPI `detail.error`八欄、pre-routing `X-Correlation-ID`與response-only correlation rebase，
  見`00_Global_共同契約.md` §4.1；
- outer Unit of Work、outbox、receipt、retry、timeout 與 conflict policy；
- 身分驗證、同權限內部存取、稽核及外部整合安全邊界；
- preserve-data migration、部署、備份、復原、可觀測性及 release gate；
- Streamlit 薄顯示層與所有正式 API 的共同限制。

Global 不擁有任何 Domain 金額、日期、狀態或資格公式。

### 4.2 Global 不變量

1. Query 唯讀；Preview 零寫入；Apply 以 fresh root facts 重建並驗證。
2. 同一業務命令只有一個 outer Unit of Work owner，Repository／Adapter 不得 hidden commit。
3. 正式狀態、金額與服務日判斷只在後端 owning Domain；所有已登入且 enabled 的內部使用者具有
   相同業務功能權限。
4. 外部 webhook、LINE、File Watcher 與 UI 不得直接寫 Orders、
   Scheduling、Client Finance、Payroll 或 Staff Payables 根事實。
5. 外部副作用一律由已提交的 outbox／durable task 觸發；外部失敗不得回滾已提交
   的 Domain transaction，也不得偽造 Domain 成功。
6. 管理操作要求 service authentication（如適用）與 internal-user authentication；actor identity
   用於操作歸屬及 audit，不以 role／capability 造成內部使用者間的業務功能差異。
7. 正式資料庫不得直接暴露公網；測試、candidate、fixture 與正式資料嚴格隔離。
8. legacy writer 必須移除、只讀或固定回 `410 Gone`；不得以相容 adapter 偷跑舊規則。
9. Streamlit 只呼叫 typed API、保存 UI 暫態並顯示 typed result，不計算業務衍生值。
10. 任何 architecture、data contract、public interface、external side effect 或
    production cutover 變更，都必須重新取得人工確認。

### 4.3 Global SSOT

- 業務語意 SSOT：人工已確認的正式架構與 Domain 規格。
- 身分 SSOT：各 Domain 的 canonical identity 與 immutable mapping event。
- 時間 SSOT：`BusinessClock` 與明確 IANA timezone。
- 金額 SSOT：integer `MoneyNTD` 與 immutable obligation／ledger event。
- mutation SSOT：typed Application Command、outer Unit of Work receipt 與 outbox。
- release SSOT：versioned release manifest、artifact digest、migration journal 與 switch receipt。

Read model、cache、Alert、UI session、XLSX、log、legacy table 與外部平台顯示都不是
業務根事實；只能由正式根事實推導或作證據。

## 5. Domain 版圖

| Domain | 正式責任 | 正式規格 |
|---|---|---|
| Orders | 條款、契約完成事實、生命週期、取消、reopen、服務資料鎖 | `01_Orders_Domain.md` |
| Assignments／Scheduling | generation、assignment、正式服務日、檔期、請假代班與versioned Holiday horizon | `02_Assignments_Scheduling_Domain.md` |
| Payroll | assignment-owned 薪資義務與調整 | `03_Payroll_Domain.md` |
| Client Finance | 客戶應收、收款、退款、沖正、調整及核銷 | `04_Client_Finance_Domain.md`、`16_Staff_Payables與Client_Refund正式規格.md` |
| Staff Payables | 月嫂應付義務、正式出款、退匯／沖正與付款投影 | `05_Staff_Payables_Export_Domain.md`、`16_Staff_Payables與Client_Refund正式規格.md` |
| Anomalies | 根事實異常的 canonical projection 與人工處理進度 | `06_Anomalies_Domain.md` |
| Finance Import | 銀行來源事實、分類、人工修正與 owning Domain dispatch | `09_Finance_Import_Domain.md` |
| Government Subsidy | 申請、送件、核准、政府撥款、allocation 與 reversal | `14_Government_Subsidy_Domain.md` |
| LINE Integration | LINE platform identity、webhook inbox、delivery、review、publication 與 media | `17_External_Integration_LINE_Access正式規格.md` |
| Internal Access | 內部使用者登入、session、actor identity、安全稽核與後續 production cutover／external alert | `17_External_Integration_LINE_Access正式規格.md`、`25_Access_Control_Production_Cutover與External_Security_Alert正式規格.md` |
| Case Import | BeClass／HCM 來源事實、驗證、review 與正式 case bootstrap | `17_External_Integration_LINE_Access正式規格.md` |
| Scheduling Staff Matching Profile | 自訂配對偏好定義、月嫂偏好值與版本；不新增獨立 Domain | `24_Staff_Matching_Preferences與不可服務期間正式規格.md` |
| Knowledge Retrieval | FAQ／crawler provenance、審核發布、index 與非權威 RAG 回答 | `17_External_Integration_LINE_Access正式規格.md` |
| Customer Service | LINE 客服需求、對話事件、處理狀態與人工回覆 | `20_LINE客服與月嫂自助服務正式規格.md` |
| LINE Identity Management | 身分查詢、修正、解除 saga、owner projection 清除與預設 Rich Menu 回復 | `23_LINE身分管理與解除正式規格.md` |

Contract Signing Integration 與簽約前 Scheduling Commitment 是跨 Orders、Scheduling、Client
Finance、LINE Integration 的 application/integration subsystem，不新增第十三個業務 Domain。
其唯一正式補充規格為 `21_Contract_Signing_Commitment與正常驗收資料鏈正式規格.md`。

Deployment、Migration、Performance、Runtime Supervision／Observability、Release 與
Accounts Payable Export 是 Global／Application Subsystem，不是業務 Domain。

銀行流水正常匯入、帳務異常固定處置、歷史重處理與管理端入口治理，另以
`22_銀行流水匯入與帳務異常處理正式規格.md` 為最新正式裁決。

Staff 自訂配對偏好、Orders 匯入下廚條款及 Scheduling 長假／暫停接案，另以
`24_Staff_Matching_Preferences與不可服務期間正式規格.md` 為最新正式裁決。

已完成或被取代的功能開發計畫不繼續作為 active 規格：休假代班精算、Calendar Preview、
單月嫂預設及預計服務日期表由 `02_Assignments_Scheduling_Domain.md` 承接；契約與 UI 情境矩陣由
`21_Contract_Signing_Commitment與正常驗收資料鏈正式規格.md` 承接；配對偏好與不可服務期間由
第 `24` 份正式規格承接。其封存路由集中列於
[`document/功能開發計畫/README.md`](../../功能開發計畫/README.md)，歷史 digest 與 evidence 只由
`04_已完成與上線封存/archive_manifest.json` 擁有。

### 5.1 完整目標架構圖

狀態：`approved-architecture-baseline`。實線表示 runtime call／data flow；虛線表示
架構約束或 adapter implementation。Global 不是 runtime 巨型 Service。

```mermaid
flowchart TB
  subgraph EXT["External Actors / Providers"]
    ADMIN["Approved Admin Client"]
    LINEP["LINE"]
    FILES["Bank / BeClass / HCM / Knowledge Sources"]
  end

  subgraph EDGE["Public / Private Edge"]
    PUB["HTTPS Public Edge"]
    PRIV["LAN / VPN Admin Edge"]
  end

  subgraph APP["Application Zone"]
    UI["Streamlit Thin Presentation Adapter"]
    API["FastAPI Typed HTTP / Integration Adapter"]
    WATCH["File Watcher: Durable-job Producer Only"]
    WORKER["Worker: Inbox / Outbox / Job Consumer"]
    COORD["Application Workflow Coordinators
    Single Outer UoW Owner"]
  end

  subgraph GLOBAL["Global Contracts / Subsystems"]
    CONTRACTS["Actor / Version / Fingerprint / Idempotency
    Typed Result-Error / Clock / Correlation / UoW"]
    INTEGRATION["Integration Security Boundary
    Security Receipt / Durable Inbox / Transactional Outbox"]
    EXPORT["Accounts Payable Export
    Cross-domain Read-only Reporting"]
    PERF["Performance & UX"]
    MIGRATION["Preserve-data Migration / Cutover"]
    DEPLOY["Deployment Security Boundary"]
    RELEASE["Release Orchestration / Approval Receipts"]
    OBSERVE["Runtime Supervision / Observability"]
  end

  subgraph DOMAINS["12 Business Domains"]
    ORD["Orders"]
    SCH["Assignments / Scheduling"]
    PAY["Payroll"]
    CF["Client Finance"]
    SP["Staff Payables"]
    GOV["Government Subsidy"]
    FI["Finance Import"]
    ANO["Anomalies"]
    LI["LINE Integration"]
    AC["Internal Access"]
    CASE["Case Import"]
    KNOW["Knowledge Retrieval"]
  end

  subgraph HEX["Typed Ports / Infrastructure Adapters"]
    PORTS["Repository / Query / Gateway / Archive / Clock
    Inbox / Outbox / Queue / Cache / UoW Ports"]
    DBAD["MySQL Persistence Adapters"]
    EXTAD["Provider / Notification / Archive Adapters"]
    QUEUEAD["Inbox / Outbox / Durable-job Adapters"]
    CACHEAD["Optional Cache Adapter"]
  end

  subgraph DATA["Private Data / Archive Zone"]
    DB[("MySQL")]
    ARCHIVE[("Evidence / XLSX Archive")]
    BACKUP[("Backup / Recovery Candidate")]
    QUEUE[("Durable Inbox / Outbox / Job Queue")]
    CACHE[("Optional Cache")]
  end

  ADMIN --> PRIV --> UI --> API
  LINEP --> PUB --> API
  FILES --> WATCH --> QUEUE
  API -->|"Admin / Business Command"| AC
  AC -->|"Authenticated Enabled Principal"| COORD
  API -->|"Verified Canonical Webhook Inbox"| QUEUE
  QUEUE --> WORKER --> COORD

  COORD --> ORD
  COORD --> SCH
  COORD --> PAY
  COORD --> CF
  COORD --> SP
  COORD --> GOV
  COORD --> FI
  COORD --> ANO
  COORD --> LI
  COORD --> AC
  COORD --> CASE
  COORD --> KNOW

  ORD --> PORTS
  SCH --> PORTS
  PAY --> PORTS
  CF --> PORTS
  SP --> PORTS
  GOV --> PORTS
  FI --> PORTS
  ANO --> PORTS
  LI --> PORTS
  AC --> PORTS
  CASE --> PORTS
  KNOW --> PORTS

  DBAD -. "implements" .-> PORTS
  EXTAD -. "implements" .-> PORTS
  QUEUEAD -. "implements" .-> PORTS
  CACHEAD -. "implements" .-> PORTS
  DBAD --> DB
  EXTAD --> ARCHIVE
  QUEUEAD --> QUEUE
  CACHEAD --> CACHE

  CONTRACTS -. "constrains" .-> APP
  CONTRACTS -. "constrains" .-> DOMAINS
  INTEGRATION -. "constrains ingress / egress" .-> API
  PERF -. "constrains UI / API / Query / Jobs" .-> APP
  OBSERVE -. "observes" .-> APP
  OBSERVE -. "observes" .-> DATA
  EXPORT --> SP
  EXPORT --> CF
  MIGRATION --> BACKUP
  MIGRATION --> DB
  DEPLOY -. "governs topology" .-> APP
  DEPLOY -. "governs topology" .-> DATA
  RELEASE --> MIGRATION
  RELEASE --> DEPLOY

  ORD -->|"Terms / Lifecycle Coordination"| SCH
  ORD -->|"Finance Impact Candidate"| CF
  ORD -->|"Payroll Impact Candidate"| PAY
  SCH -->|"Official Service Facts"| PAY
  PAY -->|"Immutable Staff Obligations"| SP
  FI -->|"Typed Dispatch / Borrowed UoW"| CF
  FI -->|"Typed Dispatch / Borrowed UoW"| SP
  FI -->|"Typed Dispatch / Borrowed UoW"| GOV
  CASE -->|"Owning-domain Bootstrap Intent"| COORD
  KNOW -->|"Non-authoritative Answer / Delivery Intent"| LI
```

### 5.2 關鍵 runtime 邊界

- 管理／業務 API 必須先驗證 internal-user session 與 enabled 狀態，再呼叫 typed Application
  Command／Query；不得依人員 role／capability 形成業務功能差異。root 與其他 enabled 帳號同樣可使用
  全部業務功能；唯一額外權限是 Access Control 帳號中心：依 `17` 的 2026-08-16 裁決，僅唯一 enabled
  root 帳號可執行帳號生命週期管理。
- LINE webhook 通過 request security 驗證後，只建立 canonical durable
  inbox；必須由 Worker 消費後才能呼叫 Coordinator，不得同步穿透至業務 Domain。
- FastAPI／Worker 不直接寫 Domain table。
- Domain／Application 只依賴 typed ports；concrete adapter 反向實作 ports。
- root facts、receipt 與 outbox 必須由唯一 outer Unit of Work 原子 commit。
- Worker 只消費 committed inbox／outbox／durable job；外部副作用不得位於核心交易內。
- Finance Import 只擁有 bank facts／classification，透過 borrowed UoW 委派
  Client Finance、Staff Payables 或 Government Subsidy。
- Orders 跨域 workflow 協調 Scheduling、Client Finance 與 Payroll；Scheduling 的
  official service facts 供 Payroll 使用，Payroll immutable obligations 供 Staff Payables。
- LINE 只負責平台互動。
- Case Import 透過 owning-Domain ports bootstrap；Knowledge answer 永遠
  `authoritative=false`，不得觸發業務 mutation。
- Accounts Payable Export 只讀 Staff Payables 與 Client Finance typed views，
  不擁有 ledger，也不改變付款或退款狀態。

## 6. 優先衝突裁決

| ID | 主題 | 裁決 | 狀態 |
|---|---|---|---|
| CON-SET-001 | 人工月結 | 不建立 draft／finalized／revision／paid 月結 aggregate；`staff_monthly_settlements*` 僅歷史相容 | `confirmed-inherited` |
| CON-SET-002 | 月嫂應付款彙總 | 正式義務為 Payroll 建立的 assignment-owned immutable obligation；`staff_payments` 僅可作 compatibility projection；同月嫂同付款日只在 Query／XLSX 聚合 | `confirmed-inherited` |
| CON-REF-001 | 一般客戶退款 | 納入 Client Finance 正式能力；允許多筆有效出款逐步清償，progress 與 review 分成正交投影；不允許負收款或改寫原交易。後續決策 25、32、42、45、46 已完成 partial/full allocation、return/reversal、Finance Import borrowed UoW dispatch、canonical anomaly 與 Accounts Payable read-only export；第 16 份的現行驗收收據為 `proven-current-evidence`。 | `consolidated-decision` |
| CON-REF-002 | 退款與沖正 | refund 清償退款義務；reversal 使既有正式收款失效並重開原應收，兩者不可共用命令 | `confirmed-inherited` |
| CON-LINE-001 | LINE ownership | LINE 擁有平台 identity、webhook inbox、delivery task、attempt、review 與 publication；不擁有業務 Domain 狀態 | `consolidated-decision` |
| CON-AUTH-001 | 內部存取 | 所有 authenticated、enabled `AdminPrincipal`（含 root）具有相同、完整的業務功能權限；principal 保留作 actor／audit，internal key 只作 service authentication。2026-08-16 追加唯一額外權限：恰有一個 enabled root 可管理 Access Control 帳號中心，不形成其他業務功能差異。 | `reconfirmed-by-user-2026-08-16` |
| CON-AUTH-002 | Access Control cutover | 本機驗證完成不等於 production release；production target、external security alert sink、keyring／時鐘／雙人 MFA、維護窗與 rollback owner 必須由後續精確 Work Package 另行確認。 | `user-deferred-2026-08-16` |
| CON-JOB-001 | Durable Job canonical equality | 2026-08-21 exact核准Option A：equality為type＋version＋canonical object payload＋immutable actor；correlation僅觀測、key限lowercase ASCII、repository不得hidden commit。2026-08-24 fresh rerun：core／worker／schema 36 passed、route caller focused 47 passed；以唯一 ephemeral `lu_test_*` DB 實證 canonical equality／collation 與 claim→crash→recovery 5 passed，並於 fixture teardown 清理。Bridge、caller adoption與 closed public outcome 已在 current source 實作；個別業務功能仍須各自完成真 API／Chrome acceptance，不能以 JobAccepted 冒充業務完成。 | `implementation-complete-engine-verified` |
| CON-DEP-001 | 部署 | deployment profile、target host、edge provider、RTO/RPO 與 target-host acceptance 已依決策 53 退役，不再是產品設定或 release gate；僅保留 MySQL 私有、公開 HTTP 受控與 secret 不進 Git 的安全邊界。 | `retired-by-user-2026-08-09` |
| CON-GOV-001 | ADAD 語彙 | 全部降為 historical；改用人工架構確認、Work Package、明確 write scope 與 evidence matrix | `confirmed-inherited` |

2026-08-20 traceability note：`PROV-20260817-line-knowledge-authorization-normalization-work-package.md`
已取得 exact approval 並完成 `G0-G6`；其 Access/FastAPI、LINE compatibility projection、route inventory 與
current regression evidence 完成此包範圍的 live implementation traceability，不變更 `CON-AUTH-001` 的正式
invariant，也不把 Knowledge direct authorization 納入本包。provider、browser、deployment 與 React UI
cutover 仍由相鄰工作包承接。

## 7. Subsystem 共同完成條件

每個 Subsystem 必須同時記錄：

1. owner、root facts、derived values 與 non-goals；
2. Commands／Queries、typed input／output／errors；
3. state machine、guards、transaction、lock 與 commit owner；
4. idempotency identity、replay、stale、retry、timeout 與 conflict；
5. outbox、external side effects、alerts 與人工操作入口；
6. Repository／Clock／Gateway／Archive 等 ports；
7. legacy writer／caller 退出條件；
8. Module、Subsystem、Domain、Global 四層驗收。

## 8. 來源文件處置

- `document/文件整併工作區/01`～`04`：`historical-source`，完整保留，不直接實作。
- `05_潛在狀態機規則盤點.md`：`source-analysis`；已收斂項由本總表與 `16`～`24` 覆蓋。
- `06_欄位權威性與計算邏輯盤點.md` 及子文件：`field-lineage-source`；
  schema 現況與未決欄位仍可補充，但不能覆蓋正式 owner。
- `07_正式規格收斂出口.md`：`bridge-to-formal-spec`；只指向本套文件，不另立語意。
- `08_非Markdown附件索引.md`：`attachment-evidence-index`；九份業務內容附件已依目前
  hash 逐一裁決為 `historical`、`historical/source-lineage`、`conflict`、`mixed`、
  `historical/legal-template-evidence` 或 `historical-operational-report`，不覆蓋正式規格。
- 舊文件中的「已實作」只能轉成 `implementation-evidence`，必須另以 live chain 驗證。
- 舊文件中的 ADAD／Checkpoint／Source Lock／system map gate 語彙全部為 `historical`。
- `document/架構重整/04_已完成與上線封存/superseded_specs/契約整合與正常測試資料鏈_決策草案.md`：`superseded`；人工裁決已
  收斂到第 `21` 份正式規格與 Work Package `56`，原檔只保留來源歷史。

### 8.1 本輪 live evidence snapshot

稽核日期：2026-08-03。觀察時 HEAD 為
`4081a9b40c91a030c64f1d488411287ec6c01bdc`，但 worktree 有既存未提交修改；
因此 HEAD 不足以綁定內容，下列 SHA-256 才是本輪讀取證據，不代表規格已實作：

| Path | SHA-256 |
|---|---|
| `line/line_bot.py` | `25125230bfa0de48e2d76e5eeef853bde54cc8b897187eca0a95205d6576fd8b` |
| `api/dependencies/admin_auth.py` | `b5bd899effc1d7788472cc3d0e1886c1d27045429283d92a922d97eab52ee852` |
| `services/admin_auth_service.py` | `20b40e1aa1ca4684464669aaa84efbab4c5b9edddd46aa031c15edeb6a698968` |
| `services/line_task_service.py` | `53ed139641b422bc159221316f3d25b304f9cc2ed6fd4b6ed859b92d41bcf7a0` |
| `services/line_review_service.py` | `e9f6a380c2aad40f5af59229d1a67e07aafe66aa704184aa264f6ee9e6bf3b0e` |
| `db/schema.sql` | `c2e859a213d46c1c56b49c138f7b2a982060c1682cf89f27cdd3263e48bf5a8e` |
| `online.bat` | `006701b2a6a916224b7bcbdc381f9d3d8d24bf3da0a1b96ed4f0f30b06c8ef62` |
| `start.bat` | `282fa47717f086c1a731a71eee92a3ce9c99da3449228072d84b6044fad43e07` |
| `AGENTS.md` | `3d3f0ee697e0315d33fc2b9c093405eb72e03fe92c8f2b01557303d598ff7c13` |

### 8.2 附件內容裁決（2026-08-09）

附件語意預審與人工裁決收據為
`03_追蹤清單與證據/evidence/2026-08-09_human_content_review_precheck.md`。其結果已回寫
`08_非Markdown附件索引.md`：舊 LINE 提案與帳務／來源資料為 `historical`，跨 Domain
流程圖、異常中心草案及 generic data-browser 提案為 `conflict`；契約與週報含實質法律或
營運內容，僅作限制存取、保存責任敏感的歷史證據，不授權 provider、API 或 write SSOT。
任何後續功能只可依本目錄正式規格與明確 Work Package 立案。法律效力與保存年限由
業務／法務負責，不構成產品入口授權。

## 8.3 Finance Amendment acceptance linkage（2026-08-11）

`06`、`14`、`16`、`20` 所授權的 Finance Amendment production code／schema 實作，
其 completed implementation scope、集中驗收與 production release 前置資料分別固定連結為：

- [Work Package 55（封存）](../04_已完成與上線封存/work_packages/55_Finance_Amendment_Executable_Contracts_Work_Package.md)：
  completed scope、owner、invariants、typed dispatcher 與 entrypoint governance 結論；
- 2026-08-11 finance amendment regression：`82 passed, 2 skipped` focused cross-contract plus
  governance evidence；current completion 已由相鄰封存 Work Package 與 validation closeout 承接，
  不再指向缺失的舊 evidence 檔案；
- [Finance Amendment validation closeout（封存）](../04_已完成與上線封存/work_packages/57_Finance_Amendment_Production_Release_Readiness.md)：
  isolated-test schema、UI Preview／Apply 與 focused regression 的完成記錄。

2026-08-11 Finance Amendment 的核准實作與 test-environment acceptance 已完成並封存。此範圍不
執行 deployment、production schema application 或任何會計匯款，也不要求另立 production migration
procedure 或 runbook。

## 9. 尚需人工決定

無。LINE review、Rich Menu、管理員 session、Security Audit 與內部使用者同權限政策均已決定；
deployment profile／target-host acceptance 已依
`../04_已完成與上線封存/release_records/53_Deployment_Profile_and_Target_Host_Acceptance_Retirement.md` 退出產品設定與 release gate。

## 10. 人工核准紀錄

2026-08-10 人工明確確認月嫂先簽、簽約前服務承諾、訂金可先核銷、客戶簽回後完成
Contract Completion，以及訂金與客戶契約皆成立後才能 exact conversion。2026-08-11 人工要求
將該裁決收斂至正式規格並建立可執行 Work Package；核准效果為：

- `21_Contract_Signing_Commitment與正常驗收資料鏈正式規格.md` 成為此範圍 SSOT；
- 已封存的 [`56_Contract_Signing_and_UI_Validation_Work_Package.md`](../04_已完成與上線封存/work_packages/56_Contract_Signing_and_UI_Validation_Work_Package.md) 定義 production code／schema／pytest
  的 write set 與 acceptance；
- production DB、validation DB 重建、LINE 正式傳送、部署與 cutover 未隨文件收斂獲得授權。

2026-08-03 人工明確核准：

> 核准 15～18（包含完整目標架構圖）為正式架構基線；目前只授權 Inventory v2，
> 不授權移除 production code。

核准效果：

- `15`～`18` 與第 6 節裁決成為正式架構 SSOT；
- 第 9 節未決營運參數仍 fail closed，阻擋相應 release；
- 本輪可建立 read-only／analysis-only Inventory v2 artifact；
- 一次性核准狀態同步已完成並 freeze；approval receipt 固定於
  `document/架構重整/03_追蹤清單與證據/evidence/architecture_approval_2026-08-03.json`；
- Inventory v2 active exact write set 只限：
  `document/架構重整/03_追蹤清單與證據/evidence/writer_inventory_v2/README.md`、
  `inventory_v2_candidate.manifest.json`、`inventory_v2_candidate.rows.jsonl`、
  `inventory_v2_candidate.sources.json`、
  `inventory_v2_candidate.reconciliation_receipt.json`；
- 不得修改 production code、pytest、schema、資料或外部平台；
- 任一移除、遷移、`410`、caller replacement 或測試實作必須另立 Work Package
  並取得人工確認。

## 11. 2026-08-21 LINE four-module decision identity／status index

本節是 2026-08-21 人工批准的 specification／Work Package freeze index。`approved`／`approved-for-specification-freeze` 只代表文件語意與後續 exact scope 已凍結，不代表 production、schema／DB、provider、deployment 或外部副作用已獲授權。

| Decision／Work Package identity | Scope owner／frozen effect | Current status | Implementation／DB／provider boundary |
|---|---|---|---|
| `PROV-20260820-line-liff-binding-ownership-reconciliation` | M1 Alternative A；`line_identity_bindings` 唯一 writer；Case Import 擁有 provisional registration；legacy direct writers guarded／410；onboarding 為 binding projection outcome | `M1-A backend completed / sandbox E2E user-waived` | 已知 legacy surface與canonical binding focused evidence完成；真實 LIFF／provider／cutover未驗收 |
| `PROV-20260821-line-ai-router-architecture-decision` | M2 reject production full AI；Phase 1 deterministic harness＋durable manual fallback；Phase 2 proposed；human／wrong precedence；exact protected alias only | `M2-A backend completed / module partial` | direct `reply_provider`已由durable task取代；business failure整筆rollback後另交易記錄失敗；AI/provider/deployment未授權 |
| `PROV-20260821-matching-coordination-successor-contract` | M3 Scheduling Matching Coordination subsystem；Phase A–D；accepted→fresh-effects→Assignment conversion/rematch；Phase D typed ports only | `approved backend scope completed / real E2E user-owned` | focused `165 passed`；service-date Apply fresh-lock與leave canonical receipt Preview／Apply完成；stale在lineage／receipt前rollback；M3不寫Leave／Orders／Assignment／Payroll，真實DB/API／跨域UI E2E未宣稱PASS |
| `PROV-20260820-line-runtime-alert-target-admin-contract` | M4 target owner；history-preserving disable、active singleton、opaque CAS、same-key replay、new-key no-active=`not_found`；registration/reset/enable/disable 共用 0-schema advisory boundary | `M4-A backend completed / module partial` | singleton／typed API／masked audit與manual recovery完成；provider／React E2E未驗收 |
| `PROV-20260821-line-human-escalation-closed-loop-contract` | M4 Customer Service escalation owner；Anomaly only source；HIGH only escalation；兩張 additive candidate tables；M2／runtime target sole-writer ports；Scheduling不在交易 | `local schema applied / backend completed` | create→claim→handling→resolve與hold release已完成；0 seed／backfill／destructive；production DB／provider未授權 |

### Cross-module invariants

1. Eraser current module IDs (2026-08-21 MCP fresh-read) are M1 `9vI_ssJZUHa59Yw7LXc0d`、M2 `xS5rOAuuQCUL139Tp4RA`、M3 `IXLp95YCVlOmYlkS1gBkl`、M4 `bYdfiEJlAm-XhTLuLoJ-`；previous M1 `1XwrLwQvzREt3gYfwac2` and M3 `A9x4TWcBBN1l6Ppn0-ot` are stale workspace mappings；diagram identity is input evidence, not authorization。
2. `accepted` is a decision followed by fresh-effects／typed Assignment conversion/rematch; it never directly writes Orders、Assignment or Payroll。
3. explicit human／wrong precedes all automatic routing；only exact protected identity aliases without those markers enter identity。
4. runtime target lock failure is 0 write；unknown post-commit release does not return success and must query the original idempotency key receipt。
5. Each writer has one owner: `line_identity_bindings` LINE Identity、provisional registration Case Import、M2 `service_help` M2 owner、runtime target `runtime_alert_application` Runtime Monitoring／LINE owner、M4 escalation Customer Service；typed ports cannot become competing writers。

### DB gate summary

M3 Phase E 與 M4 escalation 的 candidate inventory／spec planning 可標 `PASS`；Static release、Descriptor、Read-only plan、Engine verification、Developer acceptance 均 `NOT_RUN`。沒有 DDL 或資料庫操作，總結固定 `DB_CHANGE_NOT_READY`。

## 12. Phase 3D-W-H Import Warning terminal receipt裁決（2026-08-22）

`PROV-20260817-react-admin-phase3d-warning-transition-receipt-hardening`已完成backend收旂：Preview為零寫入獨立view，
Apply回terminal receipt，同key／同payload回同一receipt並標示replayed，同key／異payload回conflict；已認證
receipt GET可在outcome unknown時重查，回應不含PII或raw evidence。receipt identity沿用immutable tracking event
identity，event、current projection、receipt、outbox由單一outer UoW提交。Required focused command `39 passed`，
其中含2支真`lu_test_*` MySQL E2E；0 schema／seed／backfill／destructive change。本裁決只解鎖Phase 3D-W-R，
不授權Claim／Resolve、owner root repair／re-import、HCM Apply、provider、deployment或cutover。

## 13. Phase 3D-W-R Import Warning React裁決（2026-08-22）

`PROV-20260817-react-admin-phase3d-w-r-warning-transition-react`已完成strict React client／adapter與
Query → Preview → Apply → terminal receipt → authenticated receipt re-query。full React 119 files／757 tests、
focused direct-affected 8 files／66 tests、build、lint與檔案品質檢查PASS；真FastAPI＋Vite browser在development
`lu_test_*`觀察到`open v1 → awaiting_external_confirmation v2`一致receipt。Drawer close在Apply／unknown／re-query
期間原生鎖定，Claim／Resolve／owner root repair維持disabled。本裁決0 schema／seed／backfill／destructive change，
不授權provider、deployment、entry cutover或來源資料修復。

## 14. Phase 3D-DB-H Data Browser masked query裁決（2026-08-22）

`PROV-20260817-react-admin-phase3d-db-query-public-contract-hardening`已完成六來源allowlist、PK ascending cursor、
server-masked完整list-row detail、strict views與Global typed error OpenAPI matrix。focused 12 PASS；真development
`lu_test_*`查詢六來源、cursor、masking且SQL audit只允許SELECT／0 commit；GET-only API workflow exact 1 operation、
25 events、0 failures／unique。0 schema／seed／backfill／destructive change。本裁決只解鎖Phase 3D-DB-R，
不把legacy raw table metadata／source correction冒充ready，也不授權entry cutover。

### 14.1 Phase 3D-DB-R React Data Browser 狀態（2026-08-23）

`PROV-20260817-react-admin-phase3d-db-r-react-work-package`已完成六來源typed Query、搜尋、cursor、遮罩Drawer、
去敏複製、stale request guard與唯讀mutation controls。Chrome以真FastAPI＋development `lu_test_*`逐一載入六來源；
受控fault injection驗證403、10秒timeout與schema mismatch均fail closed、0 stale rows且可重試復原。focused
`6 files / 16 tests`與TypeScript no-emit PASS。此slice狀態為`completed-read-only`；0 schema／seed／backfill／
destructive change，不新增detail GET、不開啟source correction／PATCH，也不授權entry cutover。

## 15. 營運作業 React 前端優先狀態（2026-08-23）

人工優先順序已調整為先完成營運作業前端，再進行後續 Phase。Current status：

- 2026-08-24 fresh recheck：React full suite `146 files / 934 tests` 與 `npm run build` 均 PASS；這只恢復
  current source 的 compile／test gate，不能取代 Chrome 實機業務驗收。Finance `#finance` 已以受控
  `lu_test_*` 完成正常 ready-dispatch 的 Upload → Preview → confirmed durable Apply → worker terminal
  receipt，並讀回核銷成功、pending 為零。正常匯入頁已依正式規格收斂為「上傳檔案 → 預覽 →
  匯入完成」，不再顯示歷史批次、Manifest、review rows 或 Reprocess；異常只導向帳務異常中心。
- Client BeClass 於 development `lu_test_dataset_contract_signing_v4` 已重新以 Chrome 走完 unique workbook
  Preview → confirmation → Apply；既有來源列回 receipt、無 alert，並讀回 `requires_cooking=true`、
  `lifecycle_version=2`。唯一來源的料理條款補正可穿越無 segment 的歷史 `actual_start_date`，但不得改寫
  其他服務條款或跨越 service-data lock。
- 服務日期精算已完成固定排休覆寫：`週休1日` 預設週日、`週休2日` 預設週六日；Chrome 實際點擊固定
  排休後，以 `custom_work_dates` 重跑 server 精算並顯示人工服務日，再次點擊可恢復固定排休。國定假日
  與事前請假維持優先休假。Python focused `4 passed`、React focused `12 passed`、TypeScript build PASS；
  此功能只形成事前服務日期候選，正式指派後的請假／代班仍由 Scheduling workflow 擁有。
- 營運作業六頁的 typed page runtime 與既有靜態案例 Chrome 驗收已完成；Orders、OrderTracker、Scheduling、
  Staff、Data Import、Reports 均能載入且未顯示「後端未提供／未開放／API 錯誤」。此狀態只代表
  當時的 `page-runtime-accepted`；後續同一節 `115000152` 已完成新匯入訂單 11 步 lifecycle E2E，
  不得再依這筆較早頁面快照重開 lifecycle。原始 UI 設計的視覺對齊另列後續工作。
- Data Browser 六來源 Query、遮罩、錯誤復原維持 `completed-read-only`。
- HCM Current、Client BeClass、Staff Historical、Historical Orders 四類已完成 Chrome Preview → Apply →
  receipt → 同檔案 replay；positive scoped scenario 各 1 列，首次 Apply 建立所需 order／client／BeClass／staff／
  historical adoption，重播均明示「整份工作簿已重播／未新增寫入」並把數字標為原始 receipt 統計。
  `pending／outcome_unknown` 期間鎖定換檔、Preview、站內導航與 `beforeunload`，保留相同 idempotency identity
  安全重試；blocked／unmatched／review receipt 固定顯示需檢查，不得宣稱成功匯入。Historical Orders 的
  `review_required` 是 overlay，不納入 `adopted + unmatched + current_conflict` terminal 守恆總數。
- 2026-08-23 新訂單 lifecycle 驗收已從 Chrome 資料匯入中心實際選取 HCM workbook，完成 Preview（1 筆可寫、
  0 warning／review）與 Apply（新增 1 筆、receipt `#47`），建立唯一測試案件 `116990823`；OrderTracker 以 UI
  載入下一頁後讀回同一案件、完整電話／地址與 Step 1 checked completed。此資料保留供後續 Step 2～11
  累積 lifecycle 驗收，不得把單次匯入成功宣稱為整體 11 步完成。
- 2026-08-24 fresh lifecycle 案件已用 Chrome 走過匯入、媒合人工回覆、雙邊簽回、訂金核銷、日期確認、
  正式指派、實際開始與 canonical Orders 服務完成，並投影至第 7 階段。OrderTracker 的 Orders
  `service_completion` 已改讀唯一 canonical `order_auto_completion_apply_receipts`，不再錯讀 service-data
  lock fingerprint；Chrome 回讀為 completed。Staff #531 的完整 open obligations 已使用正常三步 Finance
  Import 做唯一帳戶、足額 `ready_dispatch`，terminal 顯示核銷 1／pending 0；Staff Payables v1 回讀三筆
  obligation 均 `completed`、正式 payout event NT$26,400。Orders settlement 改讀
  `staff_payable_projections` 後，案件 `115000151` 的 Step 11 與三個獨立結清投影均 completed。正常頁已移除
  「標記已付／新增調整（未開放）」假操作。Step 3 的候選聯繫與 Step 5 的客戶履歷傳達仍缺獨立人工或
  provider 根事實；這只表示 `115000151` 未完整，不再代表總目標未完成。後續 `115000152` 以各 owner
  人工根事實補齊並完成 E2E，仍不得用後續決策事實倒推補造舊案。
- 2026-08-24 另以唯一 fresh 案件 `115000152` 從 Chrome 完成 HCM 匯入、typed 媒合查詢、候選池、
  Step 3 人工候選聯繫證據、意願、正式方案、Step 5 人工客戶履歷說明、客戶決定、雙方人工簽回、訂金
  核銷、日期確認、正式指派、實際開始、服務完成與月嫂付款。OrderTracker fresh readback 的 Step 1～11
  全部 completed，`service_completion`、`client_settlement`、`staff_payout` 三個獨立結清投影亦全部
  completed；兩個人工證據是各自 owner root，不由後續狀態倒推。此案例完成全新匯入訂單的 11 步正向
  lifecycle E2E；不代表真實 LINE provider delivery 已驗收。
- 排班甘特 corrective patch 已改由 server `planned／active／completed` 與全案第一個正式服務日控制顯示；
  active／completed 不投影 stale assignment buffer，buffer 不再標為服務中。仍啟用的 legacy monthly-schedule
  Query 亦已移除「服務中／訂單完成」的合成 7 天 buffer，只允許尚未開始的 `訂單成立` 指派投影該 buffer；
  lifecycle／monthly-schedule focused Python `32 passed`。
- React current-calendar 已對目前已載入的每位服務人員各查一次 bounded server projection，選取列不再重複
  發送 GET；waiting filter 已納入 `assignment_buffer`，日期軸、今日與 query `as_of` 固定使用
  `Asia/Taipei`。Client 另 fail closed 驗證 `days` 完整涵蓋 request range 且連續升冪；成功查詢但無占用
  顯示「本月無排班占用」，不再誤稱尚未查詢。Chrome 實測首批 20 列全部有 typed 結果、0 role alert、
  0「後端未提供／未開放／尚未查詢資格」；缺 service-time tuple 的 legacy 列明確顯示待補正。React
  scheduling focused `19 passed`、build／lint PASS。月曆 retry 只重查失敗的服務人員列，已成功列不重新
  發送 GET；月份切換仍為每個可見人員各查一次，切換已預載人員為 0 額外 GET。
- OrderTracker 11 步 SOP corrective 已完成：React 依 typed status 顯示 completed 勾選、唯一 current step 橘色
  強調、blocked／unavailable 分流與 ordered-list 語意；projection failure 不再偽裝 11 筆「尚無紀錄」。Backend
  已用 current plan-scoped matching notification／latest response 與雙方 signing `sent／signed_received` owner facts，
  並以 official service dates、service-time tuple、BusinessClock 取代 stale assignment status count。Chrome 驗證
  `113000012` step 1 為 `in_progress／aria-current=step`、`115000002` step 9 為 checked completed，頁面 0 role alert、
  0「後端未提供／未開放／API 錯誤」；focused Orders Python `10 passed`、React `6 passed`、build／lint PASS。
  本機舊測試資料中的主要案件其實已有 effective generation、assignment 與 assignment-owned official
  service days；真正缺口是完整 service-time tuple、finance／payroll、contract／deposit、matching confirmation
  與 commitment roots。這些案件維持 typed `official_service_period_missing` 或相應 owner blocker，不得重複
  補 Scheduling lineage，也不得以 `orders.actual_*` compatibility fallback 偽造成服務完成。
- 2026-08-23 single-order lifecycle 盤點發現 Case Import 可直接建立完整 Orders terms root facts，但舊 Step 1
  projection 只承認 `order_terms_change_events`，可能使後續 LINE／媒合已有進度、Step 1 卻未完成。Corrective
  已讓完整 import lineage 直接滿足 Step 1，缺欄位匯入仍維持 `in_progress`，無 import／terms lineage 則維持
  unavailable；focused Python `13 passed`，Chrome 已驗證既有完整匯入案件 Step 1 顯示 checked completed。
  此早期 corrective 的待辦已由 `115000152` 正向案例關閉；不得以前端補勾、直接修改狀態欄或重建
  新案例重跑已完成 lifecycle。
- OrderTracker 不再把 `current_stage_code=null` 的歷史缺陷資料顯示成正常「尚待分類」泳道；它們被隔離為
  「歷史資料待補正」，逐卡顯示缺失 owner fact，並明示這不是業務階段且系統不猜分類。Chrome 實測
  0 role alert、0 舊分類標籤、0「後端未提供／未開放」，React Tracker focused `7 passed`。
- Orders 契約 Drawer 的 terms／completion／detail／signing 四個 required GET 已改為 fail closed：任一
  transport／schema／identity failure 只顯示 typed query alert，不再渲染條款、簽署、mutation controls 或偽
  business absence；四來源成功後才允許合法空資料呈現。React Orders focused `19 passed`，Chrome success
  path 0 role alert、0「後端未提供／未開放」。
- Staff 三分頁已完成 frozen staff identity、request／response identity binding、retry 與合法空資料分流；Chrome
  已讀回匯入服務人員 `#8950`，Drawer、偏好與 Availability 均維持同一 identity，日期查詢以真 API 顯示
  合法空結果。Staff focused `20 files / 88 tests`、lint PASS。
- 2026-08-25 Staff `#531` 已用 Chrome 在可見 Drawer 面板建立與案件 `115000015` 服務期間重疊的長假
  `2026-12-10`～`2026-12-11`，走完 Preview → Apply → typed receipt／fresh readback；收合時 mutation controls
  不再掛載，Availability GET 與 mutation Preview 使用不同 request generation／AbortController，日期刷新不再
  中止 Preview。Matching fresh query 由原本唯一完整候選 `#531` 變成 0 位，Calendar 12 月顯示同一長假；
  其後由同一 Drawer 以明確取消原因完成 Preview → Apply → receipt/readback，Matching 恢復 `#531`，Calendar
  恢復「本月無排班占用」。此 Staff → Matching → Calendar → cancel → restore browser chain 為
  `completed-runtime`，不擴張 accepted plan、正式排班或 provider 授權。
- Reports 已完成季度／年度 typed query、`general + subsidized` 雙 partition、admin-only XLSX export、stale export
  state reset 與 request sequence guard；Chrome 季度查得 11 筆並成功下載 XLSX，切換年度不保留舊成功狀態。
- Orders card projection 的 unavailable／blocked、Matching active plan failure 與 OrderTracker settlement fallback
  runtime corrective 已完成；歷史缺根事實一律走 typed correction，不再吞錯或偽裝空資料。
- 內部管理端以已認證且 enabled 的 session 查詢 Orders／OrderTracker card projection 時，Client owner 已登錄的
  電話與服務地址以 canonical 完整值顯示；缺值或 version 缺漏仍回 typed unavailable／blocked，不得 fallback
  造值。此裁決不擴張 LINE／LIFF、Delivery、Security Audit、error／log／receipt／evidence、Data Browser 或
  Reports 的去敏契約。
- 最終 React full suite `132 files / 863 tests passed`；Operations corrective fresh focused `27 passed`，資料匯入與
  reports／historical order Python focused `54 passed`；production build、lint、strict UTF-8、structured headers 與
  `git diff --check` PASS。測試內刻意 abort 的 localhost connection stderr 與既有 React `act(...)` warning 不構成
  runtime API failure；其測試均通過。

上述 Reports `completed-runtime` 只適用季度／年度補助核銷；未涵蓋營運週報。2026-08-23 人工指出週報遺失，
並指定 `document/管理端UI/表格需求模板/週報.xlsx` 的 current-year 三個業務分頁為需求來源，取代已放棄的
Stitch「營運分析與周／月度報表」畫面。此裁決授權下列 exact GET／XLSX public contract 與 React successor，
不授權 schema、DB mutation、production host、provider、deployment 或 entry switch：

### 15.1 營運週報三分頁正式契約

1. 分頁固定為「週報案件受理總表」、「補助案件統計表」、「每週服務中與工時」；活頁簿內 `113`、`114`
   為歷史年度頁，不是 current React tab 或新匯出 worksheet。
2. 報表期間以 `Asia/Taipei` 週一 00:00 至週日 23:59:59 為一週；public Query 使用 canonical Gregorian
   `week_start`，且非週一固定回 validation error。跨年週歸屬於 `week_start` 所在 Gregorian year。
3. 案件受理 owner 是 Orders／Client read facts：以 `clients.created_at` 落在週界內納入，顯示案件識別、
   去敏申請人、身分類別、審核結果、訂單狀態、服務天數／每日時數、預計服務起訖與區域；status aggregate
   直接由 server rows 計算。缺 order、日期、時數或無法判定的歷史列仍保留並附 typed data-quality code，
   不得移除、猜分類或以 0 補值。
4. 推廣次數、詢問人次及「一般／補助不符合」若沒有 canonical root fact，回 `null` 並在 `data_quality_issues`
   明示 `manual_metric_not_recorded`／`rejection_partition_unknown`；UI 顯示「未登錄／待補正」，不得顯示
   「後端未提供」、不得把未知值當 0，也不得在瀏覽器自行保存假資料。
5. 補助統計 owner 維持 Government Subsidy reconciliation formula；週報只協調 selected week 截止日以前的
   year-to-date server-redacted rows、partition totals 與金額，不在 Reporting 重算補助單價／上限。無核銷月份
   root fact時同樣以 typed issue 表示，不得由前端推算。
6. 每週服務 owner 是 Scheduling effective generation 的 `staff_schedule` 正式工作日；只有 selected week 內
   `effective_marker=1 AND is_work_day=1` 的日期計入。`weekly_work_days` 是該週正式工作日數，
   `weekly_hours = weekly_work_days × Orders.service_hours_per_day`；active／completed 都依正式工作日呈現，
   不加入 7 天防撞 buffer，取消／replaced assignment 不得列入。
7. Dedicated Reporting Query 為 `GET /api/v1/operations-reports/weekly?week_start=YYYY-MM-DD`，回 strict、
   server-redacted `weekly-operations-report.v1` view；完整匯出為同 path 的 `/export`，filename 必須含
   `week_start`／`week_end`，content type 為 XLSX，且 worksheet 名稱／順序固定對應上述三分頁。
8. Query 唯讀、export 不保存 workbook；兩者使用相同 server candidate 與 source revision。React 必須提供
   週起日選擇、三 tab、reload、完整 XLSX 下載、loading／empty／error／retry、request abort 與 stale export
   reset；不得移除既有季度／年度補助報表。
9. Focused acceptance 至少涵蓋週界 validation、歷史缺欄位、非空／空週、aggregate 守恆、PII 遮罩、
   Scheduling official-day 工時、三 worksheet contract、季度／年度 regression；最後以真 FastAPI＋
   development `lu_test_*` 在 Chrome 驗證三 tab、非空資料、空週與 XLSX 下載。

本 slice 狀態為 `completed-runtime`。Focused backend／React、TypeScript、真 FastAPI＋development
`lu_test_*`、Chrome 三分頁／非空週／空週／完整 XLSX 下載及既有季度／年度報表 regression 均已通過；
Reports 與 Operations 六頁總狀態可恢復為 `runtime-accepted`。其餘 Operations runtime corrective 維持已驗收；
不得對已有 effective Scheduling lineage 的 legacy 案件重複補資料，真正 drift 必須走 typed
anomaly／correction。本節沒有 schema／migration 變更。

## 17. LINE React successor 與 caller adoption 狀態（2026-08-25）

依人工最新明確裁決，LINE 前端連線優先於後續 phase；React caller 可採用 `17`、`20`、`23` 已完成且
具 strict public response model 的既有契約，但不擴張 provider、production DB、schema／migration、部署、
external side effect 或新 public contract。所有外送仍只由已提交 durable task 執行，瀏覽器不得直接呼叫
LINE provider。

1. Canonical React entry 固定保留四個獨立入口：`line-management`／「客服與選單」、
   `line-ai-events`／「AI 事件工作室」、`line-liff-studio`／「LIFF 卡片工作室」、
   `line-security`／「群組與安全」。`line-ai` 與 `line-studio` 只分別 alias 至其對應 canonical entry，
   不得把三頁再次 redirect／收斂回 `line-management`。接 API 時必須保留原設計的功能、操作入口與業務說明；
   應移除的是假 runtime facts、demo token、假成功及未授權 provider side effect，不是刪除未接線的功能。
2. Customer Service 已接 summary／list／detail 與 resolve Preview／Apply；Apply 必須沿用 Preview 的
   fingerprint、expected version、correlation 與 idempotency，完成後重新查詢。
3. LINE Identity 已接 masked list／detail 與 revocation Preview／Apply；若 default Rich Menu publication
   缺少，應顯示 canonical blocker，不得假造可提交狀態。Apply 只代表 durable 解除申請已受理。
4. Rich Menu 僅接 safe configuration／publication query；Delivery 已接 masked summary／list／detail／
   attempts；Order Groups 已接 list／detail／events。未獲 mutation 授權的 provider button 不得以 disabled
   shell 暴露。
5. M4 已接六種 safe configuration、runtime alert target list／candidates／add／reset／enable-disable，以及
   human escalation create／detail／claim／handling／resolve。所有 mutation 使用 server version、caller
   correlation／idempotency 並顯示 receipt；UI 不推導 action eligibility。
6. M3 Scheduling Matching Coordination 的 1 Query、7 Preview、8 Apply／decision strict React clients 已完成，
   並已接入 Scheduling canonical page 的「媒合協調工作台」。工作台可先查 root facts、自動帶入 source
   tuple／snapshot／package，再依 closed operation 選單執行 typed Preview 或經人工確認後 Apply；不得以
   通用 raw endpoint 或瀏覽器重算取代。LINE React contract caller 狀態為 `runtime-accepted-with-data-blockers`：
   現有 legacy 案件若服務日期未確認或尚無 snapshot，顯示 typed 補正指引，不假造成功資料。
7. M2 deterministic/manual fallback 目前沒有適合管理規則 catalog 的 strict public admin endpoint。
   React 可保留原規則編輯、標籤管理與明確標成零寫入的 browser-local deterministic 預覽；未命中時固定顯示
   人工 fallback，不得套用目前編輯規則。正式 Query／Preview／Apply／receipt catalog contract 尚未裁決前，
   「儲存並發布」不可假成功。滿意度調查的「有幫助／未解決」按鈕、指標槽位與未解決轉人工語意必須保留；
   feedback Query／record／receipt contract 完成前只可本機預覽，不得硬編百分比、增加計數或假造客服工單。
   Knowledge raw response 亦不得接入 React。
8. Customer Service reply／handling、Identity replacement／retry／manual completion、identity review、Rich Menu
   publish Preview／Apply 與 notification rule mutation 現均已接入既有 strict 契約；Chrome 逐頁可操作且不顯示
   「後端尚未提供／未開放」。LINE 全流程仍為 `in-progress`：Staff LIFF self-service 的 verified-token browser
   E2E 與 LINE provider sandbox delivery 仍受 token／provider 授權限制；不得因此宣稱「所有 LINE 流程完成」。
   Staff LIFF 請假待辦現已具備 typed Preview／明確確認／Apply／staff-scoped readback；Apply 只建立
   Scheduling-owned `pending` 待辦，不會直接改變正式班表。寶寶日誌與餐食照片仍須補齊 staging cleanup
   與 Preview／Apply 契約後才可解除 UI 鎖定，兩者均不得用既有 direct POST 冒充完整流程。
9. LIFF 卡片工作室固定保留原設計的 8 個 LIFF 與 4 個 Flex 資產。已有 canonical route 的入口必須開啟真實
   LIFF URL，且不得產生 client identity、demo token 或假 provider payload；缺 route／API 的
   `profile_update` 與 Flex typed design preview 應保留完整設計槽位並標示 required contract gap，不能導向
   不存在的頁面，也不能因尚未接線而移除。群組安全頁維持 typed target Query／Preview／Apply／receipt／
   fresh readback，同時保留群組狀態、單一互斥與 Super Admin 重設說明；receipt 已提交與 readback 失敗必須分開顯示。
   legacy `/static/bind.html` 現僅導向 `/line-identity`，不再暴露 direct writer。`profile_update` 的正式
   Query／Preview／Apply／receipt 與 owner DB readback 已由 `20` §6.1 裁決，current implementation／Chrome
   E2E 仍列入代辦；完成前既有頁面維持唯讀與人工補登說明，不呼叫缺失 API，也不以 query-string
   `userId` 建立任何身分或成功事實。

目前驗證：LINE focused React `15 files / 68 tests passed`、successor focused `5 files / 14 tests passed`、
M3 client／workbench `2 files / 7 tests passed`、
TypeScript `noEmit` PASS；真 FastAPI＋development `lu_test_*` Chrome 已逐頁驗證六 tab，0 role alert、
0「後端未提供／尚未納入／未開放／unavailable／mock」，identity revocation Preview 與 Delivery detail
均實際通過。Chrome extension 自身 message-channel warning 不屬 application error；React duplicate-key
已修正並 fresh reload 驗證 0 application console error。Scheduling M3 Chrome 顯示完整 15 種 closed
Preview／Apply 操作；legacy 案件 `115000003` 的 service dates 未確認時顯示補正指引，0 horizontal overflow、
0 application console error，未執行未具完整 root facts 的 Apply。

跨功能 current 待辦只由
[`96_Current_剩餘代辦任務總表.md`](../02_決策與退役執行記錄/96_Current_剩餘代辦任務總表.md)
列管；本節的較早 snapshot、session handoff 與已完成 Work Package 不得重新形成待辦。
