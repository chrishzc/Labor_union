# Order Tracker query page-slice contract matrix

Status: candidate frozen locally; browser evidence awaiting.

| Surface | Authority | Final disposition |
|---|---|---|
| Initial data | completed eight-GET client `getOrderSummaries` | one GET, fresh token, AbortSignal, stale discard |
| Seven stage slots | presentation constants only | seven visible sections, count `—`, typed stage projection unavailable |
| Loaded orders | strict `OrderSummaryItem` | separate `待後端階段投影` region; never assigned to stage |
| Raw status | `order_status` | displayed as `原始訂單狀態（非七階段）` only |
| Dates/days/amount/staff | summary fields | direct formatting; no arithmetic or settlement meaning |
| Phone/address/waiting | no typed summary field | visible unavailable |
| 11 SOP labels | approved presentation labels | 11 visible; status/timestamp null; root-fact lineage unavailable |
| LINE timeline | no case-scoped typed view | one visible unavailable sentinel; no generated row |
| Service completion | Orders owner projection absent | independent unavailable slot |
| Client settlement | Client Finance owner projection absent | independent unavailable slot |
| Staff payroll | Staff Payables owner projection absent | independent unavailable slot |
| Manual replay | mutation out of scope | native disabled, zero request |

Tracker runtime dependency closure has no compatibility stage mapper, fake SOP generator, fake notification generator or non-GET client.

