# HCM Import Result Review browser receipt

Status：`AWAITING_REAL_BROWSER_EVIDENCE`。

不再要求選檔、upload或Preview。待真password→TOTP後驗：單一recent-results GET、legacy或row-outcome Network↔DOM、refresh budget、referral本地導向、其他family disabled與0 non-GET。沒有existing detailed receipt時只能驗empty／legacy，不冒充row membership runtime PASS。

