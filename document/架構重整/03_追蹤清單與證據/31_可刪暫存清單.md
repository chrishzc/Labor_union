# 可刪暫存清單

更新日期：2026-08-04。此清單只盤點可丟棄的測試產物，不授權刪除。

## 刪除規則

- Owner：QA／release cleanup。
- 前提：不再執行使用相同 MySQL test evidence 或 pytest basetemp 的程序；確認沒有
  `pytest`、Python 或防毒掃描持有路徑後才可刪除。
- 排除：production source、`fixtures/db_snapshot_v2/v3`、任何 schema／資料庫、
  `document/架構重整/evidence` 與 Git 追蹤的檔案均不在本清單。
- Windows 若仍回傳 `Access denied`，保持原狀並在程序／防毒解除鎖定後重試；不得用
  強制 ACL 改寫或廣域遞迴刪除處理。

## 工作區 pytest 暫存目錄

| Path | Owner | 理由 | 可刪除條件 |
|---|---|---|---|
| `.pytest_tmp_finance_import_mysql_e2e_20260804` | QA | Finance Import 隔離 MySQL E2E 暫存 | E2E receipt 已保存且無 pytest 使用 |
| `.pytest_tmp_finance_import_mysql_e2e_20260804_run2` | QA | 同上重跑暫存 | 同上 |
| `.pytest_tmp_finance_import_mysql_e2e_20260804_run3` | QA | 同上重跑暫存 | 同上 |
| `.pytest_tmp_finance_import_mysql_e2e_20260804_run4` | QA | 同上重跑暫存 | 同上 |
| `.pytest_tmp_finance_reprocess_guard_20260804` | QA | Historical Reprocess guard 測試暫存 | focused test receipt 已保存且無 pytest 使用 |
| `.pytest_tmp_manual_refund_mysql_e2e_20260804` | QA | 手動一般退款隔離 E2E 暫存 | E2E receipt 已保存且無 pytest 使用 |
| `.pytest_tmp_manual_refund_mysql_e2e_20260804_run2` | QA | 同上重跑暫存 | 同上 |
| `.pytest_tmp_manual_refund_mysql_e2e_20260804_run3` | QA | 同上重跑暫存 | 同上 |
| `.pytest_tmp_reprocess_schema_20260804_run3` | QA | reprocess schema 順序測試暫存 | schema test receipt 已保存且無 pytest 使用 |
| `.pytest_tmp_schema_order_20260804` | QA | schema numeric order 測試暫存 | schema test receipt 已保存且無 pytest 使用 |

## 工作區外 pytest basetemp

| Path | Owner | 理由 | 可刪除條件 |
|---|---|---|---|
| `C:\tmp\labor_union_pytest_full_20260804_escalated` | QA | `830 passed, 5 skipped` 完整 pytest 的可丟棄 basetemp | 完整回歸結果已記入正式矩陣，且沒有 pytest 程序使用 |

## 不在本清單的隔離資源

`labor_union_disposable_mysql_20260804` 容器是正在使用的隔離 MySQL 證據環境；它不是
pytest 暫存目錄，需另行確認清理時機與保留的測試 receipt 後才可處理。
