---
doc_type: contract
---

# G05：服務完成時刻與請假／代班競爭契約

## 現況裁決

`AutoCompleteOrderService`、MySQL repository、typed API 與 immutable receipt 已完成。
`lifecycle_authoritative_facts_loader` 會鎖定 effective scheduling generation、
assignment-owned official service days、`actual_end_date`、service time terms 與
Asia/Taipei evaluation clock，再決定 `auto_complete` 是否可成立。任何 legacy 直接
更新 `orders.status` 的作法都會繞過 version、idempotency、audit、outbox 與
請假／代班的跨 Domain 交易邊界，禁止採用。

2026-08-08 已在 disposable MySQL 8.4 container
`labor_union_g05_mysql_20260808` 的 `lu_test_g05_20260808` schema 實跑以下兩個
test nodes，均通過；未使用 `union_db`：

- `tests/test_order_auto_completion_disposable_mysql_e2e.py::test_auto_completion_first_rejects_the_real_stale_leave_apply_without_writes`
- `tests/test_order_auto_completion_disposable_mysql_e2e.py::test_real_leave_apply_stales_old_completion_then_recomputed_completion_succeeds`

第一個順序先以真實 `LeaveSubstitutionWorkflow` Preview 取得 candidate，再 Apply
AutoComplete；stale Leave Apply 回 `stale_version`，Scheduling、Client Finance、
Payroll、outbox 與 leave outcome snapshot 完全不變。第二個順序先 Apply 真實 leave，
舊 completion 回 `order_version_conflict`，重算後必須等到新 completion instant 才能
完成。2026-08-08 已新增 append-only `labor-union-2026-08-08-v2` release
manifest／owned-object descriptor，並由 release chain 驗證 artifact hash 與 dependency。
automatic trigger 以 `run_order_auto_completion_scheduler.py` 有界掃描 due Orders，寫入同一
durable command queue；`run_durable_job_worker.py` 只經 `AutoCompleteOrderService` 執行。
新增 isolated MySQL durable-worker E2E node 已在同一 `lu_test_g05_20260808` target
重跑通過：due order 只會先 enqueue，再由獨立 canonical worker 寫入完成 receipt／Orders
outbox；服務日不完整時不得 enqueue 或回報成功。fixture 會使用資料庫實際產生的 staff／schedule
identity，並讓 bootstrap、workflow 與 worker 使用同一個 disposable database。

## 正式 command

`AutoCompleteOrderService` 是 Orders Domain 唯一可將服務完成時刻持久化的 command。

- input：`case_no`、expected Orders lifecycle version、evaluation instant（Taipei）、
  idempotency key、actor、reason、correlation id；
- root facts：locked Orders lifecycle envelope、assignment-owned official service days、
  effective scheduling generation、service time terms、active lifecycle controls；
- decision：只有 `evaluation_at >= completion_instant`、正式服務日完整、沒有
  `auto_complete` blocker 且非取消／已完成狀態時，才由 `服務中` 轉為 `訂單完成`；
- persistence：同一 Orders UoW 追加 immutable lifecycle event、樂觀版本更新、
  idempotency receipt 與 post-commit Orders source outbox；不重建或結清 Client Finance、
  Payroll、退款或補助義務；
- replay：同 key、同 command fingerprint 回原 receipt；payload mismatch 為 typed
  idempotency error；expected version／fingerprint 漂移為 typed conflict；
- blockers：時刻未到、服務日不完整、資料矛盾、human hold 或取消均為 typed
  `domain_blocked`，零寫入。

## 與請假／代班的競爭不變量

請假／代班 Apply 與 AutoComplete 都必須先鎖定同一 Orders lifecycle aggregate。

1. 若 AutoComplete 先提交，leave Preview 的 Orders version 已 stale；leave Apply 必須
   typed conflict，且不得產生 scheduling generation、Client Finance、Payroll、outbox 或
   leave outcome 寫入。
2. 若 leave／代班先提交，必須先更新服務日與 Orders lifecycle version；舊 completion
   Preview／command 必須 typed conflict。重新以新的服務日計算的 completion instant 才可
   再判斷是否完成。
3. 兩者不能以「先完成後再補請假」或「自動以舊日期完成」隱藏競爭；人工後續更正須由
   獨立、可稽核的 correction command 處理。

## G05 驗收

隔離 `lu_test_*` MySQL E2E 已涵蓋上述兩個順序，並驗證 locked row、typed error、
idempotency receipt、lifecycle／scheduling／finance／payroll／outbox 的精確 before/after
counts。不得使用 `union_db`、mock transaction 或 legacy `orders.status` writer。新增
`144_order_auto_completion_workflow.sql` 已納入 append-only v2 release manifest／owned-object
descriptor。automatic trigger 已實作為 queued command discovery＋獨立 worker，並已在
`lu_test_g05_20260808` 通過 durable-worker E2E；G05 delivery path 已具可追溯實證。
