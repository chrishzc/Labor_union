---
doc_type: work-package
declared_status: in-progress
date: 2026-08-29
task_id: 97
owner: architecture-governance / domain-owners / integration-writer
---

# 任務 97：架構一致性修復與全域驗收計畫

## 1. 任務邊界與登錄狀態

- **Business scenario**：依 current `Global → Domain → Subsystem → Module` 契約，盤點並修正 active
  mutation、repository-owned commit、route-owned transaction、raw-dict public Query、writer inventory
  與 entry-point governance，最後以 fresh clone 做全域驗收。
- **Current status**：`in-progress`。2026-08-29 使用者暫停 Task 96 並授權先執行 Task 97，使 Task 96 後續
  建立在更準確的架構基線上。本 Authority 涵蓋本 repository 內依 current spec 的程式、測試、tracked
  governance artifact 與受控本機驗證；不涵蓋 `union_db`、production、provider、部署、正式 entry switch、
  不可逆 source removal、Git commit／push 或未另行核准的 schema／migration 與外部效果。
- **Scope**：下列 WP0～WP8、總控指令、依賴順序、write set、測試、停止條件與最終 gate table。
- **Dependencies**：根目錄 `AGENTS.md`、`00_Global_共同契約.md`、
  `15_正式規格索引與裁決總表.md`、對應 Domain 正式規格、
  `19_Global_Entry_Point_Governance.md`，以及 WP0→WP8 內明列的前置關係。
- **Shared write set owner**：同一時間只有一位 integration writer 可修改 `README.md`、
  `15_正式規格索引與裁決總表.md`、entry-point queue、writer inventory 與共用 API schema／router registry。
- **Acceptance**：WP0～WP7 的必要驗收均有 current evidence，WP8 所有一般 gate 均為 `passed`，且適用的
  DB gate 均為 `PASS`，才可輸出
  `ARCHITECTURE_COMPLIANCE_CONFIRMED`；否則固定輸出 `ARCHITECTURE_COMPLIANCE_NOT_CONFIRMED`
  並列出精確 blocker。
- **Tests／evidence**：依各工作包明列的 focused、跨層、full suite、React、DB、Browser／runtime 與 fresh-clone
  驗收。中間輸出放 ignored `scratch/architecture-compliance/`；只有符合專案 durable evidence 規則的 aggregate
  final receipt 才可進 tracked evidence。
- **Close condition**：所有必要 acceptance 與 drift closure 都為 `passed`；完成後依 active 文件 archive gate
  更新索引。Task 96 的需求與 evidence 不被本計畫取代，但在 Task 97 terminal completion 或使用者另行調整
  優先序前維持 `blocked-by-user-priority`。

### 1.1 Current Authority 與規格路由（2026-08-29）

- `D97-PRIORITY`：Task 96 暫停，Task 97 取得目前 execution priority。
- `D97-CONFLICT-PRIORITY`：Task 97 執行中若與其他任務發生語意衝突或同路徑競寫，以 Task 97
  的 latest human Authority、current package write set 與 acceptance 為主。Integration writer 仍必須先辨識雙方
  意圖與依賴，保留與 Task 97 不衝突的使用者／其他 Agent 成果；不得用 reset、checkout、clean、
  stash 或無語意的整檔覆寫來解決競寫。
- `D97-CLIENT-WRITE`：`identity_status` 理論上在正式匯入後不變；不建立通用單欄 mutation。來源錯誤只依
  Case Import／HCM current contract 提交完整修正來源並走既有 Preview／Apply。
- `D97-CLIENT-QUERY`：`GET /api/v1/clients` 不得繼續回傳完整 `clients` table 或
  `List[Dict[str, Any]]`。先完成 exact caller inventory；保留的需求必須改由 bounded typed
  Client Query 或實際 consumer 的 owner projection 承接。
- `D97-CLIENT-DEFER`：`superseded-by-D97-CLIENT-RETIREMENT-2026-08-29`。缺少 caller evidence 不再允許
  raw GET 或 direct-mutation PUT 繼續作為正式架構；它只決定舊 entry 是直接刪除，還是先回 typed
  `410 Gone`。
- `spec_route`：`SPEC_REUSED`。控制規格為 `00_Global_共同契約.md`、
  `15_正式規格索引與裁決總表.md`、對應 Domain 規格與 `19_Global_Entry_Point_Governance.md`；上述三項
  使用者裁決補足 Task 97 原本的 priority 與 Client entry 缺口。

```yaml
convergence:
  status: READY
  blockers: []
```

Living package route：`PACKAGE_REUSED`。本文件是唯一 Task 97 umbrella package；WP0 只凍結 evidence，
後續修改必須按 owner、write set、transaction boundary 與直接 oracle 切成有界 execution slice，不把整個
repository-wide inventory 當成單一 mutation package。

### 1.2 2026-08-29 七項正式裁決與 execution gate

下列裁決是 latest human Authority，補足並取代本文中與其衝突的舊延後、候選或 spec-gap 敘述。
它們授權 repository 內依本 package 的程式、測試與 governance artifact 修正；不授權 production／
`union_db`、external provider、deployment、正式 entry switch、不可逆 source removal 或未通過 DB gate 的 schema apply。

| Decision | 正式契約 | Execution gate |
|---|---|---|
| `D97-CLIENT-RETIREMENT` | Clients raw-dict GET 與 route-owned SQL PUT 必須退役。GET 由 bounded typed Client Query 或 consumer-owned projection 承接；PUT 只能由 Client owner 的 typed Query → Preview → Confirm → Apply → receipt → fresh readback 承接。Route、repository、adapter 皆不得 commit／rollback，不建 generic root editor、raw mutation 或永久 compatibility wrapper。 | 先建 exact caller inventory，涵蓋 runtime registration、React／Streamlit／scripts／workers／CLI／runbook／deployment／external evidence。已證明只有內部 caller 且全數遷移才刪除；external 或 unknown 則停用舊 mutation 並回 typed 410，需穩定 error code、replacement identifier、移除 gate 與退役條件。無法推導期限時標 `blocked_external_evidence`，不自訂日期。 |
| `D97-MEDIA-STAGING` | Media 採 reference-aware immutable staging，digest＋idempotency identity 使 replay 重用同一內容；DB outer transaction 建 metadata、business reference 與 post-commit intent，worker 於 commit 後將已被引用的 staging object 提升為 available，reconciler 補做 finalize。不把 filesystem 與 MySQL 假裝為 distributed transaction，DB commit 失敗後不立即刪 object。 | GC 只可刪除超過 grace period、DB 零 reference 且沒有 active lease 的 staging object；必須 reference-aware、idempotent、bounded、dry-run 並產生 receipt。Metadata 存在但 bytes 缺失回 typed repair／blocker。沒有新 TTL 裁決時沿用可設定的 24 小時 operational default，不升格為 business rule；不建永久 media event history。 |
| `D97-HCM-BECLASS-UOW` | Case Import 擁有 source、review、accepted mapping 與 reconciliation；Orders 擁有 `requires_cooking` 等條款。跨域 outer transaction 由 `CaseImportReconciliationApplication` 或等價 Application／Subsystem composition 擁有，Orders mutation 只經 typed Orders command。 | `apply_in_current_uow` 可保留；infrastructure adapter 只借用 caller connection／UoW，standalone UoW wrapper 移到 Application／Subsystem。驗收同一 transaction 成功、任一步 rollback、replay 不重複 mutation、stale owner version 零寫入，adapter／repository 零 commit／rollback。 |
| `D97-ANOMALY-DELIVERY-UOW` | Anomaly 只保存仍成立的 canonical current projection；outbox／retry／claim／delivery 由通用 durable job 或 owner outbox 擁有。Owner mutation 與 bounded recheck intent 同 owner transaction；worker claim／lease 是獨立短 transaction；projection reconcile 與 intent complete 是同一 Application-owned transaction；provider call 在 DB transaction 之外，結果以通用 delivery mechanism 完成。 | Transaction contract 必須同時阻止「projection 成功但 intent 未完成」與「intent 完成但 projection 未成功」。Replay idempotent，predicate 消失直接刪 current row；maintenance 只做 bounded owner-fact recheck。不建 anomaly-specific occurrence、delivery history、claim／resolve 或永久 checkpoint。 |
| `D97-SERVICE-DAY-OWNER` | Scheduling 維持 Service Day Log 唯一 owner，擁有 log aggregate、附件關聯、completion fact、versioned receipt 與 committed outbox。Staff Operations 只擁有 actor／入口／staff-scoped query；LINE Integration 只擁有身分驗證、受控 media 傳輸與 notification delivery。 | 所有相關 Domain、Subsystem、repository、table 與 event 標為 `keep-owner: Scheduling`。Route 名稱或操作人是月嫂不改變 root owner；沒有新 T3 裁決不得重開 owner。 |
| `D97-PRODUCTION-SCRIPT-GOVERNANCE` | 逐支 exact inventory，terminal disposition 只能為 `keep-operator-only`、`rewrite-to-canonical-runner`、`delete-executable`、`test-only`、`blocked-caller-evidence`。繞過 Domain／Application writer 的 import script 必須改接 typed application，否則退役；一次性 migration 關閉後只保留 immutable artifact、descriptor 與 receipt。 | 保留的 production-capable operator script 必須預設 dry-run、explicit target DB、configured／connected／host 三方相符、schema fingerprint／plan drift、prior dry-run receipt、destructive backup receipt、explicit apply 與 exact confirmation、verify、resume／replay、terminal receipt、production authority／credential gate；不得預設 `union_db`或 production DB。 |
| `D97-ENTRY-TERMINAL-GOVERNANCE` | 先重建 fresh-clone 可執行 inventory，再治理 current 151 `review_required` 與 34 generic placeholders。每個 exact entry 的 terminal disposition 只能為 `active_canonical`、`rewrite_to_canonical`、`retired_410`、`delete`、`operator_only_guarded`、`blocked_external_evidence`。 | 每列必須有 exact identity、source path、runtime registration、inbound callers、external／operator evidence、owner、replacement symbol或 `none`、replacement readback、deletion／410 gate、focused regression、zero-reference oracle 與 terminal receipt。34 個 placeholder 必須展開或證明無真實 entry 後刪除，terminal 統計必須為 0 generic placeholder。Static caller 為零不足以刪 public HTTP；external／unknown 先 typed 410，也不建永久 410、alias 或無期 defer。 |

#### Dependency 與局部停止規則

1. 先完成 decision 回寫與 current drift scan，再以 exact owner、caller、write set 及 oracle 切成小 package。
2. Clients 與 production scripts caller evidence 優先；HCM／BeClass transaction owner 與 Service Day owner 次之，
   Media、Anomaly、entry governance 依序執行，最後才跑 repository-wide scans 與 full acceptance。
3. 單一 entry 缺 external／operator evidence 只讓該列成為 `blocked_external_evidence`，不阻擋其他已有
   唯一契約且 write set 不重疊的 package。
4. 已完成 package 只驗證 drift；沒有 drift 不重做。Shared artifact 只由 Task 97 integration writer 收旂。
5. 沒有通過必要 acceptance 前，Task 97 整體固定為 `ARCHITECTURE_COMPLIANCE_NOT_CONFIRMED`。

## 2. 完整執行計畫

### 總體執行順序

```text
WP0 基線與 inventory
          ↓
Authority／scope gate
          ↓
WP1 Clients entry ─┐
WP2 Repository commit ─┴→ WP3 Route transaction
                               ↓
                         WP4 Typed Query
                               ↓
WP5 Writer Inventory
          ↓
WP6 Entry Queue 初次重建
          ↓
WP7 逐項治理裁決
          ↓
WP6B 再生 Queue／更新文件
          ↓
WP8 Fresh-clone 全域驗收
```

建議一次只讓一個 integration writer 修改：

- `README.md`
- `15_正式規格索引與裁決總表.md`
- entry-point queue
- writer inventory
- 共用 API schema／router registry

WP1 與 WP4 的 `clients.py`／Client schema，以及 WP3 與 WP4 重疊的 route 不得平行修改；同一路徑固定由
單一 writer 依上圖序列處理。只有 WP0 凍結後證明 owner、write set 與 shared hot spots 都不重疊的 bounded
slice 才可並行。

## 3. Agent 總控指令

把下面這段放在八個工作包之前：

你正在修正 Labor_union repository 的架構一致性。

開始前必須：

1. 記錄 branch、HEAD、`git status --short`。
2. 完整讀取 `AGENTS.md`。
3. 讀取：
   - `README.md`
   - `document/架構重整/00_開發者與Agent導覽.md`
   - `document/架構重整/00_Agent任務分級與交付規範.md`
   - `document/架構重整/01_規格基線/00_Global_共同契約.md`
   - `document/架構重整/01_規格基線/15_正式規格索引與裁決總表.md`
   - 對應 Domain 正式規格
   - `19_Global_Entry_Point_Governance.md`
4. 保留所有既有 dirty、untracked、ignored 檔案。
5. 不得 reset、clean、stash、切換 branch、建立 worktree。
6. 不得操作 `union_db`、production、provider、部署或未授權 migration。
7. 不得因名稱含 commit、Dict、legacy 就機械刪除；先判斷實際責任。
8. 不得自行修改 public contract、owner、SSOT、交易邊界或外部副作用。
9. 若正式規格沒有回答 owner、command、write set 或狀態轉移，停止實作並回報 `AUTHORITY_REQUIRED`。
10. 所有 mutation 必須符合：
    Query 唯讀；
    Preview 零寫入；
    Apply fresh-read、lock、expected version、fingerprint；
    一個 outer Unit of Work；
    idempotency receipt；
    必要時 outbox；
    repository 不自行 commit。
11. 先 focused tests，再執行跨層與 full suite。
12. 不得自行 commit、push 或建立 PR。

每個工作包交付：

- 修改檔案
- 行為差異
- owner／SSOT／UoW 說明
- focused tests
- 未執行項目
- live-drift
- blocker
- `git diff --check`

## 4. WP0：建立可信基線與精確 Inventory

### 目標

先得到可重現的現況清單，不直接修改 production code。

### 執行內容

建立本次任務的 ignored 工作目錄：

`scratch/architecture-compliance/`

產生四份 inventory：

1. active mutation entry
2. repository／infrastructure commit
3. route-owned transaction
4. raw-dict API contract

每筆至少記錄：

- identity
- source path
- symbol
- HTTP method／route
- entry status
- canonical owner
- mutation／query
- transaction owner
- commit location
- response model
- authentication dependency
- replacement
- static caller
- runtime caller
- proposed disposition

### 掃描指令

```text
rg -n '\.commit\(' api domains infrastructure line scripts services subsystems ui --glob '*.py'
rg -n 'BaseResponse.*(Dict|dict)|response_model=.*(Dict|dict)' api --glob '*.py'
rg -n 'cursor\.execute|executemany|UPDATE |INSERT |DELETE ' api/routes --glob '*.py'
rg -n 'Depends\(require_|AdminPrincipal|SystemActor' api/routes --glob '*.py'
```

再將結果和：

- `entrypoint_review_queue_v1.jsonl`
- `config/production_writer_inventory.v1.json`
- `api/main.py`

交叉比對。

數量一律標記為 `OBSERVED_VALUE`，綁定掃描時的 branch、HEAD、worktree 狀態與 generator revision；不得把
「130 entries」或任一候選總數升格為跨 execution 的固定產品需求。`rg` 只提供人工 discovery evidence，
正式 writer denominator 由 current scanner roots 與 stable identity 產生器決定。

### 驗收

- 每個候選都有唯一 identity。
- 區分真正違規與合法 UoW／worker／maintenance boundary。
- 不修改 production code。
- 後續 WP1～WP4 只能處理 inventory 內已分類項目。

### 2026-08-29 執行狀態

- Discovery generation：`passed`。四份 ignored inventory 均具唯一 identity 與完整 required fields；依
  2026-08-29 current candidate 重跑後的 observed counts 為 active mutation 283、repository commit 397、
  route transaction 7、raw-dict API 55。
- Semantic classification：`in-progress`。397 個 commit candidates 仍須按 outer UoW／worker／migration／
  hidden repository commit 逐項裁決；AST receiver heuristic 只作 review routing，不是最終違規裁決。
- Entry drift：`failed`。tracked queue 為 664 entries／130 `review_required`；current discovery 為
  685 entries／151 `review_required`，tracked queue 缺 21 筆，另有 34 筆 active placeholder records。
- Writer baseline：`passed`。current baseline validator 為 1056 findings，scan fingerprint
  `a9f09114a590cb3bf37a96fbee3de0ac43fbae22607b78648564e8497be2c41f`；這只證明 v1 baseline 與 current
  scanner 一致，不代表 v3 disposition 已完成。
- Client caller evidence：repository／文件內沒有發現 PUT 或 GET 的 static caller，queue 亦未記錄 runtime
  caller；這不構成 retirement Authority。external／operator caller 仍待取得，未完成前不修改兩個 entry。
- Client entry handling：`required_later`。已確認 GET 曾是 Form Management 由 UI 直連 DB 遷移至 API 的
  過渡入口，該 repository caller 後來已改用 bounded Orders queries；PUT 只找到 route docstring 與加入歷史，
  未找到專用需求或 caller。依 `D97-CLIENT-DEFER`，先保留現況並繼續不重疊工作包。
- WP0 沒有修改 production code。完整一次性 evidence 位於 ignored `scratch/architecture-compliance/`。

## 5. WP1：退役 `clients.py` 直接 Mutation 並裁決 Query

### 優先度

P0。

### 已知問題

目前 active route：

`PUT /api/v1/clients/{client_id}/identity-status`

直接：

- 取得 connection
- 執行 `UPDATE clients`
- route 自行 commit
- 沒有 authentication dependency
- 沒有 Preview
- 沒有 expected version
- 沒有 idempotency
- 沒有 audit／receipt／outbox

route request enum 只有「一般市民／補助市民／非市民」，但 current Case Import source vocabulary 另含
「低收入戶／中低收入戶」，再由 Finance／Payroll policy 映射為補助政策；來源字彙與衍生政策不可混為
同一個 mutation enum。因本次已裁決不保留通用 mutation，不另擴張這個 legacy request schema。

### 前置裁決

2026-08-29 使用者已裁決：`identity_status` 理論上在正式匯入後不再變更，不建立通用單欄 mutation。
Client Finance 只消費已採納身分與其政策映射，不擁有來源身分；來源錯誤只依 Case Import／HCM 規格提交
完整修正來源。不得因資料位於 `clients` table 就認定 route、Client Finance 或新建 Client Profile owner
可以直接修改。

舊 PUT 的 caller、實際用途與相容影響仍須先盤點。只有 caller inventory、exact replacement、typed 410
regression 與 retirement decision 都完整時才可改為 `410`；本機 source retirement 不等於 production entry
switch。不存在合法 replacement 時維持 fail closed 並回 `AUTHORITY_REQUIRED`，不得填 `null` 冒充完成。

### 唯一允許的修正 successor

HCM 來源欄位錯誤沿用既有 `POST /api/v1/case-import/hcm/resubmissions/preview` 與
`POST /api/v1/case-import/hcm/resubmissions/apply`。它們只處理 warning-bound、完整修正來源，不是一般
Client profile editor，也不得被包裝成任意單欄 API。

### 舊直接 Mutation 的退出候選

將舊 PUT route 固定回：

```text
410 Gone
code: client_identity_status_direct_update_retired
replacement: 明確且已驗證的 Case Import correction entry
```

不得留下部分可用的直接更新。

### 先盤點 Query，再裁決 disposition

`GET /api/v1/clients` 不可繼續使用全表：

`List[Dict[str, Any]]`

不得先假定這個 endpoint 必須保留。先盤點 external／operator caller、業務情境、欄位消費、認證、資料量、
替代 route 與 retirement 影響；沒有 repository static caller 不能單獨證明可退役。

若 caller inventory 證明仍有合法 Client directory consumer，才建立 bounded、paginated、server-owned typed
Query，例如：

- `ClientSummaryView`
- `ClientListPageView`
- `ClientIdentityStatusView`

不得回 raw survey、secret、未授權個資或 persistence metadata。

### 建議 Write Set

- `api/routes/clients.py`
- `api/schemas/clients.py`
- `domains/<owner>/`
- `subsystems/<owner>/`
- `infrastructure/mysql/<owner>_repository.py`
- `tests/test_client_*.py`
- entrypoint queue（先不要由一般 writer 手改）

### 驗收

- caller inventory 明列 static、runtime、external／operator unknown 與 replacement impact；沒有 static caller
  不被當成 retirement 的唯一依據。
- direct PUT 不再 import `mysql_adapter`，route 中不存在 `UPDATE clients`、`commit()`、`rollback()`。
- direct PUT 只在 exact replacement 與 retirement evidence 完整後回 typed `410`；不存在 replacement 時
  固定 `blocked`，不得保留部分可用 mutation 或填 `null`。
- HCM source correction 仍只走既有 warning-bound whole-source Preview／Apply；Task 97 不建立新的
  `Query／Preview／ApplyClientIdentityStatusChange`。
- `GET /api/v1/clients` 依 caller inventory 裁決：保留時必須 bounded、paginated、authenticated、typed；
  退役時必須具 replacement、caller regression 與 entry governance evidence。
- source vocabulary、policy normalization、nullable／unavailable 與 historical unknown 語意分開；不得讓
  typed Query 因合法 legacy readback 直接 schema-fail。
- direct PUT retirement、HCM successor 不受影響，以及 GET final disposition 均有 focused tests。

## 6. WP2：移除 Repository-owned Commit

### 優先度

P0／P1。

### 原則

不是刪除所有 `commit()`。

以下可以保留：

- 明確的 `UnitOfWork.commit()`
- migration／cutover transaction
- worker 自己擁有的獨立 outer UoW
- 專案正式規格明確指定的 maintenance boundary

以下必須移除：

- repository `save()`、`claim()`、`append()` 內自行 commit
- repository 暴露 `commit()` 讓 workflow 呼叫
- anomaly source 在 caller 不知情時 commit
- 同一業務 command 中途 commit claim，之後再 commit result

### 第一批必查檔案

- `infrastructure/mysql/service_date_confirmation_repository.py`
- `infrastructure/mysql/hcm_workbook_import_repository.py`
- `infrastructure/mysql/historical_order_workbook_import_repository.py`
- `infrastructure/mysql/staff_historical_workbook_repository.py`
- `infrastructure/mysql/matching_schedule_confirmation_repository.py`
- `infrastructure/mysql/admin_command_repository.py`
- `infrastructure/mysql/background_job_repository.py`
- `infrastructure/mysql/knowledge_retrieval_repository.py`
- 所有 `super().commit()`、`connection.commit()`、`self._connection.commit()` 候選

### 每個 Commit 的分類

- `allowed_outer_uow`
- `allowed_worker_transaction`
- `allowed_migration_transaction`
- `hidden_repository_commit`
- `split_transaction_bug`
- `unknown_authority`

只有 `hidden_repository_commit` 與 `split_transaction_bug` 直接施工。

### 重構方式

Repository 改成只提供：

- `load`
- `load_for_update`
- `append`
- `save`
- `compare_and_swap`
- `save_receipt`
- `append_outbox`

commit／rollback 由外層 application composition 或 UoW 負責。

禁止把 commit 從 repository 移到 FastAPI route；必須移到真正的 outer UoW owner。

### 特別案例：HCM claim

目前 `claim()` 與 `save_receipt()` 分別 commit，必須判斷：

- claim 是否應與整個 command 同交易；
- 或是否為合法 durable command-claim transaction。

若為 durable claim，必須具有：

- 明確 command state machine
- crash recovery
- resume contract
- outcome query
- 不把 claim 冒充 business success

不能只因需要避免重複就保留中途 commit。

### 驗收

- production repository 不再自行決定業務 commit。
- 每個 mutation 只有一個 outer UoW owner。
- rollback 不留下 receipt／outbox／projection partial state。
- replay、crash-resume、stale、exception paths 均有測試。
- 新增靜態測試禁止 repository-owned commit，對允許清單 fail closed。

## 7. WP3：把 Route-owned Transaction 移到 Application／Subsystem

### 前置依賴

WP1、WP2 的 transaction ownership 已確定。

### 第一批候選

- `api/routes/clients.py`
- `api/routes/holidays.py`
- `api/routes/runtime_health.py`
- `api/routes/staff_leave_intake.py`
- `api/routes/staff_leave_management.py`
- `api/routes/staff_service_day_logs.py`
- `api/routes/staff_service_day_media.py`

### 執行方式

每條 route 先建立責任表：

| 欄位 | 說明 |
|---|---|
| Route | HTTP entry |
| Owning subsystem | 真正 Application owner |
| Root facts | 被鎖定的根事實 |
| Write set | 實際寫入 |
| Outer UoW | 唯一 commit owner |
| External side effect | 是否存在 |
| Receipt | terminal／job receipt |
| Replay | exact replay 行為 |

重構後 route 不得：

- 建立 concrete MySQL connection 來做 mutation
- `commit()`／`rollback()`
- 直接組 SQL
- 直接呼叫 provider
- 自行重算 Domain candidate

route 應依賴明確的 application composition：

```python
application = Depends(get_xxx_application)
result = application.apply(command)
```

### Media 特別規則

`staff_service_day_media.py` 目前存在：

先寫 filesystem object，再寫 DB metadata。

必須明確處理：

- staging
- digest
- metadata transaction
- orphan cleanup
- replay
- storage failure
- DB failure after object write
- reconciliation

不得假裝 filesystem 與 MySQL 是原子交易。

### 驗收

- `api/routes/` mutation route 不再出現 transaction commit。
- 每條 Apply 由 application owner 管理 UoW。
- typed error 經全域 error boundary 回傳。
- provider／filesystem failure 不留下假的 Domain success。
- `202` 只表示 `JobAccepted`，不代表正式 mutation 完成。

## 8. WP4：將 Active Raw-dict Query 換成 Bounded Typed Views

### 原則

不要機械取代所有 dict。

允許保留：

- 真正 opaque metadata，且有 closed schema wrapper
- internal health map，且欄位固定
- `410` response
- 不穿透 UI 的 adapter-local dict

必須重構：

- active public／admin API
- React 正在使用
- 直接反映 DB row
- 包含 Domain status、金額、日期、identity 或 version
- mutation Preview／Apply candidate
- raw `SELECT *` 結果

### 第一批高風險

- `api/routes/clients.py`
- `api/routes/client_payments.py`
- `api/routes/staff_payments.py`
- `api/routes/contract_signing.py`
- `api/routes/matches.py`
- `api/routes/match_records.py`
- `api/routes/orders.py`
- `api/routes/staff_leave_management.py`
- `api/routes/line_configurations.py`
- `api/routes/line_notification_rules.py`
- `api/routes/data_browser_admin.py`

### 每個 Endpoint 建立

- Pydantic public view
- bounded list／page view
- strict enum
- decimal-string version（BIGINT 不得進 JavaScript number）
- masked／unmasked policy
- typed unavailable／error
- adapter validation
- React client schema

成功 response 不得直接使用 persistence dict。

例如：

```python
class ClientSummaryView(BaseModel):
    client_id: int
    display_name: str
    identity_status: ClientIdentityStatus
    city: str | None
    updated_version: str
```

### 分批方式

可按 bounded domain 平行，但 write set 不得重疊：

- Orders／Contract
- Scheduling／Matching
- Client Finance
- Staff Payables
- LINE
- Access／Admin
- Reporting

`BaseResponse`、共用 schema、React shared client 只能由 integration writer 修改。

### 驗收

- active endpoint 沒有 `Dict[str, Any]` 穿透至 React。
- `SELECT *` 不直接形成 public response。
- schema drift 回 typed client error。
- optional／nullable／unavailable 語意分開。
- 前端不依中文字串重算狀態。
- BIGINT version 使用 decimal string 或其他 lossless contract。
- focused Python、React tests、TypeScript build 通過。

## 9. WP5：重建 Fresh-clone 可執行 Writer Inventory

### 問題

目前 writer v3 validator 依賴 repository 中不存在的四份 tracked artifact：

- `writer_inventory_v3_disposition.manifest.json`
- `writer_inventory_v3_candidate.findings.jsonl`
- `writer_inventory_v3_candidate.manifest.json`
- `writer_inventory_v3_disposition.records.jsonl`

fresh clone 會直接失敗。

### 目標

建立完全 tracked、可重現、無個人檔案依賴的 writer governance。

### Canonical 位置

```text
document/架構重整/03_追蹤清單與證據/evidence/writer_inventory_v3/
  writer_inventory_v3_candidate.findings.jsonl
  writer_inventory_v3_candidate.manifest.json
  writer_inventory_v3_disposition.records.jsonl
  writer_inventory_v3_disposition.manifest.json
```

沿用 current generator、validator 與 tests 已共同引用的位置與檔名，不另建 competing `config/` identity；
artifact 必須 tracked，不能依賴 ignored 或個人 evidence。

### 執行步驟

1. 以 `config/production_writer_inventory.v1.json` 作掃描規則來源。
2. 重新掃描目前 HEAD，不沿用歷史 201 清單。
3. 每個 finding 產生 stable identity。
4. disposition 必須逐筆覆蓋。
5. `final_disposition` 合法值沿用 current validator：
   - `retain_canonical`
   - `retain_restricted`
   - `migrate_then_remove`
   - `gone`
   - `needs_decision`
6. `allowed_outer_uow`、`allowed_worker_transaction`、`allowed_migration_transaction`、
   `false_positive_read`、`route_owned_mutation`、`hidden_repository_commit` 等是 finding／transaction
   classification，保存在 WP0 inventory／candidate evidence，不得混入 current validator 的
   `final_disposition` 或擴張 disposition record exact field set。
7. disposition record 沿用 current exact fields：`identity`、`fingerprint`、`owner`、
   `transaction_boundary`、`runtime_caller`、`replacement_evidence`、`final_disposition`、
   `approved_to_remove`。tables／roots 與 test evidence 由 candidate／linked evidence 保存，不遺失但不建立
   第二套 competing record schema。
8. 未分類 finding 使 validator fail。
9. candidate、records、manifest hash 必須互相綁定。
10. fresh clone 不需要私人 overlay、歷史 receipt 或本機 DB 即可執行 static validation。

### 驗收

```text
python scripts/generate_writer_inventory_v3_candidate.py
python scripts/validate_writer_inventory_v3_candidate.py
python scripts/validate_writer_inventory_v3_dispositions.py
```

全部通過，而且：

- candidate identities = disposition identities
- 不存在 unknown finding
- required scope 不存在未裁決的 `needs_decision`；若仍存在則整體 writer gate 為 `blocked`
- 修改 production writer 後，未更新 inventory 會 fail
- repository-owned commit 與 route SQL 有明確 guard

## 10. WP6：重新生成 Entry-point Queue 並修正文件

### WP6A：先重建真實 Queue

執行：

```text
python scripts/generate_entrypoint_review_queue.py
```

不得手工調整產生結果來符合 README。

驗證：

```text
python -m pytest tests/test_entrypoint_review_queue.py
```

輸出真實統計：

- active
- review_required
- operator_only
- retired_410
- removed

README 必須先如實呈現當前數字，不能繼續宣稱：

```text
348 entries
0 review_required
```

### WP6B：等待 WP7 完成後再更新最終文件

唯一 integration writer 更新：

- `README.md`
- `15_正式規格索引與裁決總表.md`
- `19_Global_Entry_Point_Governance.md`（只有契約真的改變時）
- `entrypoint_review_queue_v1.jsonl`

### 驗收

- Queue 與 AST／runtime discovery 完全一致。
- README 數字由機器產生，不手填猜測。
- 正式索引不宣稱尚未完成的 runtime／Browser／DB gate 已通過。
- route removed／410／active 與程式現況一致。
- 同一 entry 沒有兩個 owner。

## 11. WP7：完成全部未裁決與弱裁決 Entry

### 原則

這不是把某個 observed count 批次改成 active。2026-08-29 基線觀察為 664 entries：439 `active`、
75 `operator_only`、20 `retired_410`、130 `review_required`；另有 34 筆 `active` 使用
`owning bounded domain`／`已確認的 typed application API or UI workflow`／
`authenticated administrator or authorized user` 占位內容。這些數字是 `OBSERVED_VALUE`，WP6A 再生後
以 current queue 為準；至少必須覆蓋所有 `review_required` 與所有 placeholder／generic reviewed entry。

每個 entry 必須逐項回答：

- business scenario
- operator
- canonical owner
- root facts
- query or mutation
- authentication／authorization
- input schema
- output schema
- transaction boundary
- external side effect
- idempotency
- replacement
- runtime caller
- focused regression
- final disposition

### 合法裁決

- `active`
- `retired_410`
- `operator_only`
- `removed`

找不到正式 owner 或 contract 時維持：

`review_required`

並建立明確 spec gap，不能假裝完成。

`tests/test_entrypoint_review_queue.py` 必須真正拒絕所有殘留 `review_required` 與 placeholder owner／scenario／
operator；不得只核對 React entry 子集合而讓 API／CLI 未裁決項通過。

### 分組順序

#### 7.1 P0 Mutation

優先處理：

- Access login／session
- LINE identity review
- Customer Service mutation
- Matching Coordination Apply
- Contract Signing mutation
- Controlled Files mutation
- anomaly recovery mutation
- runtime target mutation

#### 7.2 P0／P1 Query

接著處理：

- receipt query
- storage download
- dead-letter query
- financial recovery query
- Rich Menu current draft
- HCM result query

#### 7.3 CLI

區分：

- developer launcher
- operator-only
- migration
- validation-only
- retired script

#### 7.4 React Anchors

每個 React page 必須對應：

- canonical route
- authenticated shell
- typed API client
- owner Domain
- loading／empty／unavailable／error
- no fake mutation
- no raw dict
- responsive／accessibility baseline

### 每組交付

- entry IDs
- before status
- final status
- owner
- evidence
- code changes
- tests
- remaining blockers

### 停止條件

遇到下列任一項不得標 active：

- owner 不明
- public contract 不明
- mutation 沒有 UoW
- external target 不明
- auth 未完成
- raw dict 穿透
- replacement 不存在
- runtime caller 未盤點
- Browser／DB gate 是 completion requirement 但尚未執行

## 12. WP8：全域架構驗收

只有 WP1～WP7 完成後執行。

### 8.1 Fresh Clone

在新的乾淨 clone 驗證：

- 不依賴 `.agents/`
- 不依賴 ignored evidence
- 不依賴個人 `.env`
- static tests 不需本機正式 DB
- 不需私人 plugin／skill

### 8.2 Dependency Scan

必須證明：

- domains 不 import FastAPI、Streamlit、PyMySQL、API、Infrastructure
- subsystems 不 import FastAPI、Streamlit、UI
- React 不直接連 DB
- API route 不直接寫 Domain root
- repository 不 hidden commit

建立機器可執行測試，不能只靠人工 `rg`。

### 8.3 Writer Scan

重新產生 writer candidate，要求：

```text
unknown = 0
route-owned mutation = 0
hidden repository commit = 0
unowned root writer = 0
retired writer caller = 0
```

合法 UoW、worker、migration writer 必須全部有 allowlisted owner 與測試。

### 8.4 Entry-point Scan

要求：

```text
discovery = review queue
review_required = 0
removed path unreachable
retired path = typed 410
operator-only path 不掛入一般 UI
```

若仍有 Authority gap，最終結論不得寫完全符合。

### 8.5 測試順序

1. Module
2. Subsystem
3. Domain
4. Global
5. API
6. React
7. Migration static gates
8. Fresh bootstrap（若本輪涉及 schema）
9. Preserve-data candidate（若本輪涉及 schema）
10. Browser（若 completion contract 要求）
11. Full Python suite
12. Full React suite
13. Build／lint

Windows 正式命令：

```powershell
.\.venv\Scripts\python.exe -m pytest -W error
cd ui_react
npm test
npm run build
npm run lint
```

每個 pytest 使用唯一：

`--basetemp .pytest_tmp/<task-slug>`

### 8.6 最終驗收表

Agent 必須輸出：

| Gate | 結果 |
|---|---|
| Domain dependency | passed／failed／blocked／not_run |
| Subsystem dependency | passed／failed／blocked／not_run |
| Route transaction | passed／failed／blocked／not_run |
| Repository commit | passed／failed／blocked／not_run |
| Typed API | passed／failed／blocked／not_run |
| Writer inventory | passed／failed／blocked／not_run |
| Entry governance | passed／failed／blocked／not_run |
| Legacy retirement | passed／failed／blocked／not_run |
| Python tests | passed／failed／blocked／not_run |
| React tests/build | passed／failed／blocked／not_run |
| DB gates | PASS／BLOCKED／NOT_RUN |
| Browser/runtime | passed／failed／blocked／not_run |

只有全部必要一般 gate 為 `passed`，且適用的 DB gates 為 `PASS`，才可以下結論：

`ARCHITECTURE_COMPLIANCE_CONFIRMED`

否則固定輸出：

`ARCHITECTURE_COMPLIANCE_NOT_CONFIRMED`

並列出精確 blocker，不得把部分測試成功外推成整體完成。

## 13. 2026-08-29 current execution checkpoint

本節只記錄目前 integrated candidate 的 current evidence，不把部分通過外推為 Task 97 完成。
`13.1`～`13.3` 是同日較早 checkpoint；數字與 gate 結論若和 `13.4` 不同，以 `13.4` 的 final-candidate
重掃為準，不得用較早 snapshot 覆蓋現況。

### 13.1 已完成且可重播的部分

- WP0 discovery：`passed`。Clients retirement 後 current ignored inventory 為 active mutation 282、
  repository commit 397、route transaction 6、raw-dict API 54；identity 均唯一。現存 6 筆
  route transaction candidates 均為唯讀 SQL query，Clients route 已無 SQL、connection、commit 或 rollback。
- WP2 bounded transaction slices：`passed`。Service Date、Matching Schedule、Admin Command callers、durable
  Finance Import job、HCM／historical order／staff historical workbook repositories 已移除 hidden commit 或改由
  application outer UoW 持有；`tests/test_task97_transaction_boundaries.py` 對已收斂路徑 fail closed。
- WP3 bounded route slices：`passed`。Holiday、Staff Leave intake／management、Service Day Log 與 Service Day
  Media route 已改由 application composition 管理 transaction；route 不再自行 commit／rollback。
- WP1 Clients retirement：`passed`。FastAPI runtime 仍掛載兩條 entry，但 repo 內未找到
  React、Streamlit、script、worker、CLI、runbook 或 deployment caller；因 external caller evidence
  仍未知，GET 與 PUT 均依 `D97-CLIENT-RETIREMENT` 回傳 typed `410 Gone`。GET 指向
  bounded Data Browser owner projection；PUT 只指向 HCM whole-source resubmission Preview／Apply。
  queue 兩列的 deletion gate 均為 `blocked_external_evidence`，未自訂移除日期。
- WP2 HCM／BeClass bounded transaction slice：`passed`。Infrastructure standalone UoW wrapper 已移除，
  `CaseImportReconciliationApplication` 擁有單一 outer UoW，adapter 只借用 caller connection；
  focused tests 覆蓋成功 commit、失敗 rollback、replay 不重複 Orders mutation 與 adapter 無 hidden commit。
  舊 HCM Current import 的 claim／root／receipt 分割 transaction 仍列 production-script rewrite blocker，
  不將 bounded slice 外推成全 HCM import 完成。
- Service Day owner zero-drift 標記：`passed`。`service_day_*` repositories 與
  `scheduling_checkpoint_notification_source_repository.py` 已在 writer owner pattern 明確歸 Scheduling，
  Staff Operations pattern 不再把 Service Day root 納入。附件實作缺口另屬 Media package，不重開 owner。
- WP4 bounded typed slices：`passed`。Staff Leave management、LINE configuration、LINE notification rules 與
  Contract Signing 成功 payload 已改為 explicit Pydantic views。三條 Luna high isolated writer lane 的原始
  effect reconciler 因把 pytest cache 及 workflow 自己的 scratch plan／results 視為 undeclared effect 而
  fail closed；未冒充 DDH success。其 current candidate 另由兩條 read-only reviewer／verifier 序列重新驗證，
  terminal receipt 為 `passed`、`ready_for_integration: true`。
- WP5 static artifacts：`passed`。tracked writer v3 candidate／disposition 四份 artifact 已存在；candidate
  validator 為 1573 findings、unresolved 0，disposition validator 為 1573 records，v1 production writer
  baseline 為 1056 findings。
- WP6A：`passed`。Clients disposition 寫回並機械重建 tracked queue 後為 685 entries：
  437 `active`、75 `operator_only`、22 `retired_410`、151 `review_required`；
  generic placeholder 由 34 降為 33；queue focused tests 7 passed。
- Focused integrated verification：`passed`，78 passed／2 skipped；writer governance tests另為 4 passed，
  `git diff --check` passed。skipped 項是需要 disposable MySQL 的既有條件式驗收，未被改寫成 passed。

### 13.2 仍阻止 Task 97 完成的 required blockers

1. WP1 Client entry 已退役，但兩條 410 仍缺 external／operator caller evidence，因此只阻擋
   日後的實體刪除，不影響 raw GET 與 direct mutation 已停止的結論。下一個取證動作是取得
   可用的 API access log／external integration registry 與 operator 簽核，不由 repo 無 static caller 推導刪除。
2. WP2 repository commit：397 candidates 尚未完成逐項語意分類。一次 Luna high commit review 超過 hard
   lease後已中止，未採納其未完成推論；不得以 receiver 名稱的機械分類宣稱全 repository 已收斂。
3. WP3 Media failure semantics：current code 仍缺 reference-aware promotion、reconciler 與 orphan GC；
   直接刪除 content-addressed object 可能破壞其他 metadata 共用的同 digest bytes。
   `D97-MEDIA-STAGING` 已提供規格 Authority，但若實作需新 schema，仍必須另過 DB 七 gate。
4. WP4 尚有 54 個 raw-dict candidates。Client／Staff Payments、Matching、Orders、Access／Admin 等 active
   或待裁決 entry 尚未全部完成 caller、owner、bounded Query 與 React consumer 驗證。
5. WP5 writer final gate：合法 schema validator 已通過，但 final dispositions 仍為 676
   `retain_canonical`、92 `retain_restricted`、11 `migrate_then_remove`、794 `needs_decision`。唯讀治理審查
   已覆蓋 665 個 runtime 與 129 個 scripts findings，並確認跨域 AdminCommand、HCM／BeClass、anomaly
   outbox、Service Day owner，以及 production-capable migration／import scripts 不能批次洗白；審查 aggregate
   因 commit lane hard-lease interruption 為 failed receipt，只保存 completed lane evidence，不形成 integration
   Authority。
6. WP6B／WP7：current queue 仍有 151 `review_required`，另有 33 筆已標 active 但使用 generic owner／scenario／
   operator placeholder。WP7 未完成前不得把 README 的歷史 release snapshot 改寫成 current terminal 結論。
7. WP8 prerequisites 未滿足；未執行 fresh clone、full Python、full React、Browser／runtime 或任何 DB apply。
   本輪沒有 schema／migration Authority，也沒有操作 `union_db`／production。

### 13.3 Current gate table

| Gate | 結果 | Current evidence／blocker |
|---|---|---|
| Domain dependency | not_run | WP8 fresh-clone dependency scan 尚未執行 |
| Subsystem dependency | not_run | WP8 integrated dependency scan 尚未執行 |
| Route transaction | passed | current 6 candidates 均為唯讀 SQL；Clients PUT 已退役且 route 無 transaction |
| Repository commit | blocked | bounded slices passed，但 397 candidates 尚未完成語意分類 |
| Typed API | blocked | 54 raw-dict candidates 尚存；已完成 slices 另有獨立 verifier receipt |
| Writer inventory | blocked | validators passed，但 794 `needs_decision` |
| Entry governance | blocked | 151 `review_required`＋33 generic placeholders |
| Legacy retirement | blocked | Client GET／PUT 已 typed 410；external evidence 仍阻擋實體刪除，其他 legacy entries 未收斂 |
| Python tests | passed | focused integrated 78 passed／2 skipped；writer governance 4 passed；queue 3 passed |
| React tests/build | passed | current Task 97 candidate 未改 React；先前 focused 3 tests與 build passed |
| DB gates | NOT_RUN | 本輪未獲 schema／migration／DB apply Authority |
| Browser/runtime | not_run | current completion prerequisites 尚未滿足 |

Current conclusion：`ARCHITECTURE_COMPLIANCE_NOT_CONFIRMED`。

### 13.4 Final-candidate current checkpoint（Task 97 integration writer）

#### 七項 exact disposition 與修改／驗證

| Package | Current disposition | Exact implementation／evidence | Focused verification／blocker |
|---|---|---|---|
| Clients GET／PUT | `retired_410`；實體刪除 `blocked_external_evidence` | `api/routes/clients.py` 已無 raw table Query、SQL、connection、commit／rollback；GET replacement 是 bounded Data Browser clients projection，PUT replacement 是 HCM whole-source resubmission Preview／Apply。兩條 runtime 仍掛載 typed 410，queue 保存 stable code、replacement 與 deletion gate。 | Client retirement tests passed。repo 內 React／Streamlit／script／worker／CLI／runbook／deployment caller 為 0；external access log／integration registry 未取得，因此不可實體刪除。 |
| Media | `partial_blocked_schema` | immutable digest staging、可設定 24h operational TTL、commit failure 保留 bytes、post-completion integrity finalize，以及 bounded／dry-run／reference-and-lease-aware GC receipt 已實作於 Controlled Files。 | 48 focused tests passed。缺 durable post-commit intent、reference／lease SSOT 與 `scheduling_service_day_log_attachments` → controlled-file bridge 的 additive release；不把 in-memory replay 或 LINE legacy path冒充 runtime 完成。 |
| HCM／BeClass | `bounded_reconciliation_passed`; historical import guard 局部 blocked | `CaseImportReconciliationApplication` 擁有 outer UoW；adapter 借用 caller connection，Orders 只經 typed command。Client／Staff BeClass 不可達 direct SQL legacy writer 已移除；CLI 預設 rehearsal，guard 未完整時 apply fail closed。 | 41 reconciliation tests passed，涵蓋同 UoW、rollback、replay、stale zero-write、adapter／repository 無 commit。舊 HCM Current claim／root／receipt split 及 operator guard仍是 exact blocker。 |
| Anomaly／outbox | `contract_passed_runtime_blocked_schema` | 新 current-only typed Domain 與 Application contract固定 owner mutation＋intent、projection reconcile＋intent complete 的原子邊界；沒有 anomaly-specific claim／delivery adapter。 | 24 focused tests passed。current schema／release、15-code owner predicate adapters、legacy occurrence／history／checkpoint cutover及通用 durable-job replacement尚缺。 |
| Service Day Log | `keep-owner: Scheduling` | writer owner patterns已把 `service_day_*` repositories及 checkpoint notification source歸 Scheduling；Staff Operations只保留 actor／entry，LINE只保留 identity／media transport／delivery。 | zero-drift scan passed；Media attachment persistence缺口不重開 owner。 |
| Production scripts | inventory current，overall `blocked` | `task97_production_script_inventory_v1.json`涵蓋88個CLI：38 `keep-operator-only`、28 `rewrite-to-canonical-runner`、2 `delete-executable`、10 `test-only`、10 `blocked-caller-evidence`。三支 Orders migration 的 canonical subprocess caller 已鎖定；Admin、Case bootstrap、Leave substitution、Assignment DDL、BeClass／historical apply均在缺契約時 fail closed。 | 4 inventory tests、package A 34 tests及 corrected package B 11 tests passed。仍缺28支 canonical absorption、10支 caller evidence，且多數 retained operator entry未滿完整 guard contract。 |
| Entry governance | current inventory，terminal governance `blocked` | tracked queue current為685 entries：437 active、74 operator-only、22 retired-410、152 legacy `review_required`；generic placeholder為0。Task97逐列terminal disposition為437 `active_canonical`、74 `operator_only_guarded`、22 `retired_410`、147 `blocked_external_evidence`、5 `rewrite_to_canonical`；所有 required欄位明確填 `none`／`not evidenced`，不使用 null 冒充證據。 | 5 queue tests passed，685列均有terminal receipt；其中152張receipt結果為`blocked`而非成功。5條 anomaly recovery必須改接通用 durable job，其餘 caller／owner／replacement／cutover evidence缺口逐列局部阻擋。 |

#### Current global scans

- WP0 post-payments observed values：active mutation 282、repository commit candidates 382、route SQL
  transaction candidates 2、raw-dict API candidates 47；queue identity drift 0、generic placeholder 0。
- Route commit／rollback scan：`passed`，`api/routes/` 為 0。先前 `runtime_health.py::_mutate` 是無 caller 的
  hidden route transaction helper，已移除；40個 runtime-alert focused tests passed。現存2個 route SQL candidates
  是 query，但 contract-signing／payments等仍須完成 bounded typed repository extraction。
- Dependency AST scan：`domains/` 對 FastAPI／Streamlit／PyMySQL／API／Infrastructure 為0違規；
  `subsystems/` 對 FastAPI／Streamlit／UI為0違規。
- Writer v3：1540 identities、unresolved 0；Task97 exact symbol review後 final dispositions為685
  `retain_canonical`、96 `retain_restricted`、9 `migrate_then_remove`、750 `needs_decision`。validator只證明完整覆蓋，750列仍阻擋
  global writer gate。
- Raw-dict：Client Finance與Staff Payables兩組 public GET已改為authenticated bounded typed Query；Client Finance
  使用canonical account／obligation／ledger／allocation roots並以decimal-string版本輸出，Staff Payables明確標為
  legacy compatibility projection。current raw-dict candidates由54降為47，仍不能外推為 typed API passed。

#### Current tests and execution gates

| Gate | Result | Current evidence／exact blocker |
|---|---|---|
| Domain dependency | passed | final AST scan 0 violations |
| Subsystem dependency | passed | final AST scan 0 violations |
| Route transaction | passed | route commit／rollback 0；2 SQL candidates均無 route transaction ownership |
| Repository commit | blocked | bounded Task97 paths passed；382 discovery candidates尚未全部取得語意 disposition |
| Typed API | blocked | Client／Staff payments slice passed；47 raw-dict candidates尚存 |
| Writer inventory | blocked | generator／validator passed；750 `needs_decision` |
| Entry governance | blocked | generic placeholder 0；685列有terminal disposition／receipt，但152列receipt結果為blocked |
| Legacy retirement | blocked | Clients raw contract已停；external deletion evidence與其他 legacy cutover未全數完成 |
| Focused Python | passed | final integrated Task97 set 164 passed（含 payments typed Query、inventory與runtime alert） |
| Full Python | failed | final rerun為4804 passed、150 skipped、3 xfailed、63 failed、12 DB setup errors；錯誤含缺 disposable MySQL credentials，失敗另含既有 evidence／fixture／React-cutover drift及仍未收斂的Task97 gates |
| React tests | failed | 1200 passed、35 failed；6 test files失敗，含 invalid localStorage test environment與Scheduling／Orders cutover drift |
| React build | passed | TypeScript＋Vite production build passed；large-chunk warning保留 |
| React lint | passed | 0 errors；7 existing warnings |
| DB gates | BLOCKED | 未提供 allowlisted `lu_test_*` credentials；沒有操作 `union_db`／production或套用schema |
| Browser/runtime | not_run | 尚非可宣稱 cutover 的 final candidate，且無 deployment authority |

Current conclusion remains：`ARCHITECTURE_COMPLIANCE_NOT_CONFIRMED`。單一 external／operator evidence缺口只
阻擋該 entry，但 Media／Anomaly schema、47 raw-dict、382 commit candidates、750 writer decisions及152 entry
blocked receipts仍是互相獨立的 required blockers；不得把 focused pass、build pass或placeholder歸零外推成
Task 97整體完成。
